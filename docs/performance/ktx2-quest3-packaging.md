# Quest 3 KTX2 / Basis packaging

Concrete recipe for turning a DCC **glTF 2.0 GLB** into a Quest 3–safe package. Gate: [quest-3-target](../shipping/quest-3-target.md) — **90 Hz** ship / **72 Hz** fallback; never require 207/240 Hz. Soft caps: ≲100 draws, ≲750k tris/eye. Props: textures **≤1024²** (LOD0 / LOD1 / LOD2 may be **unlit/basic** or `KHR_materials_unlit` **without** a `baseColorTexture`, or a tiny 1×1 / vertex color — no `normalTexture`, no ORM texture). **No 4K.**

This page is the missing “how.” Policy lives in [photoreal-realtime](photoreal-realtime.md) and [content-pipeline](../../studio/content-pipeline.md) step 5. Identity stays on the same `objectId` ([additive-object-iteration](../../studio/additive-object-iteration.md)).

`crate-toolbox` today still **renders procedural color-only MeshBasic** (no albedo canvases; LOD0 / LOD1 / LOD2 unlit, no `normalMap`, no ORM). Drop a packaged GLB at the URL the example probes (`/packaged/crate-toolbox.glb`) and the runtime prefers it. The loader does **not** strip, downsample, or rewrite materials at ingest — author LOD0, LOD1, and LOD2 as unlit/basic (or `KHR_materials_unlit`) **without** a `baseColorTexture` (or a tiny 1×1 / vertex color). Do not invent headset ms after packaging — fill [quest-3-on-device-qa](../shipping/quest-3-on-device-qa.md) on a headset.

## Before you run a compressor

1. Export **one GLB** (y-up, 1 unit = 1 m). Visual meshes and `collider_*` hulls are **different nodes**. Do not merge the grab hull into the hero batch.
2. Keep **LOD0 / LOD1 / LOD2** groups if they already exist. Packaging must not flatten LODs into a single draw soup. Name those groups **`lod0` / `lod1` / `lod2`** (case-insensitive; `lod_0` / `lod-0` also match) or tag `userData.lodLevel` 0/1/2 — the same convention procedural `lodGroup()` uses. crate-toolbox **v0.36** wires those nodes into `userData.lod` and shows only one level; if none exist, ingest fails soft (all authored visuals stay visible — do not invent fake LODs). Keep `collider_*` and `fastener` / `fastenerMesh` **outside** LOD groups. Within each `lod*` group, merge **static** meshes that share a material (crate-toolbox **v0.37** does this on the procedural path: LOD0 14 → 6 draws, LOD1 8 → 4; tris unchanged; **v0.38** runs the same helper at packaged ingest on each discovered `lod*` node). After concat, **weld coincident vertices** (crate-toolbox **v0.39**; unique verts 440 → 230 / 192 → 100; prefer DCC pre-weld, runtime weld is a safety net). After weld, **strip unused `uv` / `normal`** on color-only unlit/basic (crate-toolbox **v0.40**; attrBytes 8800 → 4200 / 3776 → 1776 / 1680 → 720; author may omit unused attrs in DCC). After that, **compact a lingering Uint32 index to Uint16** when verts fit (crate-toolbox **v0.41**; LOD attrBytes stay 4200 / 1776 / 720 — already Uint16 after weld; fastener unused-attr strip + compact 840 → 360, still outside LOD merge). Procedural **v0.42** shares identical color-only MeshBasic instances across LOD levels when the midtone hex matches (unique instances 6 → 3); packaged ingest does **not** hex-dedupe — **share glTF material slots across LOD levels in DCC**. After GPU upload, **v0.43** releases CPU typed arrays on color-only unlit MeshBasic packed geos (`StaticDrawUsage` + r170 `onUpload`; colliders keep arrays). After Uint16 compact and before that upload hook, **v0.44** quantizes Float32 `position` to Three r170 `Float16BufferAttribute` (WebGL2 `HALF_FLOAT`) on those same color-only geos (attrBytes 4200 → 2820 / 1776 → 1176 / 720 → 432; fastener 360 → 216; colliders stay Float32). **v0.45** freezes `matrixAutoUpdate` on static packed color-only MeshBasic body leaves after one `updateMatrixWorld(true)` (lid/latch/tool/fastener stay live). **v0.46** disables `Mesh.raycast` on packed color-only MeshBasic visuals (body + lid/latch/tool + fastener; colliders keep default). **v0.47** pins `fog = false` and `toneMapped = false` on those packed color-only unlit MeshBasic materials (does not hex-dedupe or invent materials; mapped/lit/colliders stay r170 defaults). **v0.48** also pins opaque FrontSide draw-state (`transparent = false`, `opacity = 1`, `depthWrite = true`, `depthTest = true`, `side = FrontSide`) on those same materials (mapped/lit/colliders stay authored / r170 defaults). **v0.49** pins `castShadow = false` and `receiveShadow = false` on packed color-only MeshBasic visual meshes (mapped/lit/colliders stay authored / r170 Mesh defaults). **v0.50** pins `frustumCulled = true` on those same packed color-only MeshBasic visual meshes (mapped/lit/colliders stay authored / r170 Mesh defaults). **v0.51** also pins `blending = NormalBlending` / `premultipliedAlpha = false` / `alphaTest = 0` (companions `dithering = false` / `alphaToCoverage = false`) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). **v0.52** also pins `wireframe = false` / `colorWrite = true` / `depthFunc = LessEqualDepth` / `polygonOffset = false` (`polygonOffsetFactor = 0` / `polygonOffsetUnits = 0`) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). **v0.53** also pins r170 Material stencil defaults (`stencilWrite = false` / `AlwaysStencilFunc` / Keep ops) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). **v0.54** also pins r170 Material clipping defaults (`clippingPlanes = null` / `clipIntersection = false` / `clipShadows = false`) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). **v0.55** also pins remaining r170 Material boolean GPU-state defaults (`alphaHash = false` / `forceSinglePass = false`) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). **v0.56** also pins r170 NormalBlending factor/equation companions (`blendSrc = SrcAlphaFactor` / `blendDst = OneMinusSrcAlphaFactor` / `blendEquation = AddEquation` / `blendSrcAlpha = null` / `blendDstAlpha = null` / `blendEquationAlpha = null`) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). **v0.57** also pins r170 Material `vertexColors = false` on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). **v0.58** also pins r170 Material `precision = null` on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). **v0.59** also pins r170 Material `shadowSide = null` on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). **v0.60** also pins `renderOrder = 0` on packed color-only MeshBasic visual meshes (mapped/lit/colliders stay authored / r170 Mesh defaults). **v0.61** also pins r170 Material `visible = true` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not pin `mesh.visible`). **v0.62** also pins r170 MeshBasic envMap companions (`combine = MultiplyOperation` / `reflectivity = 1` / `refractionRatio = 0.98`) on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not force envMap or attach maps). **v0.63** also pins r170 MeshBasic map-intensity companions (`lightMapIntensity = 1` / `aoMapIntensity = 1`) on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not force lightMap / aoMap or attach maps). **v0.64** also pins r170 MeshBasic `wireframeLinewidth = 1` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not enable wireframe; does not pin `mesh.visible`). **v0.65** also pins r170 MeshBasic `wireframeLinecap = 'round'` / `wireframeLinejoin = 'round'` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not enable wireframe; does not pin `mesh.visible`). **v0.66** also pins r170 MeshBasic `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'` on packed color-only MeshBasic materials (keeps the existing Euler instance; mapped/lit/colliders stay authored / r170 defaults; does not force envMap or attach maps; does not enable wireframe; does not pin `mesh.visible`). **v0.67** also pins r170 Object3D layers default (layer 0 only / `mask = 1`) on packed color-only MeshBasic visual meshes (keeps the existing Layers instance; mapped/lit/colliders stay authored / r170 Mesh defaults; does not pin `mesh.visible`; does not change `matrixWorldAutoUpdate` / `matrixAutoUpdate`). **v0.68** also pins r170 Material CustomBlending color/alpha companions (`blendColor` `(0, 0, 0)` / `blendAlpha = 0`) on packed color-only unlit MeshBasic materials (keeps the existing Color instance; mapped/lit/colliders stay authored / r170 defaults; does not enable CustomBlending or change `blending` away from NormalBlending; does not pin `mesh.visible`). Do **not** merge across lid / latch / tool pivots. LOD0 may be **unlit/basic** (`KHR_materials_unlit` / Three `MeshBasicMaterial`) **without** a `baseColorTexture` (or tiny 1×1 / vertex color) — crate-toolbox v0.35 extends the v0.29/v0.30 Quest-far/mid color-only pattern to hero LOD. Mid and far LOD primitives should be **unlit** the same way — crate-toolbox v0.30 LOD1 and v0.29 LOD2. The runtime prefers a packaged GLB when present and does **not** strip, downsample, or rewrite materials at ingest. **v0.38** does merge same-material **direct mesh children** inside each discovered `lod*` group (same helper as procedural v0.37); **v0.39** welds coincident corners on that merged geometry; **v0.40** strips unused `uv` / `normal` on color-only MeshBasic after weld; **v0.41** compact lingering Uint32 indices to Uint16 when verts fit, and applies unused-attr strip + compact to an authored root-level fastener MeshBasic (do not invent one). **v0.42** is procedural-only material sharing; author shared glTF materials across LOD levels when midtones match. Author the GLB to this recipe; shared glTF materials get the ingest merge + weld + unused-attr strip + Uint16 compact + Float16 position quantize + post-upload CPU-array release (v0.44). After attach, static packed color-only MeshBasic body leaves freeze `matrixAutoUpdate` (v0.45; lid/latch/tool/fastener stay live). After that freeze, disable triangle `Mesh.raycast` on packed color-only visuals (v0.46; colliders keep default). After that disable, pin `fog = false` / `toneMapped = false` on packed color-only MeshBasic materials (v0.47; mapped/lit/colliders stay r170 defaults). After that pin, also pin opaque FrontSide draw-state on those same materials (v0.48; mapped/lit/colliders stay authored / r170 defaults). After that pin, also pin `castShadow = false` / `receiveShadow = false` on packed color-only MeshBasic visual meshes (v0.49; mapped/lit/colliders stay authored / r170 Mesh defaults). After that pin, also pin `frustumCulled = true` on packed color-only MeshBasic visual meshes (v0.50; mapped/lit/colliders stay authored / r170 Mesh defaults). After that pin, also pin `blending = NormalBlending` / `premultipliedAlpha = false` / `alphaTest = 0` on packed color-only MeshBasic materials (v0.51; mapped/lit/colliders stay authored / r170 defaults). After that pin, also pin `wireframe = false` / `colorWrite = true` / `depthFunc = LessEqualDepth` / `polygonOffset = false` on packed color-only MeshBasic materials (v0.52; mapped/lit/colliders stay authored / r170 defaults). After that pin, also pin r170 Material stencil defaults (`stencilWrite = false` / `AlwaysStencilFunc` / Keep ops) on packed color-only MeshBasic materials (v0.53; mapped/lit/colliders stay authored / r170 defaults). After that pin, also pin r170 Material clipping defaults (`clippingPlanes = null` / `clipIntersection = false` / `clipShadows = false`) on packed color-only MeshBasic materials (v0.54; mapped/lit/colliders stay authored / r170 defaults). After that pin, also pin remaining r170 Material boolean GPU-state defaults (`alphaHash = false` / `forceSinglePass = false`) on packed color-only MeshBasic materials (v0.55; mapped/lit/colliders stay authored / r170 defaults). Pin r170 NormalBlending factor/equation companions (`blendSrc = SrcAlphaFactor` / `blendDst = OneMinusSrcAlphaFactor` / `blendEquation = AddEquation` / null alpha companions) on packed color-only MeshBasic materials (v0.56; mapped/lit/colliders stay authored / r170 defaults). Pin `vertexColors = false` on those same packed color-only MeshBasic materials (v0.57; mapped/lit/colliders stay authored / r170 defaults). Pin `precision = null` on those same packed color-only MeshBasic materials (v0.58; mapped/lit/colliders stay authored / r170 defaults). Pin `shadowSide = null` on those same packed color-only MeshBasic materials (v0.59; mapped/lit/colliders stay authored / r170 defaults). Pin `renderOrder = 0` on packed color-only MeshBasic visual meshes (v0.60; mapped/lit/colliders stay authored / r170 Mesh defaults). Pin `visible = true` on packed color-only MeshBasic materials (v0.61; mapped/lit/colliders stay authored / r170 defaults; do not pin `mesh.visible`). Pin `combine = MultiplyOperation` / `reflectivity = 1` / `refractionRatio = 0.98` on packed color-only MeshBasic materials (v0.62; mapped/lit/colliders stay authored / r170 defaults; do not force envMap or attach maps). Also pin `lightMapIntensity = 1` / `aoMapIntensity = 1` on packed color-only MeshBasic materials (v0.63; mapped/lit/colliders stay authored / r170 defaults; do not force lightMap / aoMap or attach maps). Also pin `wireframeLinewidth = 1` on packed color-only MeshBasic materials (v0.64; mapped/lit/colliders stay authored / r170 defaults; do not enable wireframe; do not pin `mesh.visible`). Also pin `wireframeLinecap = 'round'` / `wireframeLinejoin = 'round'` on packed color-only MeshBasic materials (v0.65; mapped/lit/colliders stay authored / r170 defaults; do not enable wireframe; do not pin `mesh.visible`). **v0.66** also pins r170 MeshBasic `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'` (keeps the existing Euler instance; mapped/lit/colliders stay authored / r170 defaults; do not force envMap or attach maps; do not enable wireframe; do not pin `mesh.visible`). **v0.67** also pins r170 Object3D layers default (layer 0 only / `mask = 1`) on packed color-only MeshBasic visual meshes (keeps the existing Layers instance; mapped/lit/colliders stay authored / r170 Mesh defaults; do not pin `mesh.visible`; do not change `matrixWorldAutoUpdate` / `matrixAutoUpdate`). **v0.68** also pins r170 Material CustomBlending color/alpha companions (`blendColor` `(0, 0, 0)` / `blendAlpha = 0`) on packed color-only unlit MeshBasic materials (keeps the existing Color instance; mapped/lit/colliders stay authored / r170 defaults; does not enable CustomBlending or change `blending` away from NormalBlending; does not pin `mesh.visible`).
3. `gltf-transform inspect in.glb` — list texture slots and pixel sizes. If anything is >1024 on a handheld prop, resize **down**. Never upscale 256 → 512/1024 “for quality.” crate-toolbox v0.35 authors LOD0 as color-only unlit/basic (no `baseColorTexture`); do not smash a *larger* authored hero on another object with a global 256 resize if that object still ships 512. Author LOD0, LOD1, and LOD2 without a baseColorTexture before this pass.
4. Albedo is sRGB and unlit (no baked scene shadows). ORM / metallicRoughness is linear.

## Recipe (`gltf-transform` CLI)

Studio default: **npx** so the repo does not pin a global. Node LTS. Commands match [@gltf-transform/cli](https://gltf-transform.dev/) (`uastc`, `etc1s`, `resize`, `meshopt`).

```bash
# 0. Inspect (slot names + dimensions). Fail if a prop map is 4K.
npx --yes @gltf-transform/cli inspect authored.glb

# 1. Cap prop maps. crate-toolbox LOD0 / LOD1 / LOD2 may be
#    unlit/basic without a baseColorTexture (or a 1×1). Do not
#    use a global resize to smash a larger authored hero if that
#    object still ships 512; crate-toolbox v0.35 authors LOD0
#    color-only unlit/basic. Skip when every remaining
#    color/ORM/normal is already ≤ the cap.
npx --yes @gltf-transform/cli resize authored.glb resized.glb --width 1024 --height 1024

# 2. UASTC + zstd on normals and packed ORM (uncorrelated RGB — ETC1S blocks badly).
npx --yes @gltf-transform/cli uastc resized.glb step-uastc.glb \
  --slots "{normalTexture,occlusionTexture,metallicRoughnessTexture}" \
  --level 4 --rdo --rdo-lambda 4 --zstd 18

# 3. ETC1S on remaining color-like maps (albedo, emissive). Already-KTX2 slots are skipped.
npx --yes @gltf-transform/cli etc1s step-uastc.glb step-ktx2.glb --quality 200

# 4. Runtime mesh compression (decode off the XR frame — plaza / load, not rAF).
npx --yes @gltf-transform/cli meshopt step-ktx2.glb crate-toolbox.glb

# 5. Validate
npx --yes @gltf-transform/cli inspect crate-toolbox.glb
# plus https://github.khronos.org/glTF-Validator/ — fail CI on errors
```

If the inspect pass shows **no** normal/ORM slots (canvas-bake stand-in exported as albedo-only), step 2 no-ops; step 3 still wraps albedo in ETC1S. That is fine.

**Do not** also run Draco on the same mesh “just in case.” meshopt is the default decode. Draco (`gltf-transform draco`) is optional when **download** size dominates and you can decode before `requestSession`. Measure; do not stack both.

## What must stay in the GLB

| Keep | Drop / never |
| --- | --- |
| `lod0` / `lod1` / `lod2` (or `userData.lodLevel` 0/1/2). Runtime (`crate-toolbox` v0.36) wires these into `userData.lod` and draws only one level. Missing names = all visuals stay visible. Prefer one mesh per material inside each lod group (v0.37 procedural: LOD0 6 draws, LOD1 4; v0.38 packaged ingest merges shared-material children the same way). Pre-weld coincident corners in DCC; v0.39 also welds after concat as a safety net. Omit unused uv/normal on color-only unlit/basic; v0.40 also strips them after weld as a safety net. Prefer Uint16 indices when verts fit; v0.41 also compact lingering Uint32 as a safety net. Share glTF material slots across LOD levels when midtones match (v0.42 procedural sharing; packaged ingest does not hex-dedupe). Keep fastener outside LOD groups; v0.41 still strips + compact an authored fastener MeshBasic; v0.43 also hooks post-upload CPU-array release on packed color-only MeshBasic (not colliders); v0.44 also quantizes Float32 position to Float16 on those packed geos (not colliders); v0.45 also freezes `matrixAutoUpdate` on static packed color-only MeshBasic body leaves (lid/latch/tool/fastener stay live); v0.46 also disables `Mesh.raycast` on packed color-only visuals (colliders keep default); v0.47 also pins `fog = false` / `toneMapped = false` on those packed color-only MeshBasic materials (mapped/lit/colliders stay r170 defaults); v0.48 also pins opaque FrontSide draw-state on those same materials (mapped/lit/colliders stay authored / r170 defaults); v0.49 also pins `castShadow = false` / `receiveShadow = false` on packed color-only MeshBasic visual meshes (mapped/lit/colliders stay authored / r170 Mesh defaults); v0.50 also pins `frustumCulled = true` on packed color-only MeshBasic visual meshes (mapped/lit/colliders stay authored / r170 Mesh defaults); v0.51 also pins `blending = NormalBlending` / `premultipliedAlpha = false` / `alphaTest = 0` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults); v0.52 also pins `wireframe = false` / `colorWrite = true` / `depthFunc = LessEqualDepth` / `polygonOffset = false` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults); v0.53 also pins r170 Material stencil defaults (`stencilWrite = false` / `AlwaysStencilFunc` / Keep ops) on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults); v0.54 also pins r170 Material clipping defaults (`clippingPlanes = null` / `clipIntersection = false` / `clipShadows = false`) on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults); v0.55 also pins remaining r170 Material boolean GPU-state defaults (`alphaHash = false` / `forceSinglePass = false`) on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults); v0.56 also pins r170 NormalBlending factor/equation companions (`blendSrc = SrcAlphaFactor` / `blendDst = OneMinusSrcAlphaFactor` / `blendEquation = AddEquation` / null alpha companions) on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults); v0.57 also pins `vertexColors = false` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults); v0.58 also pins `precision = null` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults); v0.59 also pins `shadowSide = null` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults); v0.60 also pins `renderOrder = 0` on packed color-only MeshBasic visual meshes (mapped/lit/colliders stay authored / r170 Mesh defaults); v0.61 also pins `visible = true` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not pin `mesh.visible`); v0.62 also pins `combine = MultiplyOperation` / `reflectivity = 1` / `refractionRatio = 0.98` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not force envMap or attach maps); v0.63 also pins `lightMapIntensity = 1` / `aoMapIntensity = 1` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not force lightMap / aoMap or attach maps); v0.64 also pins `wireframeLinewidth = 1` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not enable wireframe; does not pin `mesh.visible`); v0.65 also pins `wireframeLinecap = 'round'` / `wireframeLinejoin = 'round'` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not enable wireframe; does not pin `mesh.visible`); v0.66 also pins `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'` on packed color-only MeshBasic materials (keeps the existing Euler instance; mapped/lit/colliders stay authored / r170 defaults; does not force envMap or attach maps; does not enable wireframe; does not pin `mesh.visible`); v0.67 also pins Object3D layers default (layer 0 only / `mask = 1`) on packed color-only MeshBasic visual meshes (keeps the existing Layers instance; mapped/lit/colliders stay authored / r170 Mesh defaults; does not pin `mesh.visible`; does not change `matrixWorldAutoUpdate` / `matrixAutoUpdate`); v0.68 also pins r170 Material CustomBlending color/alpha companions (`blendColor` `(0, 0, 0)` / `blendAlpha = 0`) on packed color-only unlit MeshBasic materials (keeps the existing Color instance; mapped/lit/colliders stay authored / r170 defaults; does not enable CustomBlending or change `blending` away from NormalBlending; does not pin `mesh.visible`) | 4K PNG/JPEG, mipmaps-off; parenting `collider_*` or fastener under an LOD group; merging lid/latch/tool into the body batch |
| `collider_grab` / `collider_latch` / `collider_lid` / `collider_tool` / `collider_fastener` as **separate** nodes | Using the hero mesh as the pick target |
| Sidecar `behavior.json` next to the GLB (ADR 0004) | Baking latch state into `KHR_materials_variants` |
| Power-of-two KTX2 with mips (`KHR_texture_basisu`) | Desktop-only WebP/PNG “it looks finer in Chrome” |

Copy the result to `assets/objects/<objectId>/current.glb` **and** `revisions/<semver>/` (Git LFS). The interactive-prop drop folder is `examples/interactive-prop/public/packaged/` (see README there).

## Runtime (Three.js)

`GLTFLoader` + `KTX2Loader` (Basis transcoder) + optional `MeshoptDecoder`. After load: hide `collider_*`, attach the sidecar, do not raycast hero meshes. Discover `lod0` / `lod1` / `lod2` (or `userData.lodLevel`) and attach the same `userData.lod` shape as the procedural crate, then `setToolboxLod(..., 0)`. The example implements this in `packaged-visual.js`: **probe URL → load if present → wire LOD visibility if named groups exist → merge same-material meshes inside each lod* group (v0.38), weld coincident vertices (v0.39), strip unused uv/normal on color-only MeshBasic (v0.40), compact lingering Uint32 indices to Uint16 (v0.41; authored fastener MeshBasic gets strip + compact too), quantize Float32 position to Float16 (v0.44), hook post-upload CPU-array release on those packed color-only geos (v0.43; not colliders), freeze `matrixAutoUpdate` on static packed color-only MeshBasic body leaves (v0.45; lid/latch/tool/fastener stay live), disable `Mesh.raycast` on packed color-only visuals (v0.46; colliders keep default), and pin `fog = false` / `toneMapped = false` on those packed color-only MeshBasic materials (v0.47; mapped/lit/colliders stay r170 defaults), then pin opaque FrontSide draw-state on those same materials (v0.48; mapped/lit/colliders stay authored / r170 defaults), then pin `castShadow = false` / `receiveShadow = false` on packed color-only MeshBasic visual meshes (v0.49; mapped/lit/colliders stay authored / r170 Mesh defaults), then pin `frustumCulled = true` on packed color-only MeshBasic visual meshes (v0.50; mapped/lit/colliders stay authored / r170 Mesh defaults), then pin `blending = NormalBlending` / `premultipliedAlpha = false` / `alphaTest = 0` on those same materials (v0.51; mapped/lit/colliders stay authored / r170 defaults), then pin `wireframe = false` / `colorWrite = true` / `depthFunc = LessEqualDepth` / `polygonOffset = false` on those same materials (v0.52; mapped/lit/colliders stay authored / r170 defaults), then pin r170 Material stencil defaults (`stencilWrite = false` / `AlwaysStencilFunc` / Keep ops) on those same materials (v0.53; mapped/lit/colliders stay authored / r170 defaults), then pin r170 Material clipping defaults (`clippingPlanes = null` / `clipIntersection = false` / `clipShadows = false`) on those same materials (v0.54; mapped/lit/colliders stay authored / r170 defaults), then pin remaining r170 Material boolean GPU-state defaults (`alphaHash = false` / `forceSinglePass = false`) on those same materials (v0.55; mapped/lit/colliders stay authored / r170 defaults); r170 NormalBlending factor/equation companions (`blendSrc = SrcAlphaFactor` / `blendDst = OneMinusSrcAlphaFactor` / `blendEquation = AddEquation` / null alpha companions) on those same materials (v0.56; mapped/lit/colliders stay authored / r170 defaults), pin `vertexColors = false` on those same materials (v0.57; mapped/lit/colliders stay authored / r170 defaults), pin `precision = null` on those same materials (v0.58; mapped/lit/colliders stay authored / r170 defaults), pin `shadowSide = null` on those same materials (v0.59; mapped/lit/colliders stay authored / r170 defaults), pin `renderOrder = 0` on packed color-only MeshBasic visual meshes (v0.60; mapped/lit/colliders stay authored / r170 Mesh defaults), pin `visible = true` on packed color-only MeshBasic materials (v0.61; mapped/lit/colliders stay authored / r170 defaults; does not pin `mesh.visible`), pin r170 MeshBasic envMap companions (`combine = MultiplyOperation` / `reflectivity = 1` / `refractionRatio = 0.98`) on packed color-only MeshBasic materials (v0.62; mapped/lit/colliders stay authored / r170 defaults; does not force envMap or attach maps); v0.63 also pins `lightMapIntensity = 1` / `aoMapIntensity = 1` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not force lightMap / aoMap or attach maps); v0.64 also pins `wireframeLinewidth = 1` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not enable wireframe; does not pin `mesh.visible`); v0.65 also pins `wireframeLinecap = 'round'` / `wireframeLinejoin = 'round'` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not enable wireframe; does not pin `mesh.visible`); v0.66 also pins `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'` on packed color-only MeshBasic materials (keeps the existing Euler instance; mapped/lit/colliders stay authored / r170 defaults; does not force envMap or attach maps; does not enable wireframe; does not pin `mesh.visible`); v0.67 also pins Object3D layers default (layer 0 only / `mask = 1`) on packed color-only MeshBasic visual meshes (keeps the existing Layers instance; mapped/lit/colliders stay authored / r170 Mesh defaults; does not pin `mesh.visible`; does not change `matrixWorldAutoUpdate` / `matrixAutoUpdate`); v0.68 also pins r170 Material CustomBlending color/alpha companions (`blendColor` `(0, 0, 0)` / `blendAlpha = 0`) on packed color-only unlit MeshBasic materials (keeps the existing Color instance; mapped/lit/colliders stay authored / r170 defaults; does not enable CustomBlending or change `blending` away from NormalBlending; does not pin `mesh.visible`) → else procedural color-only MeshBasic.** v0.42 shares MeshBasic instances on the procedural path only; packaged ingest does not hex-dedupe. v0.43 hooks StaticDrawUsage + onUpload CPU-array release on packed color-only MeshBasic (LOD merge helper + authored fastener; not colliders). v0.44 quantizes those packed positions to Float16 before the upload hook. v0.45 freezes `matrixAutoUpdate` on static packed color-only MeshBasic body leaves after attach (lid/latch/tool/fastener stay live). v0.46 disables `Mesh.raycast` on packed color-only visuals after that freeze (colliders keep default). v0.47 pins `fog = false` / `toneMapped = false` on those packed color-only MeshBasic materials after that disable (mapped/lit/colliders stay r170 defaults). v0.48 also pins opaque FrontSide draw-state on those same materials (mapped/lit/colliders stay authored / r170 defaults); v0.49 also pins `castShadow = false` / `receiveShadow = false` on packed color-only MeshBasic visual meshes (mapped/lit/colliders stay authored / r170 Mesh defaults). v0.50 also pins `frustumCulled = true` on packed color-only MeshBasic visual meshes (mapped/lit/colliders stay authored / r170 Mesh defaults). v0.51 also pins `blending = NormalBlending` / `premultipliedAlpha = false` / `alphaTest = 0` on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). v0.52 also pins `wireframe = false` / `colorWrite = true` / `depthFunc = LessEqualDepth` / `polygonOffset = false` on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). v0.53 also pins r170 Material stencil defaults (`stencilWrite = false` / `AlwaysStencilFunc` / Keep ops) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). v0.54 also pins r170 Material clipping defaults (`clippingPlanes = null` / `clipIntersection = false` / `clipShadows = false`) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). v0.55 also pins remaining r170 Material boolean GPU-state defaults (`alphaHash = false` / `forceSinglePass = false`) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). v0.56 also pins r170 NormalBlending factor/equation companions (`blendSrc = SrcAlphaFactor` / `blendDst = OneMinusSrcAlphaFactor` / `blendEquation = AddEquation` / `blendSrcAlpha = null` / `blendDstAlpha = null` / `blendEquationAlpha = null`) on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). v0.57 also pins r170 Material `vertexColors = false` on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). v0.58 also pins r170 Material `precision = null` on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). v0.59 also pins r170 Material `shadowSide = null` on those same packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults). v0.60 also pins `renderOrder = 0` on packed color-only MeshBasic visual meshes (mapped/lit/colliders stay authored / r170 Mesh defaults). v0.61 also pins `visible = true` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not pin `mesh.visible`). v0.62 also pins r170 MeshBasic envMap companions (`combine = MultiplyOperation` / `reflectivity = 1` / `refractionRatio = 0.98`) on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not force envMap or attach maps). v0.63 also pins r170 MeshBasic map-intensity companions (`lightMapIntensity = 1` / `aoMapIntensity = 1`) on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not force lightMap / aoMap or attach maps). v0.64 also pins r170 MeshBasic `wireframeLinewidth = 1` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not enable wireframe; does not pin `mesh.visible`). v0.65 also pins r170 MeshBasic `wireframeLinecap = 'round'` / `wireframeLinejoin = 'round'` on packed color-only MeshBasic materials (mapped/lit/colliders stay authored / r170 defaults; does not enable wireframe; does not pin `mesh.visible`). v0.66 also pins r170 MeshBasic `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'` on packed color-only MeshBasic materials (keeps the existing Euler instance; mapped/lit/colliders stay authored / r170 defaults; does not force envMap or attach maps; does not enable wireframe; does not pin `mesh.visible`). v0.67 also pins r170 Object3D layers default (layer 0 only / `mask = 1`) on packed color-only MeshBasic visual meshes (keeps the existing Layers instance; mapped/lit/colliders stay authored / r170 Mesh defaults; does not pin `mesh.visible`; does not change `matrixWorldAutoUpdate` / `matrixAutoUpdate`). v0.68 also pins r170 Material CustomBlending color/alpha companions (`blendColor` `(0, 0, 0)` / `blendAlpha = 0`) on packed color-only unlit MeshBasic materials (keeps the existing Color instance; mapped/lit/colliders stay authored / r170 defaults; does not enable CustomBlending or change `blending` away from NormalBlending; does not pin `mesh.visible`). Missing LOD names do not crash and do not invent groups.

## Quest 3 sign-off

Packaging is not a 90 Hz pass. After a real GLB lands, run [quest-3-on-device-qa](../shipping/quest-3-on-device-qa.md). Leave the results table blank until a headset run. Do not copy desktop rAF into that table.
