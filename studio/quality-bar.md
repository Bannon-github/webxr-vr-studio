# Quality bar — definition of done

An immersive feature or release is **done** only when all applicable boxes pass.

**Gate device:** Meta Quest 3 + Meta Quest Browser @ **90 Hz** (72 Hz fallback). Details and studio budgets: [quest-3-target](../docs/shipping/quest-3-target.md). Desktop/emulator is smoke, not the gate. 120 Hz is stretch; 207/240 Hz is out of scope.

## XR correctness

- [ ] Secure context verified on headset URL
- [ ] isSessionSupported gates the Enter VR CTA
- [ ] requestSession only on user gesture
- [ ] Tracking loss handled (null poses) (`crate-toolbox` v0.10: held crate/tool `endGrab` on null grip/ray/joint pose or removed input source)
- [ ] Visibility loss handled (session `hidden` / `visible-blurred`, or `document.hidden` while presenting) (`crate-toolbox` v0.11: same `endGrab`; no auto-regrab on restore)
- [ ] Session end restores 2D UI; re-entry works
- [ ] Controllers: rays, select, squeeze as designed
- [ ] Reference space fallback chain works

## Comfort

- [ ] Default locomotion is Tier A (or justified ADR exception)
- [ ] Settings expose turn and loco preferences
- [ ] Comfort-sensitive tester signed off
- [ ] No forced artificial pitch

## Performance

- [ ] Quest 3 holds **90 Hz** in the hero scene (≈11.1 ms); 72 Hz is a recorded fallback, not the happy path ([quest-3-target](../docs/shipping/quest-3-target.md))
- [ ] Thermal soak 10+ minutes on Quest 3 without collapse
- [ ] Hero view: draw calls ≲100; triangles ≲750k/eye soft ceiling; props prefer ≤1024² (max 2048²); KTX2 + mipmaps ([ktx2-quest3-packaging](../docs/performance/ktx2-quest3-packaging.md))
- [ ] FFR medium/high (`fixedFoveation` ≈0.5–1) when the UA exposes it
- [ ] No allocations / `new` in the XR frame loop (**frame-loop allocation scrub**: hoist scratch vectors; no per-frame arrays or `new THREE.*` on the present path when diagnostics are off — `crate-toolbox` v0.8)
- [ ] Present-path pixel ratio clamped to 1 while XR presenting; restore desktop ratio + `setSize` on session end (`crate-toolbox` v0.15)
- [ ] Present-path antialias / MSAA off while XR presenting (`crate-toolbox` v0.16: constructor `antialias: false` so Three’s `XRWebGLLayer` inherits it; WebGL attribute is immutable — do not invent a live flip)
- [ ] Present-path `NoToneMapping` while XR presenting; restore lookdev `ACESFilmicToneMapping` + prior `toneMappingExposure` on session end (`crate-toolbox` v0.17)
- [ ] Present-path IBL / `scene.environment` off while XR presenting; restore the saved PMREM + lookdev `environmentIntensity` on session end without disposing (`crate-toolbox` v0.18)
- [ ] Present-path directional / punctual off while XR presenting; restore lookdev `DirectionalLight` visible + intensity on session end (`crate-toolbox` v0.19)
- [ ] Present-path ambient-only fill while XR presenting (HemisphereLight off + one `AmbientLight` at intensity 0.4); restore lookdev hemi and disable/detach the present-only ambient on session end (`crate-toolbox` v0.20)
- [ ] Present-path texture anisotropy clamped to 1 while XR presenting; restore lookdev `.anisotropy` on session end (`crate-toolbox` v0.21)
- [ ] Present-path XR framebuffer scale factor clamped to 1 while XR presenting; restore lookdev/desktop scale on session end (`crate-toolbox` v0.22; Three r170 `setFramebufferScaleFactor` only — no getter; set while presenting does not rebuild the current layer)
- [ ] Texture/draw/tri budgets written on the object manifest `perf` block
- [ ] On-device matrix filled per [quest-3-on-device-qa](../docs/shipping/quest-3-on-device-qa.md) (Browser, firmware, SHA, requested Hz, FFR, method). Leave cells blank until a headset run — do not invent ms.

## Interactive assets

Applies when the user hovers, grabs, or drives a multi-step activity. Full path: [asset-to-interaction-workflow.md](asset-to-interaction-workflow.md). Architecture: [ADR 0004](adr/0004-asset-interaction-architecture.md).

- [ ] Visual mesh is not the grab/ray target (separate `collider_*` hulls)
- [ ] Hover / pressed / disabled / grabbed affordances readable under FFR
- [ ] Activity states named; illegal transitions nack; drop does not soft-lock
- [ ] Controllers: `select` = use, `squeeze` = grab; core loop works without hands
- [ ] Hand path (if advertised) is optional; pinch maps to the same intents; hover-before-pinch matches controller emissive (`crate-toolbox` v0.9)
- [ ] Texture / LOD / triangle class meets [photoreal-realtime](../docs/performance/photoreal-realtime.md) **on Quest 3** (L2/L3 inside [quest-3-target](../docs/shipping/quest-3-target.md); baked maps + LODs, not scan density)
- [ ] LOD2 far materials are unlit `MeshBasicMaterial` (or glTF `KHR_materials_unlit`) **without** a `baseColorTexture` (color-only, or tiny 1×1 / vertex color) — no `normalMap`, no ORM, no roughness/metalness uniforms (`crate-toolbox` v0.29; loader does not rewrite materials)
- [ ] LOD1 mid materials are unlit `MeshBasicMaterial` (or glTF `KHR_materials_unlit`) **without** a `baseColorTexture` (color-only, or tiny 1×1 / vertex color) — no `normalMap`, no ORM, no roughness/metalness uniforms (`crate-toolbox` v0.30; loader does not rewrite materials)
- [ ] LOD0 hero materials may be unlit/basic (or `KHR_materials_unlit`) **without** a `baseColorTexture` (color-only, or tiny 1×1 / vertex color) — no `normalMap`, no ORM, no roughness/metalness uniforms (`crate-toolbox` v0.35; loader does not rewrite materials)
- [ ] Packaged GLB ingest wires conventional `lod0` / `lod1` / `lod2` (or `userData.lodLevel`) into `userData.lod` and shows only one level; missing names fail soft without inventing fake LODs; colliders and fastener stay out of LOD groups (`crate-toolbox` v0.36; visibility-only switch)
- [ ] Procedural LOD0/LOD1 merge same-material meshes within each static lodGroup (do not merge across body / lid / latch / tool); duplicate same-color MeshBasics share one instance (`crate-toolbox` v0.37; measured LOD0 14 → 6 draws, LOD1 8 → 4; tris unchanged)
- [ ] Packaged GLB ingest merges same-material meshes within each discovered `lod0` / `lod1` / `lod2` group (same helper as procedural v0.37; direct mesh children; skip colliders / fastener / multi-material; do not merge across LOD levels); missing names still fail soft (`crate-toolbox` v0.38; unit/mock 3→1 draws, tris unchanged; no material rewrite)
- [ ] After same-material concat, weld coincident vertices (position hash 1e-4 m; keep UV/normal channels). Prefer DCC pre-weld; runtime weld is a safety net (`crate-toolbox` v0.39; unique verts 440 → 230 / 192 → 100; tris and draws unchanged; unit/mock 72 → 8)
- [ ] After weld, strip unused `uv` / `normal` (and other unused channels) from color-only unlit MeshBasic (no map / no lighting). Prefer omitting them in DCC; runtime strip is a safety net (`crate-toolbox` v0.40; attrBytes 8800 → 4200 / 3776 → 1776 / 1680 → 720; draws / tris / unique verts unchanged)
- [ ] After unused-attr strip, compact a lingering Uint32 index to Uint16 when `position.count` ≤ 65535 (concat always builds Uint32; weld only rewrites Uint16 when verts drop). Fastener stays outside LOD merge but gets the same strip + compact (`crate-toolbox` v0.41; LOD attrBytes stay 4200 / 1776 / 720; fastener 840 → 360)
- [ ] Procedural color-only unlit MeshBasic instances are shared across LOD levels when the midtone hex matches (one wood for LOD0/1/2, one brass for LOD0/1 + fastener, one steel). Packaged ingest does not hex-dedupe (`crate-toolbox` v0.42; unique MeshBasic 6 → 3; draws / tris / verts / attrBytes unchanged vs v0.41)
- [ ] After that pack, color-only unlit MeshBasic LOD/fastener geometries set `StaticDrawUsage` and Three r170 `onUpload` so the first GPU upload releases CPU `.array`. Colliders keep CPU arrays. Pre-upload attrBytes stay at the v0.42 envelope; post-upload CPU attrBytes → 0 (`crate-toolbox` v0.43)
- [ ] After Uint16 compact and before that `onUpload` hook, quantize Float32 `position` to Three r170 `Float16BufferAttribute` (WebGL2 `HALF_FLOAT`) on color-only unlit MeshBasic LOD/fastener geometries. Colliders stay Float32. Pre-upload attrBytes 4200 → 2820 / 1776 → 1176 / 720 → 432; fastener 360 → 216; post-upload CPU attrBytes → 0 (`crate-toolbox` v0.44)
- [ ] After that pack, freeze `matrixAutoUpdate` on static packed color-only unlit MeshBasic visual leaves that are not descendants of `lid` / `latch` / `tool` pivots (one `updateMatrixWorld(true)` first). Fastener and colliders stay live. Draws / tris / verts / attrBytes stay at the v0.44 envelope; measured 3 frozen / 10 live MeshBasic (`crate-toolbox` v0.45)
- [ ] After that freeze, disable `Mesh.raycast` on packed color-only unlit MeshBasic visual meshes (LOD body + lid/latch/tool + fastener) with a named no-op. Colliders keep `Mesh.prototype.raycast`. Draws / tris / verts / attrBytes / matrix freeze stay at the v0.45 envelope; measured 13 raycast-off / 5 collider default (`crate-toolbox` v0.46)
- [ ] After that raycast disable, pin `fog = false` and `toneMapped = false` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay r170 defaults. Draws / tris / verts / attrBytes / raycast-off / matrix freeze stay at the v0.46 envelope (`crate-toolbox` v0.47)
- [ ] After that flag pin, also pin opaque FrontSide draw-state (`transparent = false`, `opacity = 1`, `depthWrite = true`, `depthTest = true`, `side = FrontSide`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Draws / tris / verts / attrBytes / raycast-off / matrix freeze stay at the v0.47 envelope (`crate-toolbox` v0.48)
- [ ] After that opaque FrontSide pin, also pin `castShadow = false` and `receiveShadow = false` on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate). Mapped / lit / colliders stay authored / r170 Mesh defaults. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze stay at the v0.48 envelope (`crate-toolbox` v0.49)
- [ ] After that shadow-flag pin, also pin `frustumCulled = true` on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate). Mapped / lit / colliders stay authored / r170 Mesh defaults. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off stay at the v0.49 envelope (`crate-toolbox` v0.50)
- [ ] After that frustumCulled pin, also pin `blending = NormalBlending`, `premultipliedAlpha = false`, and `alphaTest = 0` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers; companions `dithering = false` / `alphaToCoverage = false`). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.50 envelope (`crate-toolbox` v0.51)
- [ ] After that blending/alpha pin, also pin `wireframe = false`, `colorWrite = true`, `depthFunc = LessEqualDepth`, and `polygonOffset = false` (`polygonOffsetFactor = 0` / `polygonOffsetUnits = 0`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.51 envelope (`crate-toolbox` v0.52)
- [ ] After that wireframe/depthFunc/colorWrite/polygonOffset pin, also pin r170 Material stencil defaults (`stencilWrite = false`, `stencilFunc = AlwaysStencilFunc`, `stencilRef = 0`, `stencilWriteMask = 0xff`, `stencilFuncMask = 0xff`, `stencilFail = KeepStencilOp`, `stencilZFail = KeepStencilOp`, `stencilZPass = KeepStencilOp`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.52 envelope (`crate-toolbox` v0.53)
- [ ] After that stencil pin, also pin r170 Material clipping defaults (`clippingPlanes = null`, `clipIntersection = false`, `clipShadows = false`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.53 envelope (`crate-toolbox` v0.54)
- [ ] After that clipping pin, also pin remaining r170 Material boolean GPU-state defaults (`alphaHash = false`, `forceSinglePass = false`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.54 envelope (`crate-toolbox` v0.55)
- [ ] After that alphaHash / forceSinglePass pin, also pin r170 NormalBlending factor/equation companions (`blendSrc = SrcAlphaFactor`, `blendDst = OneMinusSrcAlphaFactor`, `blendEquation = AddEquation`, `blendSrcAlpha = null`, `blendDstAlpha = null`, `blendEquationAlpha = null`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.55 envelope (`crate-toolbox` v0.56)
- [ ] After that NormalBlending companion pin, also pin r170 Material `vertexColors = false` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.56 envelope (`crate-toolbox` v0.57)
- [ ] After that vertexColors pin, also pin r170 Material `precision = null` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do not force `'mediump'`/`'lowp'`/`'highp'`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.57 envelope (`crate-toolbox` v0.58)
- [ ] After that precision pin, also pin r170 Material `shadowSide = null` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do not force a non-null shadowSide. Mesh-level `castShadow`/`receiveShadow` stay pinned false (v0.49). Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.58 envelope (`crate-toolbox` v0.59)
- [ ] After that shadowSide pin (and after the v0.50 frustumCulled / v0.49 shadow-flag pins), also pin `renderOrder = 0` on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate; sibling helpers `pinColorOnlyUnlitBasicRenderOrder` / `pinColorOnlyVisualRenderOrder`). Mapped / lit / colliders stay authored / r170 Mesh defaults. Do not pin `mesh.visible`, change `layers`, or force a non-zero renderOrder. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on stay at the v0.59 envelope (`crate-toolbox` v0.60)
- [ ] After that renderOrder pin, also pin r170 Material `visible = true` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do **not** pin `mesh.visible` (LOD visibility uses it). Do not force `material.visible = false`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 stay at the v0.60 envelope (`crate-toolbox` v0.61)
- [ ] After that Material `visible = true` pin, also pin r170 MeshBasic envMap companions (`combine = MultiplyOperation`, `reflectivity = 1`, `refractionRatio = 0.98`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do **not** change the map/envMap gate, force envMap, or attach maps. Do **not** pin `mesh.visible`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 stay at the v0.61 envelope (`crate-toolbox` v0.62)
- [ ] After that envMap companion pin, also pin r170 MeshBasic map-intensity companions (`lightMapIntensity = 1`, `aoMapIntensity = 1`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do **not** change the map / lightMap / aoMap gate, force lightMap / aoMap, or attach maps. Do **not** pin `mesh.visible`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 stay at the v0.62 envelope (`crate-toolbox` v0.63)
- [ ] After that map-intensity companion pin, also pin r170 MeshBasic `wireframeLinewidth = 1` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Keep `wireframe = false` (already pinned in v0.52). Do **not** enable wireframe. Do **not** pin `mesh.visible`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 stay at the v0.63 envelope (`crate-toolbox` v0.64)
- [ ] After that wireframeLinewidth pin, also pin r170 MeshBasic wireframe line-style defaults (`wireframeLinecap = 'round'`, `wireframeLinejoin = 'round'`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Keep `wireframe = false` (already pinned in v0.52). Do **not** enable wireframe. Do **not** pin `mesh.visible`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 stay at the v0.64 envelope (`crate-toolbox` v0.65)
- [ ] After that wireframe line-style pin, also pin r170 MeshBasic `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers; keep the existing Euler instance). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do **not** change the map/envMap gate, force envMap, or attach maps. Keep `wireframe = false` (already pinned in v0.52). Do **not** enable wireframe. Do **not** pin `mesh.visible`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 stay at the v0.65 envelope (`crate-toolbox` v0.66)
- [ ] After that envMapRotation pin (and after the v0.60 renderOrder pin), also pin r170 Object3D layers default (layer 0 only / `mask = 1`) on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate; sibling helpers `pinColorOnlyUnlitBasicLayers` / `pinColorOnlyVisualLayers`; keep the existing Layers instance). Mapped / lit / colliders stay authored / r170 Mesh defaults. Do **not** pin `mesh.visible` (LOD visibility uses it). Do **not** change `matrixWorldAutoUpdate` / `matrixAutoUpdate`. Do **not** enable extra camera/layers tricks or invent a custom layer mask. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 stay at the v0.66 envelope (`crate-toolbox` v0.67)
- [ ] After that layers pin (and after the v0.56 NormalBlending factor/equation companions), also pin r170 Material CustomBlending color/alpha companions (`blendColor` `(0, 0, 0)`, `blendAlpha = 0`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers; keep the existing Color instance). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do **not** enable CustomBlending or change `blending` away from NormalBlending. Do **not** pin `mesh.visible`. Do **not** change `layers` / `matrixWorldAutoUpdate` / `matrixAutoUpdate`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default stay at the v0.67 envelope (`crate-toolbox` v0.68)
- [ ] After that layers pin (and after the v0.68 blendColor / blendAlpha material pin), also pin r170 Object3D `matrixWorldAutoUpdate = true` (`Object3D.DEFAULT_MATRIX_WORLD_AUTO_UPDATE`) on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate; sibling helpers `pinColorOnlyUnlitBasicMatrixWorldAutoUpdate` / `pinColorOnlyVisualMatrixWorldAutoUpdate`). Mapped / lit / colliders stay authored / r170 Mesh defaults. Do **not** pin `mesh.visible` (LOD visibility uses it). Do **not** change `matrixAutoUpdate` (v0.45 already freezes static body LOD leaves). Do **not** change `layers` / `blendColor` / `blendAlpha`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default stay at the v0.68 envelope (`crate-toolbox` v0.69)
- [ ] After that matrixWorldAutoUpdate pin, also pin r170 Object3D `up` `(0, 1, 0)` (`Object3D.DEFAULT_UP`) on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate; sibling helpers `pinColorOnlyUnlitBasicUp` / `pinColorOnlyVisualUp`; keep the existing Vector3 instance). Mapped / lit / colliders stay authored / r170 Mesh defaults. Do **not** pin `mesh.visible` (LOD visibility uses it). Do **not** change `matrixAutoUpdate` (v0.45 already freezes static body LOD leaves). Do **not** change `matrixWorldAutoUpdate` / `layers` / `blendColor` / `blendAlpha`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on stay at the v0.69 envelope (`crate-toolbox` v0.70)
- [ ] After that up pin, also pin r170 Object3D `scale` `(1, 1, 1)` on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate; sibling helpers `pinColorOnlyUnlitBasicScale` / `pinColorOnlyVisualScale`; keep the existing Vector3 instance). Mapped / lit / colliders stay authored / r170 Mesh defaults. Do **not** pin `mesh.visible` (LOD visibility uses it). Do **not** change `matrixAutoUpdate` (v0.45 already freezes static body LOD leaves). Do **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `blendColor` / `blendAlpha`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default stay at the v0.70 envelope (`crate-toolbox` v0.71)
- [ ] After that scale pin (and after the v0.68 blendColor / blendAlpha / v0.53 stencil suite), also pin r170 Material `dithering = false` / `alphaToCoverage = false` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers; first-class measured flags after the v0.51 blending/alpha companions). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `blendColor` / `blendAlpha`. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default stay at the v0.71 envelope (`crate-toolbox` v0.72)
- [ ] After that scale pin (and after the v0.72 dithering / A2C material pin), also pin r170 Object3D `rotation.order = 'XYZ'` on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate; sibling helpers `pinColorOnlyUnlitBasicRotationOrder` / `pinColorOnlyVisualRotationOrder`; keep the existing Euler instance; do not rewrite `rotation.xyz`; do not touch `mesh.quaternion`). Mapped / lit / colliders stay authored / r170 Mesh defaults. Do **not** pin `mesh.visible` (LOD visibility uses it). Do **not** change `matrixAutoUpdate` (v0.45 already freezes static body LOD leaves). Do **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `blendColor` / `blendAlpha` / dithering / A2C. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default stay at the v0.72 envelope (`crate-toolbox` v0.73)
- [ ] After that rotation.order pin (and after the v0.72 dithering / A2C material pin), also pin r170 Material `polygonOffset = false` / `polygonOffsetFactor = 0` / `polygonOffsetUnits = 0` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers; first-class measured flags after the v0.52 GPU-state companions). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do **not** enable `polygonOffset`. Do **not** invent non-zero factors/units. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / `blendColor` / `blendAlpha` / dithering / A2C. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ stay at the v0.73 envelope (`crate-toolbox` v0.74)
- [ ] After that polygonOffset companion pin (and after v0.53 already assigned the full r170 Material stencil suite), also pin r170 Material `stencilRef = 0` / `stencilWriteMask = 0xff` / `stencilFuncMask = 0xff` / `stencilZFail = KeepStencilOp` / `stencilZPass = KeepStencilOp` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate and helpers; first-class measured flags after the v0.53 short-form `stencilWrite` / `stencilFunc` / `stencilFail` companions). Shared wood/brass/steel instances get the flags once. Mapped / lit / colliders stay authored / r170 defaults. Do **not** enable stencil write. Do **not** invent non-Always func / non-Keep ops / non-zero ref / non-0xff masks. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including polygonOffset companions / dithering / A2C / blendColor / blendAlpha. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ stay at the v0.74 envelope (`crate-toolbox` v0.75)
- [ ] After that stencil companion pin (and after v0.49 already pinned Mesh `castShadow = false` / `receiveShadow = false` and v0.59 pinned Material `shadowSide = null`), also clear leftover Mesh `customDepthMaterial` / `customDistanceMaterial` to the r170 Mesh default absence (`undefined` / absent) on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate; sibling helpers `pinColorOnlyUnlitBasicCustomShadowMaterials` / `pinColorOnlyVisualCustomShadowMaterials`). Mapped / lit / colliders stay authored / r170 Mesh defaults. Do **not** invent replacement materials. Do **not** enable `castShadow` / `receiveShadow`. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ stay at the v0.75 envelope (`crate-toolbox` v0.76)
- [ ] After that customDepth/Distance clear, also delete leftover own-property `onBeforeRender` / `onAfterRender` so the r170 Object3D prototype empty no-ops remain on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate; sibling helpers `pinColorOnlyUnlitBasicRenderCallbacks` / `pinColorOnlyVisualRenderCallbacks`). Mapped / lit / colliders stay authored / r170 Mesh defaults. Do **not** invent replacement callbacks. Do **not** assign `undefined`. Do **not** enable `castShadow` / `receiveShadow`. Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent stay at the v0.76 envelope (`crate-toolbox` v0.77)
- [ ] After that Mesh render-callback clear (and after the long material-flag fence through v0.75 stencil companions / v0.74 polygonOffset companions / v0.72 dithering+A2C), also delete leftover own-property Material `onBeforeCompile` / `onBeforeRender` so the r170 Material.prototype empty no-ops remain on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate; helper `pinColorOnlyUnlitBasicMaterialRenderCallbacks` wired through `pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags`). Mapped / lit / colliders stay authored / r170 defaults. Do **not** invent replacement callbacks or custom shaders. Do **not** assign `undefined`. Do **not** touch Mesh `onBeforeRender` / `onAfterRender`. Do **not** touch `onBeforeShadow` / `onAfterShadow`. Do **not** enable shadows. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / the v0.77 Mesh render-callback clear. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent stay at the v0.77 envelope (`crate-toolbox` v0.78)
- [ ] After that Material compile/render-callback clear (and after the v0.77 Mesh render-callback clear / v0.76 customDepth/Distance clear / v0.49 Mesh `castShadow`/`receiveShadow` false / v0.59 Material `shadowSide = null`), also delete leftover own-property Mesh `onBeforeShadow` / `onAfterShadow` so the r170 Object3D prototype empty no-ops remain on packed color-only unlit MeshBasic visual meshes (same `isColorOnlyUnlitBasic` gate; sibling helpers `pinColorOnlyUnlitBasicShadowCallbacks` / `pinColorOnlyVisualShadowCallbacks`). Mapped / lit / colliders stay authored / r170 Mesh defaults. Do **not** invent replacement callbacks. Do **not** assign `undefined`. Do **not** enable `castShadow` / `receiveShadow`. Do **not** touch Mesh `onBeforeRender` / `onAfterRender`. Do **not** touch Material `onBeforeCompile` / `onBeforeRender`. Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent stay at the v0.78 envelope (`crate-toolbox` v0.79)
- [ ] After that Mesh shadow-callback clear (and after the v0.78 Material compile/render-callback clear), also delete leftover own-property Material `customProgramCacheKey` so the r170 `Material.prototype.customProgramCacheKey` remains on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate; helper `pinColorOnlyUnlitBasicCustomProgramCacheKey` wired through `pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags`). Mapped / lit / colliders stay authored / r170 defaults. Do **not** invent a replacement `customProgramCacheKey`. Do **not** assign `undefined`. Do **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Do **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Do **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** enable shadows. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears / Mesh shadow-callback clear. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent stay at the v0.79 envelope (`crate-toolbox` v0.80)
- [ ] After that Material `customProgramCacheKey` clear (and after the v0.78 Material compile/render-callback clear), also clear leftover Material `defines` so the r170 MeshBasicMaterial / Material default absence remains (`defines === undefined`) on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate; helper `pinColorOnlyUnlitBasicDefines` wired through `pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags`). Mapped / lit / colliders stay authored / r170 defaults. Do **not** invent a replacement `#define` map. Do **not** assign a sentinel empty `{}`. Do **not** touch Material `customProgramCacheKey` (v0.80). Do **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Do **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Do **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** enable shadows. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears / Mesh shadow-callback clear / customProgramCacheKey clear. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default stay at the v0.80 envelope (`crate-toolbox` v0.81)
- [ ] After that Material `defines` clear (and after the long Material flag fence through v0.80 `customProgramCacheKey` / v0.78 `onBeforeCompile` + `onBeforeRender` / v0.75 stencil companions), also pin Material `flatShading = false` on packed color-only unlit MeshBasic materials (same `isColorOnlyUnlitBasic` gate; helper `pinColorOnlyUnlitBasicFlatShading` wired through `pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags`). Mapped / lit / colliders stay authored / r170 defaults. Do **not** invent custom shaders. Do **not** enable flat shading. Fresh r170 Material / MeshBasicMaterial leave `flatShading` unset (`undefined`); WebGLPrograms copies `flatShading: material.flatShading === true` into program parameters and the program cache key; WebGLProgram emits `#define FLAT_SHADED` only when that parameter is true. Do **not** touch Material `defines` (v0.81). Do **not** touch Material `customProgramCacheKey` (v0.80). Do **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Do **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Do **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** enable shadows. Do **not** pin `mesh.visible`. Do **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears / Mesh shadow-callback clear / customProgramCacheKey clear. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent stay at the v0.81 envelope (`crate-toolbox` v0.82)
- [ ] Scale 1 unit = 1 m; seated reach or ray-use for every required step
- [ ] Throw / physics never applies impulse to the camera rig
- [ ] State change is visual + audio and/or haptic; not audio-only
- [ ] Sidecar or `extras.studio` ingest validated (glTF Validator + behavior sanity)

## A11y / UX

- [ ] Seated mode viable
- [ ] Critical info not audio-only
- [ ] Recenter / height adjustment available

## Shipping

- [ ] Device/browser matrix updated
- [ ] CHANGELOG entry
- [ ] Known issues listed in release notes
