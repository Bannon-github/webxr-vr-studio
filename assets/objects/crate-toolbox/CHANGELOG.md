# crate-toolbox

## 0.87.0 — 2026-09-22

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.86 cleared leftover BufferGeometry `morphAttributes` so `Object.keys(geometry.morphAttributes).length === 0` and `morphTargetsRelative === false` on the **13** packed color-only unlit MeshBasic visual geometries (body LOD leaves + lid/latch/tool + fastener), **`pinColorOnlyUnlitBasicGroups` / `pinColorOnlyVisualGroups` clear leftover BufferGeometry `groups` so the r170 empty array remains (`Array.isArray(geometry.groups) && geometry.groups.length === 0`) on those same visuals’ geometries**. Procedural create runs the helper after `pinColorOnlyVisualMorphAttributes`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes or groups). When `groups` is an array, mutate it (`groups.length = 0`); if missing or non-array, assign `groups = []`. r170 `clearGroups()` assigns a new `[]` and is not used. Mapped / lit / interleaved stay authored. Collider meshes stay untouched. A visual whose material or geometry is shared with a blocked collider or interleaved / mapped / lit mesh skips the pin. Does **not** invent groups. Does **not** assign a material array or change materials-per-group. Does **not** touch `drawRange`. Does **not** touch `morphAttributes` / `morphTargetsRelative` (v0.86). Does **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary` (v0.85). Does **not** touch Object3D `animations` (v0.84). Does **not** touch Material `glslVersion` / `flatShading` / `defines` / `customProgramCacheKey` / `onBeforeCompile` / `onBeforeRender`. Does **not** touch Mesh `onBeforeRender` / `onAfterRender` / `onBeforeShadow` / `onAfterShadow`. Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / prior pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3**; visual MeshBasic groups-empty **13** (`Array.isArray(geometry.groups) && geometry.groups.length === 0`; **13 unique geometries**, one per visual); visual MeshBasic morphAttributes-empty stay **13**; visual MeshBasic morphTargets-absent stay **13**; visual MeshBasic animations-empty stay **13**; unique color-only MeshBasic glslVersion-absent stay **3**; flatShading-off stay **3**; defines-absent stay **3**; customProgramCacheKey-default stay **3**; material-render-callbacks-absent stay **3**; visual MeshBasic shadow-callbacks-absent stay **13**; render-callbacks-absent stay **13**; customDepth/Distance-absent stay **13**; rotation-order-XYZ stay **13**; scale-default stay **13**; up-default stay **13**; matrixWorldAutoUpdate-on stay **13**; layers-default stay **13**; renderOrder-0 stay **13**; frustumCulled-on stay **13**; shadow-off stay **13**; raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover BufferGeometry `groups` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Leftover BufferGeometry `groups` are the DCC/GLB MultiMaterial (or exporter) range list. Verified in-repo three@0.170.0: `WebGLRenderer.projectObject` pushes one render item per group **only when `Array.isArray(material)`**. A single MeshBasicMaterial pushes one item with `group = null`, and `renderBufferDirect` then draws `geometry.drawRange`. Leftover groups therefore do not multiply draws on these color-only stand-ins while the material stays a single MeshBasicMaterial. They still matter on Quest 3: `BufferGeometry.copy` re-adds every group, and a later material-array binding would issue one draw per group on a TBDR mobile GPU. Clearing to the empty r170 array keeps that single-draw contract stable beside the v0.86 morph fence and drops the round-trip group list. `clearGroups()` assigns a new `[]`; the helper mutates the existing array with `groups.length = 0`. Does **not** invent groups, assign a material array, touch `drawRange`, touch `morphAttributes` / `morphTargetsRelative`, touch Mesh `morphTargetInfluences` / `morphTargetDictionary`, touch Object3D `animations`, touch Material program-cache fields, touch Mesh render/shadow callbacks, touch customDepth/Distance, enable shadows, pin `mesh.visible`, or change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Draws / tris / verts / attrBytes / unique MeshBasic / morphAttributes-empty / morphTargets-absent / animations-empty unchanged vs v0.86. groups-empty **13** is the new count (13 unique geometries). Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.87.0/`

## 0.86.0 — 2026-09-22

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.85 deleted leftover Mesh `morphTargetInfluences` / `morphTargetDictionary` so both are absent (`=== undefined`) on the **13** packed color-only unlit MeshBasic visual meshes (body LOD leaves + lid/latch/tool + fastener), **`pinColorOnlyUnlitBasicMorphAttributes` / `pinColorOnlyVisualMorphAttributes` clear leftover BufferGeometry `morphAttributes` and pin `morphTargetsRelative` to the r170 default `false` on those same visuals’ geometries** so `Object.keys(geometry.morphAttributes).length === 0` and `geometry.morphTargetsRelative === false`. Procedural create runs the helper after `pinColorOnlyVisualMorphTargets`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes or morph targets). Mutate the existing plain object: delete own keys; dispose a leftover BufferAttribute only when `dispose` exists and the attribute is not a live geometry attribute/index or still used by a skipped mesh. If `morphAttributes` is missing or not a plain object, assign `{}`. Do **not** reassign `null` / `undefined`. Set `morphTargetsRelative = false` only when it is not already false. Mapped / lit / interleaved stay authored. Collider meshes stay untouched. A visual whose material or geometry is shared with a blocked collider or interleaved / mapped / lit mesh skips the pin. Color-only leftover keys (including an empty array or empty BufferAttribute under a key) are cleared — that is the pulse. Does **not** invent morph targets. Does **not** call `updateMorphTargets()`. Does **not** add morphAttributes. Does **not** enable morphing. Does **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary` (v0.85). Does **not** touch Object3D `animations` (v0.84). Does **not** touch Material `glslVersion` / `flatShading` / `defines` / `customProgramCacheKey` / `onBeforeCompile` / `onBeforeRender`. Does **not** touch Mesh `onBeforeRender` / `onAfterRender` / `onBeforeShadow` / `onAfterShadow`. Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / prior pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3**; visual MeshBasic morphAttributes-empty **13** (`Object.keys(geometry.morphAttributes).length === 0` && `geometry.morphTargetsRelative === false`; **13 unique geometries**, one per visual); visual MeshBasic morphTargets-absent stay **13**; visual MeshBasic animations-empty stay **13**; unique color-only MeshBasic glslVersion-absent stay **3**; flatShading-off stay **3**; defines-absent stay **3**; customProgramCacheKey-default stay **3**; material-render-callbacks-absent stay **3**; visual MeshBasic shadow-callbacks-absent stay **13**; render-callbacks-absent stay **13**; customDepth/Distance-absent stay **13**; rotation-order-XYZ stay **13**; scale-default stay **13**; up-default stay **13**; matrixWorldAutoUpdate-on stay **13**; layers-default stay **13**; renderOrder-0 stay **13**; frustumCulled-on stay **13**; shadow-off stay **13**; raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover BufferGeometry `morphAttributes` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Leftover non-empty `morphAttributes` (even an empty array or empty BufferAttribute under `position` / `normal` / `color`) still makes r170 `WebGLRenderer` call `WebGLMorphtargets.update`, which allocates morph data-texture bookkeeping on a TBDR mobile GPU and confuses DCC/GLTF round-trips. Clearing keys keeps the color-only stand-ins off that path beside the v0.85 Mesh influence absence. Verified in-repo three@0.170.0: the BufferGeometry constructor assigns `this.morphAttributes = {}` and `this.morphTargetsRelative = false`. r170 `BufferAttribute` has no `dispose`; the helper calls `dispose` only when the method exists and the attribute is not a live geometry attribute/index. Does **not** invent morph targets, call `updateMorphTargets()`, add morphAttributes, enable morphing, touch Mesh `morphTargetInfluences` / `morphTargetDictionary`, touch Object3D `animations`, touch Material program-cache fields, touch Mesh render/shadow callbacks, touch customDepth/Distance, enable shadows, pin `mesh.visible`, or change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Draws / tris / verts / attrBytes / unique MeshBasic / morphTargets-absent count unchanged vs v0.85. morphAttributes-empty **13** is the new count (13 unique geometries). Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.86.0/`

## 0.85.0 — 2026-09-21

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.84 cleared leftover Object3D `animations` so `Array.isArray(mesh.animations) && mesh.animations.length === 0` remains on the **13** packed color-only unlit MeshBasic visual meshes (and after the Mesh callback fence through v0.79 `onBeforeShadow` / `onAfterShadow` / v0.77 `onBeforeRender` / `onAfterRender` / v0.76 customDepth/Distance), **`pinColorOnlyUnlitBasicMorphTargets` / `pinColorOnlyVisualMorphTargets` delete leftover Mesh `morphTargetInfluences` / `morphTargetDictionary` so the r170 absence remains (`morphTargetInfluences === undefined` && `morphTargetDictionary === undefined`) on those same packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after `pinColorOnlyVisualAnimations`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes or morph targets). Prefer `delete` when present. Does **not** assign `null` or empty `[]` / `{}`. Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** invent morph targets. Does **not** call `updateMorphTargets()`. Does **not** add morphAttributes. Does **not** enable morphing. Does **not** touch Object3D `animations` (v0.84). Does **not** touch Material `glslVersion` (v0.83). Does **not** touch Material `flatShading` (v0.82). Does **not** touch Material `defines` (v0.81). Does **not** touch Material `customProgramCacheKey` (v0.80). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including glslVersion / flatShading / defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/flatShading/glslVersion/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance/Mesh render-callback/Material compile-render-callback/Mesh shadow-callback/customProgramCacheKey/defines/animations pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `glslVersion === undefined` plus prior pins including `flatShading === false` / `defines === undefined` / `stencilWrite === false` / `stencilRef === 0` / `polygonOffsetFactor === 0` / `dithering === false` / `alphaToCoverage === false` intact; visual MeshBasic morphTargets-absent **13** (`morphTargetInfluences === undefined` && `morphTargetDictionary === undefined`); visual MeshBasic animations-empty stay **13**; unique color-only MeshBasic glslVersion-absent stay **3**; unique color-only MeshBasic flatShading-off stay **3**; unique color-only MeshBasic defines-absent stay **3**; unique color-only MeshBasic customProgramCacheKey-default stay **3**; unique color-only MeshBasic material-render-callbacks-absent stay **3**; visual MeshBasic shadow-callbacks-absent stay **13**; visual MeshBasic render-callbacks-absent stay **13**; visual MeshBasic customDepth/Distance-absent stay **13**; visual MeshBasic rotation-order-XYZ stay **13**; visual MeshBasic scale-default stay **13**; visual MeshBasic up-default stay **13**; visual MeshBasic matrixWorldAutoUpdate-on stay **13**; visual MeshBasic layers-default stay **13**; visual MeshBasic renderOrder-0 stay **13**; visual MeshBasic frustumCulled-on stay **13**; visual MeshBasic shadow-off stay **13**; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover Mesh `morphTargetInfluences` / `morphTargetDictionary` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Leftover Mesh `morphTargetInfluences` / `morphTargetDictionary` on color-only unlit stand-ins are not, by themselves, the WebGL morph-texture upload. Verified in-repo three@0.170.0: `WebGLRenderer` calls `WebGLMorphtargets.update` only when `geometry.morphAttributes.position`, `.normal`, or `.color` is defined, and those attributes are already blocked on this pin’s targets by `colorOnlyGeometryBlocksPack`. The leftover that still matters is instance state: a present influences array (including empty `[]`, which is truthy) or dictionary is copied by `Mesh.copy`, and GLTFLoader weight-track binding plus `PropertyBinding` treat a truthy `morphTargetInfluences` as a morph node. Deleting both properties restores the fresh non-morph Mesh absence so those paths do not see morph influence bookkeeping on the 13 color-only visuals. Does **not** assign `null` or empty `[]` / `{}`, invent morph targets, call `updateMorphTargets()`, add morphAttributes, enable morphing, touch Object3D `animations`, touch Material `glslVersion` / `flatShading` / `defines` / `customProgramCacheKey` / `onBeforeCompile` / `onBeforeRender`, touch Mesh render or shadow callbacks, touch customDepth/Distance, enable shadows, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including glslVersion / flatShading / defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, or invent transparent materials. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent / flatShading-off / glslVersion-absent / animations-empty count unchanged vs v0.84. morphTargets-absent **13** is the new count. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.85.0/`

## 0.84.0 — 2026-09-21

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.83 cleared leftover Material `glslVersion` so `glslVersion === undefined` remains on the **3** shared packed color-only unlit MeshBasic materials (and after the Mesh callback fence through v0.79 `onBeforeShadow` / `onAfterShadow` / v0.77 `onBeforeRender` / `onAfterRender` / v0.76 customDepth/Distance), **`pinColorOnlyUnlitBasicAnimations` / `pinColorOnlyVisualAnimations` clear leftover Object3D `animations` so the r170 empty list remains (`Array.isArray(mesh.animations) && mesh.animations.length === 0`) on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after `pinColorOnlyVisualShadowCallbacks`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes, AnimationClips, or an AnimationMixer). When `animations` is an array, mutate it (`animations.length = 0`); if missing or non-array, assign `animations = []`. Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** invent AnimationClips. Does **not** create an AnimationMixer. Does **not** call `AnimationMixer.update`. Does **not** touch Material `glslVersion` (v0.83). Does **not** touch Material `flatShading` (v0.82). Does **not** touch Material `defines` (v0.81). Does **not** touch Material `customProgramCacheKey` (v0.80). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including glslVersion / flatShading / defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/flatShading/glslVersion/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance/Mesh render-callback/Material compile-render-callback/Mesh shadow-callback/customProgramCacheKey/defines pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `glslVersion === undefined` plus prior pins including `flatShading === false` / `defines === undefined` / `stencilWrite === false` / `stencilRef === 0` / `polygonOffsetFactor === 0` / `dithering === false` / `alphaToCoverage === false` intact; visual MeshBasic animations-empty **13** (`Array.isArray(mesh.animations) && mesh.animations.length === 0`); unique color-only MeshBasic glslVersion-absent stay **3**; unique color-only MeshBasic flatShading-off stay **3**; unique color-only MeshBasic defines-absent stay **3**; unique color-only MeshBasic customProgramCacheKey-default stay **3**; unique color-only MeshBasic material-render-callbacks-absent stay **3**; visual MeshBasic shadow-callbacks-absent stay **13**; visual MeshBasic render-callbacks-absent stay **13**; visual MeshBasic customDepth/Distance-absent stay **13**; visual MeshBasic rotation-order-XYZ stay **13**; visual MeshBasic scale-default stay **13**; visual MeshBasic up-default stay **13**; visual MeshBasic matrixWorldAutoUpdate-on stay **13**; visual MeshBasic layers-default stay **13**; visual MeshBasic renderOrder-0 stay **13**; visual MeshBasic frustumCulled-on stay **13**; visual MeshBasic shadow-off stay **13**; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover Object3D `animations` AnimationClip arrays on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover Object3D `animations` (AnimationClip arrays) on color-only unlit midtone stand-ins can pull clip/mixer work into the frame loop on a TBDR mobile GPU even when this prop’s motion is procedural (`tryUse` / `tryDriveFastener`). Clearing to the r170 empty default keeps the Quest-safe static-visual contract beside the existing Mesh shadow/render-callback fences and the v0.83 Material `glslVersion` absence. Verified in-repo three@0.170.0: the Object3D constructor assigns `this.animations = []`. Does **not** invent AnimationClips, create an AnimationMixer, call `AnimationMixer.update`, touch Material `glslVersion`, touch Material `flatShading`, touch Material `defines`, touch Material `customProgramCacheKey`, touch Material `onBeforeCompile` / `onBeforeRender`, touch Mesh `onBeforeRender` / `onAfterRender`, touch Mesh `onBeforeShadow` / `onAfterShadow`, touch customDepth/Distance, enable shadows, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including glslVersion / flatShading / defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, or invent transparent materials. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent / flatShading-off / glslVersion-absent count unchanged vs v0.83. animations-empty **13** is the new count. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.84.0/`

## 0.83.0 — 2026-09-21

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.82 pinned Material `flatShading = false` on the **3** shared packed color-only unlit MeshBasic materials (and after the Material program-cache fence through v0.81 `defines` / v0.80 `customProgramCacheKey` / v0.78 `onBeforeCompile` + `onBeforeRender`), **`pinColorOnlyUnlitBasicGlslVersion` (wired through `pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags`) deletes leftover Material `glslVersion` so the r170 MeshBasicMaterial / Material default absence remains (`glslVersion === undefined`) on packed color-only unlit MeshBasic materials**. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials or custom shaders). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** assign a sentinel string. Does **not** assign `GLSL3` / `GLSL1` / `'300 es'` / `'100'`. Does **not** convert MeshBasic to ShaderMaterial (`ShaderMaterial` assigns `glslVersion = null` in its own constructor). Does **not** invent custom shaders / ShaderMaterial / RawShaderMaterial. Does **not** touch Material `flatShading` (v0.82). Does **not** touch Material `defines` (v0.81). Does **not** touch Material `customProgramCacheKey` (v0.80). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including flatShading / defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears / Mesh shadow-callback clear / customProgramCacheKey clear. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/flatShading/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance/Mesh render-callback/Material compile-render-callback/Mesh shadow-callback/customProgramCacheKey/defines pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `flatShading === false` plus prior pins including `defines === undefined` / `stencilWrite === false` / `stencilRef === 0` / `polygonOffsetFactor === 0` / `dithering === false` / `alphaToCoverage === false` intact; unique color-only MeshBasic glslVersion-absent **3** (`material.glslVersion === undefined`); unique color-only MeshBasic flatShading-off stay **3**; unique color-only MeshBasic defines-absent stay **3**; unique color-only MeshBasic customProgramCacheKey-default stay **3**; unique color-only MeshBasic material-render-callbacks-absent stay **3**; visual MeshBasic shadow-callbacks-absent stay **13**; visual MeshBasic render-callbacks-absent stay **13**; visual MeshBasic customDepth/Distance-absent stay **13**; visual MeshBasic rotation-order-XYZ stay **13**; visual MeshBasic scale-default stay **13**; visual MeshBasic up-default stay **13**; visual MeshBasic matrixWorldAutoUpdate-on stay **13**; visual MeshBasic layers-default stay **13**; visual MeshBasic renderOrder-0 stay **13**; visual MeshBasic frustumCulled-on stay **13**; visual MeshBasic shadow-off stay **13**; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover Material `glslVersion` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover Material `glslVersion` (`GLSL3` / `'300 es'` / `'100'`) on color-only unlit midtone stand-ins can force a `#version` / GLSL3 preamble branch on built-in MeshBasic programs (r170 `WebGLPrograms` copies `glslVersion: material.glslVersion`, and `WebGLProgram` omits the `pc_fragColor` / `gl_FragColor` defines when `parameters.glslVersion === GLSL3`) on a TBDR mobile GPU. Clearing to the r170 absence keeps the Quest-safe opaque unlit contract beside the existing flatShading / defines / customProgramCacheKey / onBeforeCompile fences. Verified in-repo three@0.170.0: Material / MeshBasicMaterial constructors leave `glslVersion` unset (`undefined`; `Object.hasOwn` false); `ShaderMaterial` assigns `glslVersion = null` in its own constructor. Does **not** assign a sentinel string, assign `GLSL3` / `GLSL1` / `'300 es'` / `'100'`, invent custom shaders, convert MeshBasic to ShaderMaterial, touch Material `flatShading`, touch Material `defines`, touch Material `customProgramCacheKey`, touch Material `onBeforeCompile` / `onBeforeRender`, touch Mesh `onBeforeRender` / `onAfterRender`, touch Mesh `onBeforeShadow` / `onAfterShadow`, touch customDepth/Distance, enable shadows, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including flatShading / defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, or invent transparent materials. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent / flatShading-off count unchanged vs v0.82. glslVersion-absent **3** is the new count. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.83.0/`

## 0.82.0 — 2026-09-21

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.81 cleared leftover Material `defines` so `defines === undefined` remains on the **3** shared packed color-only unlit MeshBasic materials (and after the long Material flag fence through v0.80 `customProgramCacheKey` / v0.78 `onBeforeCompile` + `onBeforeRender` / v0.75 stencil companions), **`pinColorOnlyUnlitBasicFlatShading` (wired through `pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags`) sets Material `flatShading = false` on packed color-only unlit MeshBasic materials**. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials or custom shaders). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** invent custom shaders or enable flat shading. Does **not** touch Material `defines` (v0.81). Does **not** touch Material `customProgramCacheKey` (v0.80). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears / Mesh shadow-callback clear / customProgramCacheKey clear. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance/Mesh render-callback/Material compile-render-callback/Mesh shadow-callback/customProgramCacheKey/defines pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `flatShading === false` plus prior pins including `defines === undefined` / `stencilWrite === false` / `stencilRef === 0` / `polygonOffsetFactor === 0` / `dithering === false` / `alphaToCoverage === false` intact; unique color-only MeshBasic flatShading-off **3** (`material.flatShading === false`); unique color-only MeshBasic defines-absent stay **3**; unique color-only MeshBasic customProgramCacheKey-default stay **3**; unique color-only MeshBasic material-render-callbacks-absent stay **3**; visual MeshBasic shadow-callbacks-absent stay **13**; visual MeshBasic render-callbacks-absent stay **13**; visual MeshBasic customDepth/Distance-absent stay **13**; visual MeshBasic rotation-order-XYZ stay **13**; visual MeshBasic scale-default stay **13**; visual MeshBasic up-default stay **13**; visual MeshBasic matrixWorldAutoUpdate-on stay **13**; visual MeshBasic layers-default stay **13**; visual MeshBasic renderOrder-0 stay **13**; visual MeshBasic frustumCulled-on stay **13**; visual MeshBasic shadow-off stay **13**; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover `flatShading = true` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover `flatShading = true` on color-only unlit midtone stand-ins fragments WebGL program cache keys (r170 `WebGLPrograms` copies `flatShading: material.flatShading === true` and `WebGLProgram` emits `#define FLAT_SHADED` when that parameter is true) and can force a separate FLAT_SHADED program variant on a TBDR mobile GPU. Pinning `false` keeps the Quest-safe opaque unlit contract beside the existing defines / customProgramCacheKey / onBeforeCompile fences. Verified in-repo three@0.170.0: Material / MeshBasicMaterial constructors leave `flatShading` unset (`undefined`); only `=== true` selects the flat variant. Does **not** invent custom shaders, enable flat shading, touch Material `defines`, touch Material `customProgramCacheKey`, touch Material `onBeforeCompile` / `onBeforeRender`, touch Mesh `onBeforeRender` / `onAfterRender`, touch Mesh `onBeforeShadow` / `onAfterShadow`, touch customDepth/Distance, enable shadows, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, or invent transparent materials. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent count unchanged vs v0.81. flatShading-off **3** is the new count. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.82.0/`

## 0.81.0 — 2026-09-21

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.80 deleted leftover own-property Material `customProgramCacheKey` so the r170 `Material.prototype.customProgramCacheKey` remains on the **3** shared packed color-only unlit MeshBasic materials (and after v0.78 cleared Material `onBeforeCompile` / `onBeforeRender`), **`pinColorOnlyUnlitBasicDefines` (wired through `pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags`) clears leftover Material `defines` so the r170 MeshBasicMaterial / Material default absence remains (`defines === undefined`) on packed color-only unlit MeshBasic materials**. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials or a replacement `#define` map). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** invent custom `#define` maps or replacement shaders. Does **not** assign a sentinel empty `{}` (r170 `WebGLPrograms` treats `parameters.defines !== undefined` as present and walks keys into the program cache key). Does **not** touch Material `customProgramCacheKey` (v0.80). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears / Mesh shadow-callback clear / customProgramCacheKey clear. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance/Mesh render-callback/Material compile-render-callback/Mesh shadow-callback/customProgramCacheKey pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `stencilWrite === false` && `stencilRef === 0` && `stencilWriteMask === 0xff` && `stencilFuncMask === 0xff` && `stencilZFail === KeepStencilOp` && `stencilZPass === KeepStencilOp` plus prior pins including `polygonOffsetFactor === 0` / `polygonOffsetUnits === 0` / `dithering === false` / `alphaToCoverage === false` intact; unique color-only MeshBasic defines-absent **3** (`material.defines === undefined`); unique color-only MeshBasic customProgramCacheKey-default stay **3**; unique color-only MeshBasic material-render-callbacks-absent stay **3**; visual MeshBasic shadow-callbacks-absent stay **13**; visual MeshBasic render-callbacks-absent stay **13**; visual MeshBasic customDepth/Distance-absent stay **13**; visual MeshBasic rotation-order-XYZ stay **13**; visual MeshBasic scale-default stay **13**; visual MeshBasic up-default stay **13**; visual MeshBasic matrixWorldAutoUpdate-on stay **13**; visual MeshBasic layers-default stay **13**; visual MeshBasic renderOrder-0 stay **13**; visual MeshBasic frustumCulled-on stay **13**; visual MeshBasic shadow-off stay **13**; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover Material `defines` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover Material `defines` on color-only unlit midtone stand-ins fragment WebGL program cache keys (r170 `WebGLPrograms` walks defines into the cache key when the property is not `undefined`) and can force extra shader compiles on a TBDR mobile GPU. Clearing to the r170 absence keeps the Quest-safe opaque unlit contract beside the existing customProgramCacheKey / onBeforeCompile fences. Does **not** invent custom `#define` maps, assign a sentinel empty `{}`, touch Material `customProgramCacheKey`, touch Material `onBeforeCompile` / `onBeforeRender`, touch Mesh `onBeforeRender` / `onAfterRender`, touch Mesh `onBeforeShadow` / `onAfterShadow`, touch customDepth/Distance, enable shadows, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default count unchanged vs v0.80. defines-absent **3** is the new count. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.81.0/`

## 0.80.0 — 2026-09-21

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.79 deleted leftover own-property Mesh `onBeforeShadow` / `onAfterShadow` so the r170 Object3D.prototype empty no-ops remain on the **13** packed color-only unlit MeshBasic visual meshes (and after v0.78 cleared Material `onBeforeCompile` / `onBeforeRender` on the **3** shared materials), **`pinColorOnlyUnlitBasicCustomProgramCacheKey` (wired through `pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags`) deletes leftover own-property Material `customProgramCacheKey` so the r170 `Material.prototype.customProgramCacheKey` remains on packed color-only unlit MeshBasic materials**. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials or a replacement function). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** invent a replacement `customProgramCacheKey`. Does **not** assign `undefined` (WebGLRenderer uses `customProgramCacheKey()` when building/caching programs). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears / Mesh shadow-callback clear. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance/Mesh render-callback/Material compile-render-callback/Mesh shadow-callback pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `stencilWrite === false` && `stencilRef === 0` && `stencilWriteMask === 0xff` && `stencilFuncMask === 0xff` && `stencilZFail === KeepStencilOp` && `stencilZPass === KeepStencilOp` plus prior pins including `polygonOffsetFactor === 0` / `polygonOffsetUnits === 0` / `dithering === false` / `alphaToCoverage === false` intact; unique color-only MeshBasic customProgramCacheKey-default **3** (`!Object.hasOwn(customProgramCacheKey)` && the method resolves to `Material.prototype.customProgramCacheKey`); unique color-only MeshBasic material-render-callbacks-absent stay **3**; visual MeshBasic shadow-callbacks-absent stay **13**; visual MeshBasic render-callbacks-absent stay **13**; visual MeshBasic customDepth/Distance-absent stay **13**; visual MeshBasic rotation-order-XYZ stay **13**; visual MeshBasic scale-default stay **13**; visual MeshBasic up-default stay **13**; visual MeshBasic matrixWorldAutoUpdate-on stay **13**; visual MeshBasic layers-default stay **13**; visual MeshBasic renderOrder-0 stay **13**; visual MeshBasic frustumCulled-on stay **13**; visual MeshBasic shadow-off stay **13**; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover own-property Material `customProgramCacheKey` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover own-property Material `customProgramCacheKey` on color-only unlit midtone stand-ins fragments WebGL program cache keys and can force extra shader compiles on a TBDR mobile GPU. Clearing leftover own properties so the r170 Material.prototype method remains keeps the Quest-safe opaque unlit contract beside the existing Material compile/render-callback, Mesh render-callback, Mesh shadow-callback, customDepth/Distance, castShadow/receiveShadow, and shadowSide fences. Does **not** invent a replacement `customProgramCacheKey`, assign `undefined`, touch Material `onBeforeCompile` / `onBeforeRender`, touch Mesh `onBeforeRender` / `onAfterRender`, touch Mesh `onBeforeShadow` / `onAfterShadow`, touch customDepth/Distance, enable shadows, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent count unchanged vs v0.79. customProgramCacheKey-default **3** is the new count. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.80.0/`

## 0.79.0 — 2026-09-21

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.78 deleted leftover own-property Material `onBeforeCompile` / `onBeforeRender` so the r170 Material.prototype empty no-ops remain on the **3** shared packed color-only unlit MeshBasic materials (and after v0.77 cleared Mesh `onBeforeRender` / `onAfterRender` on the **13** visual meshes, and after v0.76 cleared Mesh `customDepthMaterial` / `customDistanceMaterial`, and after v0.49 pinned Mesh `castShadow`/`receiveShadow` false and v0.59 pinned Material `shadowSide = null`), **`pinColorOnlyUnlitBasicShadowCallbacks` / `pinColorOnlyVisualShadowCallbacks` delete leftover own-property `onBeforeShadow` / `onAfterShadow` so the r170 Object3D prototype empty no-ops remain on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after `pinColorOnlyVisualRenderCallbacks`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes or replacement callbacks). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** invent replacement callbacks. Does **not** assign `undefined` (WebGLShadowMap always invokes these when a mesh is selected for a shadow-map pass). Does **not** enable `castShadow` / `receiveShadow`. Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance/Mesh render-callback/Material compile-render-callback pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `stencilWrite === false` && `stencilRef === 0` && `stencilWriteMask === 0xff` && `stencilFuncMask === 0xff` && `stencilZFail === KeepStencilOp` && `stencilZPass === KeepStencilOp` plus prior pins including `polygonOffsetFactor === 0` / `polygonOffsetUnits === 0` / `dithering === false` / `alphaToCoverage === false` intact; unique color-only MeshBasic material-render-callbacks-absent stay **3**; visual MeshBasic shadow-callbacks-absent **13** (`!Object.hasOwn(onBeforeShadow)` && `!Object.hasOwn(onAfterShadow)` && both resolve to `Object3D.prototype` empty no-ops); visual MeshBasic render-callbacks-absent stay **13**; visual MeshBasic customDepth/Distance-absent stay **13**; visual MeshBasic rotation-order-XYZ stay **13**; visual MeshBasic scale-default stay **13**; visual MeshBasic up-default stay **13**; visual MeshBasic matrixWorldAutoUpdate-on stay **13**; visual MeshBasic layers-default stay **13**; visual MeshBasic renderOrder-0 stay **13**; visual MeshBasic frustumCulled-on stay **13**; visual MeshBasic shadow-off stay **13**; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover own-property Mesh `onBeforeShadow` / `onAfterShadow` on unlit midtone stand-ins even when `castShadow === false`), not a new layer.
- **Quest 3:** Accidental leftover own-property `onBeforeShadow` / `onAfterShadow` on color-only unlit midtone visuals become per-shadow-draw JS work on Quest Browser / TBDR if a light later has `castShadow`, because r170 WebGLShadowMap always invokes them when a mesh is selected for a shadow-map pass. Even with `castShadow` / `receiveShadow` already false, leftover own-property stubs are unused callback noise and can confuse DCC round-trips. Clearing leftover own properties so the r170 Object3D prototype empty no-ops remain keeps the Quest-safe opaque unlit contract beside the existing Mesh render-callback, Material compile/render-callback, customDepth/Distance, castShadow/receiveShadow, and shadowSide fences. Does **not** invent replacement callbacks, assign `undefined`, enable shadows, touch Mesh `onBeforeRender` / `onAfterRender`, touch Material `onBeforeCompile` / `onBeforeRender`, touch customDepth/Distance, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent count unchanged vs v0.78. Shadow-callbacks-absent **13** is the new count. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.79.0/`

## 0.78.0 — 2026-09-20

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.77 deleted leftover own-property Mesh `onBeforeRender` / `onAfterRender` so the r170 Object3D prototype empty no-ops remain on the **13** packed color-only unlit MeshBasic visual meshes (and after the long material-flag fence through v0.75 stencil companions / v0.74 polygonOffset companions / v0.72 dithering+A2C on the **3** shared materials), **`pinColorOnlyUnlitBasicMaterialRenderCallbacks` (wired through `pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags`) deletes leftover own-property Material `onBeforeCompile` / `onBeforeRender` so the r170 Material.prototype empty no-ops remain on packed color-only unlit MeshBasic materials**. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials or replacement callbacks). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** invent replacement callbacks or custom shaders. Does **not** assign `undefined` (WebGLRenderer always invokes `material.onBeforeRender` per-draw and `material.onBeforeCompile` on program compile). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch `onBeforeShadow` / `onAfterShadow`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / the v0.77 Mesh render-callback clear. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance/Mesh render-callback pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `stencilWrite === false` && `stencilRef === 0` && `stencilWriteMask === 0xff` && `stencilFuncMask === 0xff` && `stencilZFail === KeepStencilOp` && `stencilZPass === KeepStencilOp` plus prior pins including `polygonOffsetFactor === 0` / `polygonOffsetUnits === 0` / `dithering === false` / `alphaToCoverage === false` intact; unique color-only MeshBasic material-render-callbacks-absent **3** (`!Object.hasOwn(onBeforeCompile)` && `!Object.hasOwn(onBeforeRender)` && both resolve to `Material.prototype` empty no-ops); visual MeshBasic render-callbacks-absent stay **13**; visual MeshBasic customDepth/Distance-absent stay **13**; visual MeshBasic rotation-order-XYZ stay **13**; visual MeshBasic scale-default stay **13**; visual MeshBasic up-default stay **13**; visual MeshBasic matrixWorldAutoUpdate-on stay **13**; visual MeshBasic layers-default stay **13**; visual MeshBasic renderOrder-0 stay **13**; visual MeshBasic frustumCulled-on stay **13**; visual MeshBasic shadow-off stay **13**; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover own-property Material `onBeforeCompile` / `onBeforeRender` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover own-property `onBeforeCompile` / `onBeforeRender` on color-only unlit midtone materials become compile/per-draw JS work on Quest Browser / TBDR because r170 WebGLRenderer always invokes them. Leftover `onBeforeCompile` stubs can also fragment `customProgramCacheKey`. Clearing leftover own properties so the r170 Material.prototype empty no-ops remain keeps the Quest-safe opaque unlit contract beside the existing Mesh render-callback, customDepth/Distance, castShadow/receiveShadow, and shadowSide fences. Does **not** invent replacement callbacks, custom shaders, assign `undefined`, touch Mesh `onBeforeRender` / `onAfterRender`, touch `onBeforeShadow` / `onAfterShadow`, enable shadows, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, or invent transparent materials. Draws / tris / verts / attrBytes / unique MeshBasic / raycast-off / matrix freeze / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent count unchanged vs v0.77. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.78.0/`

## 0.77.0 — 2026-09-20

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.76 cleared leftover Mesh `customDepthMaterial` / `customDistanceMaterial` to the r170 Mesh default absence (`undefined`) on packed color-only unlit MeshBasic visual meshes (and after v0.49 already pinned Mesh `castShadow = false` / `receiveShadow = false` and v0.59 pinned Material `shadowSide = null`), **`pinColorOnlyUnlitBasicRenderCallbacks` / `pinColorOnlyVisualRenderCallbacks` delete leftover own-property `onBeforeRender` / `onAfterRender` so the r170 Object3D prototype empty no-ops remain on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after `pinColorOnlyVisualCustomShadowMaterials`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes or replacement callbacks). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** invent replacement callbacks. Does **not** assign `undefined` (WebGLRenderer always invokes these). Does **not** enable `castShadow` / `receiveShadow`. Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `stencilWrite === false` && `stencilRef === 0` && `stencilWriteMask === 0xff` && `stencilFuncMask === 0xff` && `stencilZFail === KeepStencilOp` && `stencilZPass === KeepStencilOp` plus prior pins including `polygonOffsetFactor === 0` / `polygonOffsetUnits === 0` / `dithering === false` / `alphaToCoverage === false` intact; visual MeshBasic render-callbacks-absent **13** (`!Object.hasOwn(onBeforeRender)` && `!Object.hasOwn(onAfterRender)` && both resolve to `Object3D.prototype` empty no-ops); visual MeshBasic customDepth/Distance-absent stay **13** (`customDepthMaterial == null` && `customDistanceMaterial == null`); visual MeshBasic rotation-order-XYZ stay **13** (`rotation.order === 'XYZ'`); visual MeshBasic scale-default stay **13** (`scale.x === 1` && `scale.y === 1` && `scale.z === 1`); visual MeshBasic up-default stay **13** (`up.x === 0` && `up.y === 1` && `up.z === 0`); visual MeshBasic matrixWorldAutoUpdate-on stay **13** (`matrixWorldAutoUpdate === true`); visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover own-property Mesh `onBeforeRender` / `onAfterRender` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover own-property `onBeforeRender` / `onAfterRender` on color-only unlit midtone visuals become per-draw JS work on Quest Browser / TBDR because r170 WebGLRenderer always invokes them. Clearing leftover own properties so the r170 Object3D prototype empty no-ops remain keeps the Quest-safe opaque unlit contract beside the existing customDepth/Distance, castShadow/receiveShadow, and shadowSide fences. Does **not** invent replacement callbacks, assign `undefined`, enable shadows, touch customDepth/Distance, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent count unchanged vs v0.76. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.77.0/`

## 0.76.0 — 2026-09-20

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.75 elevated Material `stencilRef = 0` / `stencilWriteMask = 0xff` / `stencilFuncMask = 0xff` / `stencilZFail = KeepStencilOp` / `stencilZPass = KeepStencilOp` as first-class measured flags on packed color-only unlit MeshBasic **materials** (and after v0.49 already pinned Mesh `castShadow = false` / `receiveShadow = false` and v0.59 pinned Material `shadowSide = null`), **`pinColorOnlyUnlitBasicCustomShadowMaterials` / `pinColorOnlyVisualCustomShadowMaterials` clear leftover Mesh `customDepthMaterial` / `customDistanceMaterial` to the r170 Mesh default absence (`undefined` / absent) on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after `pinColorOnlyVisualRotationOrder`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes or replacement depth/distance materials). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** invent replacement materials. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `stencilWrite === false` && `stencilRef === 0` && `stencilWriteMask === 0xff` && `stencilFuncMask === 0xff` && `stencilZFail === KeepStencilOp` && `stencilZPass === KeepStencilOp` plus prior pins including `polygonOffsetFactor === 0` / `polygonOffsetUnits === 0` / `dithering === false` / `alphaToCoverage === false` intact; visual MeshBasic customDepth/Distance-absent **13** (`customDepthMaterial == null` && `customDistanceMaterial == null`); visual MeshBasic rotation-order-XYZ stay **13** (`rotation.order === 'XYZ'`); visual MeshBasic scale-default stay **13** (`scale.x === 1` && `scale.y === 1` && `scale.z === 1`); visual MeshBasic up-default stay **13** (`up.x === 0` && `up.y === 1` && `up.z === 0`); visual MeshBasic matrixWorldAutoUpdate-on stay **13** (`matrixWorldAutoUpdate === true`); visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover Mesh `customDepthMaterial` / `customDistanceMaterial` on unlit midtone stand-ins even when `castShadow === false`), not a new layer.
- **Quest 3:** Accidental leftover `customDepthMaterial` / `customDistanceMaterial` on color-only unlit midtone visuals are unused when `castShadow === false`, confuse DCC round-trips, and can force extra shadow-material compiles/paths if shadow casting is later enabled on a TBDR mobile GPU. Clearing to the r170 Mesh default (absent) keeps the Quest-safe opaque unlit contract beside the existing castShadow/receiveShadow and shadowSide fences. Does **not** invent replacement materials, enable shadows, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ count unchanged vs v0.75. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.76.0/`

## 0.75.0 — 2026-09-20

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.74 elevated Material `polygonOffsetFactor = 0` / `polygonOffsetUnits = 0` as first-class measured companions (and after v0.53 already assigned the full r170 Material stencil suite inside `pinColorOnlyUnlitBasicFlags`), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material `stencilRef = 0` / `stencilWriteMask = 0xff` / `stencilFuncMask = 0xff` / `stencilZFail = KeepStencilOp` / `stencilZPass = KeepStencilOp` on packed color-only unlit MeshBasic materials**. v0.53 already assigned these as stencil companions and measured `stencilWrite` / `stencilFunc` / `stencilFail` in the short form; this pulse makes the Ref / masks / ZFail / ZPass companions first-class measured flags. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide/visible/envMap companion/map-intensity companion/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companion pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** enable stencil write or invent non-Always func / non-Keep ops / non-zero ref / non-0xff masks. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including polygonOffset companions / dithering / A2C / blendColor / blendAlpha. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `stencilWrite === false` && `stencilRef === 0` && `stencilWriteMask === 0xff` && `stencilFuncMask === 0xff` && `stencilZFail === KeepStencilOp` && `stencilZPass === KeepStencilOp` plus prior pins including `polygonOffsetFactor === 0` / `polygonOffsetUnits === 0` / `dithering === false` / `alphaToCoverage === false` intact; visual MeshBasic rotation-order-XYZ stay **13** (`rotation.order === 'XYZ'`); visual MeshBasic scale-default stay **13** (`scale.x === 1` && `scale.y === 1` && `scale.z === 1`); visual MeshBasic up-default stay **13** (`up.x === 0` && `up.y === 1` && `up.z === 0`); visual MeshBasic matrixWorldAutoUpdate-on stay **13** (`matrixWorldAutoUpdate === true`); visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover non-zero `stencilRef` / non-0xff masks / non-Keep `stencilZFail` / `stencilZPass` on unlit midtone stand-ins even when `stencilWrite === false`), not a new layer.
- **Quest 3:** Accidental leftover non-zero `stencilRef` / non-0xff masks / non-Keep `stencilZFail` / `stencilZPass` can still sit on color-only unlit midtone stand-ins even when `stencilWrite === false`. When stencil write is off they are unused GPU state noise and can confuse DCC round-trips; if stencil write were later flipped on, leftovers would test/write the stencil buffer on a TBDR mobile GPU. Pinning the r170 Material defaults keeps the Quest-safe opaque unlit contract. Does **not** enable stencil write, invent non-Always func / non-Keep ops / non-zero ref / non-0xff masks, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including polygonOffset companions / dithering / A2C / blendColor / blendAlpha, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ count unchanged vs v0.74. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.75.0/`

## 0.74.0 — 2026-09-20

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.73 pinned r170 Object3D `rotation.order = 'XYZ'` on packed color-only unlit MeshBasic visual **meshes** (and after v0.72 pinned Material `dithering = false` / `alphaToCoverage = false` as first-class measured flags), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material `polygonOffset = false` / `polygonOffsetFactor = 0` / `polygonOffsetUnits = 0` on packed color-only unlit MeshBasic materials**. v0.52 already assigned these as GPU-state companions; this pulse makes `polygonOffsetFactor` / `polygonOffsetUnits` first-class measured flags. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide/visible/envMap companion/map-intensity companion/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** enable `polygonOffset`. Does **not** invent non-zero factors/units. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `polygonOffset === false` && `polygonOffsetFactor === 0` && `polygonOffsetUnits === 0` plus prior pins intact; visual MeshBasic rotation-order-XYZ stay **13** (`rotation.order === 'XYZ'`); visual MeshBasic scale-default stay **13** (`scale.x === 1` && `scale.y === 1` && `scale.z === 1`); visual MeshBasic up-default stay **13** (`up.x === 0` && `up.y === 1` && `up.z === 0`); visual MeshBasic matrixWorldAutoUpdate-on stay **13** (`matrixWorldAutoUpdate === true`); visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover non-zero `polygonOffsetFactor` / `polygonOffsetUnits` on unlit midtone stand-ins even when `polygonOffset === false`), not a new layer.
- **Quest 3:** Accidental leftover non-zero `polygonOffsetFactor` / `polygonOffsetUnits` can still sit on color-only unlit midtone stand-ins even when `polygonOffset === false`. When offset is off they are unused GPU state noise and can confuse DCC round-trips; if offset were later flipped on, leftovers would bias depth on a TBDR mobile GPU. Pinning the r170 Material defaults keeps the Quest-safe opaque unlit contract. Does **not** enable `polygonOffset`, invent non-zero factors/units, pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / `blendColor` / `blendAlpha` / dithering / A2C, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ count unchanged vs v0.73. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.74.0/`

## 0.73.0 — 2026-09-20

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.72 pinned r170 Material `dithering = false` / `alphaToCoverage = false` on packed color-only unlit MeshBasic **materials** (and after the Object3D transform fence through v0.71 `scale` `(1, 1, 1)` / v0.70 `up` `(0, 1, 0)` / v0.69 `matrixWorldAutoUpdate = true` / v0.67 layers default), **`pinColorOnlyUnlitBasicRotationOrder` / `pinColorOnlyVisualRotationOrder` set r170 Object3D `rotation.order = 'XYZ'` on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Keeps the existing `mesh.rotation` Euler instance — does not replace it with `new Euler()` / `new THREE.Euler()`. Does **not** zero or rewrite `rotation.x` / `rotation.y` / `rotation.z` (lid/latch/tool/fastener intentional local rotations must stay). Does **not** touch `mesh.quaternion` (Three keeps quaternion in sync from Euler when rotation is edited; do not force identity quaternion). Procedural create runs the helper after `pinColorOnlyVisualScale`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `blendColor` / `blendAlpha` / dithering / A2C / prior material pins. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `dithering === false` && `alphaToCoverage === false` plus prior pins intact; visual MeshBasic rotation-order-XYZ **13** (`rotation.order === 'XYZ'`); visual MeshBasic scale-default stay **13** (`scale.x === 1` && `scale.y === 1` && `scale.z === 1`); visual MeshBasic up-default stay **13** (`up.x === 0` && `up.y === 1` && `up.z === 0`); visual MeshBasic matrixWorldAutoUpdate-on stay **13** (`matrixWorldAutoUpdate === true`); visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover non-`XYZ` Object3D Euler `order` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover non-`XYZ` Euler `order` (`YXZ`, `ZYX`, etc.) on color-only unlit midtone visuals can change how subsequent local Euler edits compose even when current xyz values look fine — common DCC export leftover. Pinning the r170 Object3D default (`'XYZ'`) keeps the Quest-safe transform contract without freezing animated pivots and without touching the v0.45 static-body freeze. Does **not** pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `blendColor` / `blendAlpha` / dithering / A2C, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default count unchanged vs v0.72. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.73.0/`

## 0.72.0 — 2026-09-20

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.71 pinned r170 Object3D `scale` `(1, 1, 1)` on packed color-only unlit MeshBasic visual **meshes** (and after the long material-flag fence through v0.68 `blendColor` / `blendAlpha` / v0.53 full stencil suite), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material `dithering = false` / `alphaToCoverage = false` on packed color-only unlit MeshBasic materials**. v0.51 already assigned these as blending/alpha companions; this pulse makes them first-class measured flags. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide/visible/envMap companion/map-intensity companion/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / prior material pins. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `dithering === false` && `alphaToCoverage === false` plus prior pins intact; visual MeshBasic scale-default stay **13** (`scale.x === 1` && `scale.y === 1` && `scale.z === 1`); visual MeshBasic up-default stay **13** (`up.x === 0` && `up.y === 1` && `up.z === 0`); visual MeshBasic matrixWorldAutoUpdate-on stay **13** (`matrixWorldAutoUpdate === true`); visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover `dithering = true` / `alphaToCoverage = true` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover `dithering = true` can add fragment cost on a TBDR mobile GPU for opaque unlit midtones that do not need it. Accidental `alphaToCoverage = true` expects MSAA coverage samples and can produce wrong edges / wasted work on Quest Browser paths that are not relying on A2C for these stand-ins. Pinning the r170 Material defaults keeps the Quest-safe opaque unlit contract. Does **not** pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `blendColor` / `blendAlpha`, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default count unchanged vs v0.71. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.72.0/`

## 0.71.0 — 2026-09-19

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.70 pinned r170 Object3D `up` `(0, 1, 0)` (`Object3D.DEFAULT_UP`) on packed color-only unlit MeshBasic visual **meshes** (and after v0.69 pinned Object3D `matrixWorldAutoUpdate = true` on those same meshes), **`pinColorOnlyUnlitBasicScale` / `pinColorOnlyVisualScale` set r170 Object3D `scale` `(1, 1, 1)` on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Keeps the existing `mesh.scale` Vector3 instance — does not replace it with `new Vector3(1, 1, 1)` or reassign `mesh.scale`. Procedural create runs the helper after `pinColorOnlyVisualUp`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `blendColor` / `blendAlpha` / prior material pins. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3**; visual MeshBasic scale-default **13** (`scale.x === 1` && `scale.y === 1` && `scale.z === 1`); visual MeshBasic up-default stay **13** (`up.x === 0` && `up.y === 1` && `up.z === 0`); visual MeshBasic matrixWorldAutoUpdate-on stay **13** (`matrixWorldAutoUpdate === true`); visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover non-unit / negative / non-uniform Object3D `scale` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover non-unit / negative / non-uniform `scale` (common non-uniform bake, negative axis flip, or non-1 uniform leftovers) on color-only unlit midtone visuals can invert face winding under FrontSide culling (missing draws) and skew world-matrix composition even when local position/rotation are intentional. Pinning the r170 Object3D default (`(1, 1, 1)`) keeps the Quest-safe transform contract without touching the v0.45 static-body freeze. Does **not** pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `up` / `blendColor` / `blendAlpha`, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default count unchanged vs v0.70. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.71.0/`

## 0.70.0 — 2026-09-19

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.69 pinned r170 Object3D `matrixWorldAutoUpdate = true` (`Object3D.DEFAULT_MATRIX_WORLD_AUTO_UPDATE`) on packed color-only unlit MeshBasic visual **meshes** (and after v0.67 pinned Object3D layers default on those same meshes), **`pinColorOnlyUnlitBasicUp` / `pinColorOnlyVisualUp` set r170 Object3D `up` `(0, 1, 0)` (`Object3D.DEFAULT_UP`) on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Keeps the existing `mesh.up` Vector3 instance — does not replace it with `Object3D.DEFAULT_UP.clone()` or `new Vector3()`. Procedural create runs the helper after `pinColorOnlyVisualMatrixWorldAutoUpdate`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `blendColor` / `blendAlpha` / prior material pins. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3**; visual MeshBasic up-default **13** (`up.x === 0` && `up.y === 1` && `up.z === 0`); visual MeshBasic matrixWorldAutoUpdate-on stay **13** (`matrixWorldAutoUpdate === true`); visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover non-Y-up Object3D `up` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover non-Y-up `up` (common Z-up `(0,0,1)` or other non-defaults) on color-only unlit midtone visuals can skew Object3D `lookAt` and related orientation helpers even when local transforms are intentional. Pinning the r170 Object3D default (`(0, 1, 0)`) keeps the Quest-safe transform contract without touching the v0.45 static-body freeze. Does **not** pin `mesh.visible`, change `matrixAutoUpdate`, change `matrixWorldAutoUpdate` / `layers` / `blendColor` / `blendAlpha`, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on count unchanged vs v0.69. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.70.0/`

## 0.69.0 — 2026-09-19

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.68 pinned r170 Material CustomBlending color/alpha companions (`blendColor` `(0, 0, 0)` / `blendAlpha = 0`) on packed color-only unlit MeshBasic **materials** (and after v0.67 pinned Object3D layers default on those same visual meshes), **`pinColorOnlyUnlitBasicMatrixWorldAutoUpdate` / `pinColorOnlyVisualMatrixWorldAutoUpdate` set r170 Object3D `matrixWorldAutoUpdate = true` (`Object3D.DEFAULT_MATRIX_WORLD_AUTO_UPDATE`) on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after `pinColorOnlyVisualLayers`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `layers` / `blendColor` / `blendAlpha` / prior material pins. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/shadow/frustumCulled/renderOrder/layers pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3**; visual MeshBasic matrixWorldAutoUpdate-on **13** (`matrixWorldAutoUpdate === true`); visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover `matrixWorldAutoUpdate = false` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover `matrixWorldAutoUpdate = false` on color-only unlit midtone visuals can stall automatic world-matrix updates from animated parents even when local `matrixAutoUpdate` policy is intentional. Pinning the r170 Object3D default (`true`) keeps the Quest-safe transform contract without touching the v0.45 static-body freeze. Does **not** pin `mesh.visible`, change `matrixAutoUpdate`, change `layers` / `blendColor` / `blendAlpha`, invent custom shaders, or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default count unchanged vs v0.68. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.69.0/`

## 0.68.0 — 2026-09-19

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.67 pinned r170 Object3D layers default (layer 0 only / `mask = 1`) on packed color-only unlit MeshBasic visual **meshes** (and after v0.56 already pinned NormalBlending factor/equation companions `blendSrc` / `blendDst` / `blendEquation` / null alpha companions on those same materials), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material CustomBlending color/alpha companions on packed color-only unlit MeshBasic materials**: `blendColor` `(0, 0, 0)` (keeps the existing Color instance — does not replace it) and `blendAlpha = 0`. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide/visible/envMap companion/map-intensity companion/wireframeLinewidth/wireframe line-style/envMapRotation pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `layers` / `matrixWorldAutoUpdate` / `matrixAutoUpdate`. Does **not** enable CustomBlending or change `blending` away from NormalBlending. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/shadow/frustumCulled/renderOrder/layers pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null` && `visible === true` && `combine === MultiplyOperation` && `reflectivity === 1` && `refractionRatio === 0.98` && `lightMapIntensity === 1` && `aoMapIntensity === 1` && `wireframeLinewidth === 1` && `wireframeLinecap === 'round'` && `wireframeLinejoin === 'round'` && `envMap === null` && `envMapRotation.x === 0` && `envMapRotation.y === 0` && `envMapRotation.z === 0` && `blendColor.r === 0` && `blendColor.g === 0` && `blendColor.b === 0` && `blendAlpha === 0`; visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover non-default `blendColor` / `blendAlpha` on unlit midtone stand-ins even when `blending` is already NormalBlending), not a new layer.
- **Quest 3:** Accidental leftover non-default Material `blendColor` / `blendAlpha` still sit on color-only unlit midtone stand-ins even when `blending` is already NormalBlending. These only affect CustomBlending paths; pinning the r170 Material defaults keeps the Quest-safe opaque unlit contract so leftovers cannot leak into a later blend-mode change. Does **not** pin `mesh.visible`, invent custom shaders, transparent materials, enable CustomBlending, or change `blending` away from NormalBlending. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default count unchanged vs v0.67. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.68.0/`

## 0.67.0 — 2026-09-19

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.66 pinned MeshBasic `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'` on packed color-only unlit MeshBasic **materials** (and after v0.60 pinned `renderOrder = 0` on those same visual meshes), **`pinColorOnlyUnlitBasicLayers` / `pinColorOnlyVisualLayers` set r170 Object3D `layers` default (layer 0 only / `mask = 1`) on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Keeps the existing `mesh.layers` Layers instance — does not replace it with a new `Layers()`. Procedural create runs the helper after `pinColorOnlyVisualRenderOrder`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `matrixWorldAutoUpdate` / `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves; animated pivots stay live). Does **not** enable extra camera/layers tricks, custom layer masks beyond the r170 default, or invent layer-based culling. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/shadow/frustumCulled/renderOrder pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3**; visual MeshBasic layers-default **13** (`layers.mask === 1` && `layers.isEnabled(0)`); visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover non-default Object3D layer masks on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental leftover non-default layer masks can hide draws from the default camera or force unexpected multi-layer membership. Pinning the r170 Object3D default (layer 0 only) keeps the Quest-safe opaque unlit contract. Does **not** pin `mesh.visible`, change `matrixWorldAutoUpdate` / `matrixAutoUpdate`, invent custom shaders, transparent materials, extra camera/layers tricks, or a custom layer mask. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 count unchanged vs v0.66. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.67.0/`

## 0.66.0 — 2026-09-19

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.65 pinned r170 MeshBasic `wireframeLinecap = 'round'` / `wireframeLinejoin = 'round'` on packed color-only unlit MeshBasic **materials** (and after the v0.62 envMap companions `combine` / `reflectivity` / `refractionRatio`, and `isColorOnlyUnlitBasic` already requiring `envMap` falsy), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 MeshBasic `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'` on packed color-only unlit MeshBasic materials** (keeps the existing Euler instance — does not replace it). Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide/visible/envMap companion/map-intensity companion/wireframeLinewidth/wireframe line-style pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `layers` / `matrixWorldAutoUpdate`. Does **not** enable wireframe (`wireframe` stays pinned false from v0.52). Does not force envMap or attach maps. Does not change the `isColorOnlyUnlitBasic` map / lightMap / aoMap / specularMap / alphaMap / envMap gate. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/shadow/frustumCulled/renderOrder pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null` && `visible === true` && `combine === MultiplyOperation` && `reflectivity === 1` && `refractionRatio === 0.98` && `lightMapIntensity === 1` && `aoMapIntensity === 1` && `wireframeLinewidth === 1` && `wireframeLinecap === 'round'` && `wireframeLinejoin === 'round'` && `envMap === null` && `envMapRotation.x === 0` && `envMapRotation.y === 0` && `envMapRotation.z === 0`; visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB leftover non-zero `envMapRotation` on unlit midtone stand-ins even when `envMap` is null), not a new layer.
- **Quest 3:** Accidental leftover non-zero MeshBasic `envMapRotation` still sits on color-only unlit midtone stand-ins even when `envMap` is already gated null. Pinning the r170 MeshBasic default `(0, 0, 0)` keeps the Quest-safe opaque unlit contract. Does **not** pin `mesh.visible`, invent custom shaders, transparent materials, force envMap, attach maps, or enable wireframe. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 count unchanged vs v0.65. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.66.0/`

## 0.65.0 — 2026-09-19

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.64 pinned r170 MeshBasic `wireframeLinewidth = 1` on packed color-only unlit MeshBasic **materials** (and after the v0.52 `wireframe = false` pin), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 MeshBasic wireframe line-style defaults on packed color-only unlit MeshBasic materials**: `wireframeLinecap = 'round'`, `wireframeLinejoin = 'round'`. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide/visible/envMap companion/map-intensity companion/wireframeLinewidth pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `layers` / `matrixWorldAutoUpdate`. Does **not** enable wireframe (`wireframe` stays pinned false from v0.52). Does not force maps or attach maps. Does not change the `isColorOnlyUnlitBasic` map / lightMap / aoMap / specularMap / alphaMap / envMap gate. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/shadow/frustumCulled/renderOrder pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null` && `visible === true` && `combine === MultiplyOperation` && `reflectivity === 1` && `refractionRatio === 0.98` && `lightMapIntensity === 1` && `aoMapIntensity === 1` && `wireframeLinewidth === 1` && `wireframeLinecap === 'round'` && `wireframeLinejoin === 'round'`; visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB `wireframeLinecap` / `wireframeLinejoin` leftovers such as `'butt'` / `'miter'` on unlit midtone stand-ins even when `wireframe === false`), not a new layer.
- **Quest 3:** Accidental non-round MeshBasic `wireframeLinecap` / `wireframeLinejoin` leftovers still sit on color-only unlit midtone stand-ins even when `wireframe` is already false. WebGL ignores them for filled triangles, but non-defaults are unused noise and can confuse DCC round-trips. Pinning the r170 MeshBasic defaults keeps the Quest-safe opaque unlit contract. Does **not** pin `mesh.visible`, invent custom shaders, transparent materials, or enable wireframe. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 count unchanged vs v0.64. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.65.0/`

## 0.64.0 — 2026-09-18

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.63 pinned r170 MeshBasic map-intensity companions (`lightMapIntensity = 1` / `aoMapIntensity = 1`) on packed color-only unlit MeshBasic **materials** (and after the v0.62 envMap companion pin), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 MeshBasic `wireframeLinewidth = 1` on packed color-only unlit MeshBasic materials**. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide/visible/envMap companion/map-intensity companion pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `layers` / `matrixWorldAutoUpdate`. Does **not** enable wireframe (`wireframe` stays pinned false from v0.52). Does not force maps or attach maps. Does not change the `isColorOnlyUnlitBasic` map / lightMap / aoMap / specularMap / alphaMap / envMap gate. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/shadow/frustumCulled/renderOrder pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null` && `visible === true` && `combine === MultiplyOperation` && `reflectivity === 1` && `refractionRatio === 0.98` && `lightMapIntensity === 1` && `aoMapIntensity === 1` && `wireframeLinewidth === 1`; visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB `wireframeLinewidth !== 1` leftovers on unlit midtone stand-ins even when `wireframe === false`), not a new layer.
- **Quest 3:** Accidental non-1 MeshBasic `wireframeLinewidth` leftovers still sit on color-only unlit midtone stand-ins even when `wireframe` is already false. Most WebGL implementations only support linewidth 1; non-1 leftovers are unused noise and can confuse DCC round-trips. Pinning the r170 MeshBasic default keeps the Quest-safe opaque unlit contract. Does **not** pin `mesh.visible`, invent custom shaders, transparent materials, or enable wireframe. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 count unchanged vs v0.63. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.64.0/`

## 0.63.0 — 2026-09-18

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.62 pinned r170 MeshBasic envMap companions (`combine = MultiplyOperation` / `reflectivity = 1` / `refractionRatio = 0.98`) on packed color-only unlit MeshBasic **materials** (and after the v0.61 material visible pin), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 MeshBasic map-intensity companions on packed color-only unlit MeshBasic materials**: `lightMapIntensity = 1`, `aoMapIntensity = 1`. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide/visible/envMap companion pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `layers` / `matrixWorldAutoUpdate`. Does not force lightMap / aoMap or attach maps. Does not change the `isColorOnlyUnlitBasic` map / lightMap / aoMap / specularMap / alphaMap / envMap gate. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/shadow/frustumCulled/renderOrder pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null` && `visible === true` && `combine === MultiplyOperation` && `reflectivity === 1` && `refractionRatio === 0.98` && `lightMapIntensity === 1` && `aoMapIntensity === 1`; visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB `lightMapIntensity !== 1` / `aoMapIntensity !== 1` leftovers on unlit midtone stand-ins even when maps are already gated null), not a new layer.
- **Quest 3:** Accidental non-default MeshBasic `lightMapIntensity` / `aoMapIntensity` leftovers still sit on color-only unlit midtone stand-ins even when maps are already gated null. Pinning r170 MeshBasic map-intensity companions keeps the Quest-safe opaque unlit contract. Does **not** pin `mesh.visible`, invent custom shaders, transparent materials, force lightMap / aoMap, or attach maps. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 count unchanged vs v0.62. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.63.0/`

## 0.62.0 — 2026-09-18

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.61 pinned r170 Material `visible = true` on packed color-only unlit MeshBasic **materials** (and after the v0.60 mesh renderOrder pin), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 MeshBasic envMap companions on packed color-only unlit MeshBasic materials**: `combine = MultiplyOperation` (0), `reflectivity = 1`, `refractionRatio = 0.98`. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide/visible pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `layers` / `matrixWorldAutoUpdate`. Does not force envMap or attach maps. Does not change the `isColorOnlyUnlitBasic` map/envMap gate. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/shadow/frustumCulled/renderOrder pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null` && `visible === true` && `combine === MultiplyOperation` && `reflectivity === 1` && `refractionRatio === 0.98`; visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB MixOperation/AddOperation / non-1 reflectivity / non-0.98 refractionRatio leftovers on unlit midtone stand-ins even when `envMap` is null), not a new layer.
- **Quest 3:** Accidental non-default MeshBasic `combine` / `reflectivity` / `refractionRatio` leftovers still sit on color-only unlit midtone stand-ins even when `envMap` is already gated null. Pinning r170 MeshBasic envMap companions keeps the Quest-safe opaque unlit contract. Does **not** pin `mesh.visible`, invent custom shaders, transparent materials, force envMap, or attach maps. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 count unchanged vs v0.61. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.62.0/`

## 0.61.0 — 2026-09-18

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.60 pinned r170 Object3D `renderOrder = 0` on packed color-only unlit MeshBasic visual **meshes** (and after v0.59 material shadowSide), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material `visible = true` on packed color-only unlit MeshBasic materials**. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision/shadowSide pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `layers` / `matrixWorldAutoUpdate`. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/shadow/frustumCulled/renderOrder pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null` && `visible === true`; visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB `material.visible = false` leftovers on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental `material.visible = false` on color-only unlit midtone stand-ins drops draws silently while LOD still thinks the mesh is visible — wasted setup and missing pixels on a TBDR mobile GPU. Pinning r170 Material default `true` keeps the Quest-safe opaque unlit contract. Does **not** pin `mesh.visible`, invent custom shaders, transparent materials, or force `material.visible = false`. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 count unchanged vs v0.60. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.61.0/`

## 0.60.0 — 2026-09-18

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.59 pinned r170 Material `shadowSide = null` on packed color-only unlit MeshBasic **materials** (and after the v0.50 mesh frustumCulled / v0.49 mesh shadow flags), **`pinColorOnlyUnlitBasicRenderOrder` / `pinColorOnlyVisualRenderOrder` set `mesh.renderOrder = 0` on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after `pinColorOnlyVisualFrustumCulled`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `layers` / `matrixWorldAutoUpdate`. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null`; visual MeshBasic renderOrder-0 **13** (`renderOrder === 0`); visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB non-zero `renderOrder` leftovers on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental non-zero `renderOrder` from DCC / packaged GLB tooling forces separate transparent/opaque sort buckets and can break batching / increase draw overhead even when materials are already Quest-safe opaque unlit stand-ins. Pinning the r170 Object3D default `0` keeps the cheap default path. Does **not** pin `mesh.visible`, change `layers`, invent custom shaders, transparent materials, or force a non-zero renderOrder. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.59. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.60.0/`

## 0.59.0 — 2026-09-18

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.58 pinned r170 Material `precision = null` on packed color-only unlit MeshBasic **materials**, **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material `shadowSide = null` on packed color-only unlit MeshBasic materials**. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors/precision pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null`; visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB FrontSide/BackSide/DoubleSide `shadowSide` leftovers on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental non-null `shadowSide` on color-only unlit midtone stand-ins can force a FrontSide/BackSide/DoubleSide shadow-cast face instead of letting shadow side derive from `side` (already FrontSide). Mesh-level `castShadow`/`receiveShadow` stay pinned false (v0.49) — this is the matching **material** fence. Pinning `shadowSide = null` keeps the Quest-safe opaque unlit contract. Does **not** invent custom shaders, transparent materials, or force a non-null shadowSide. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.58. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.59.0/`

## 0.58.0 — 2026-09-18

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.57 pinned r170 Material `vertexColors = false` on packed color-only unlit MeshBasic **materials**, **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material `precision = null` on packed color-only unlit MeshBasic materials**. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion/vertexColors pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null`; visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB `precision = 'highp'` leftovers on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental non-null `precision` on color-only unlit midtone stand-ins can force highp (or other) fragment precision instead of letting Quest Browser / the WebGLRenderer choose. Pinning `precision = null` keeps the Quest-safe opaque unlit contract. Does **not** invent custom shaders, transparent materials, or force `'mediump'`/`'lowp'`/`'highp'` strings. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.57. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.58.0/`

## 0.57.0 — 2026-09-17

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.56 pinned r170 NormalBlending factor/equation companions (`blendSrc = SrcAlphaFactor` / `blendDst = OneMinusSrcAlphaFactor` / `blendEquation = AddEquation` / null alpha companions) on packed color-only unlit MeshBasic **materials**, **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material `vertexColors = false` on packed color-only unlit MeshBasic materials**. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companion pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false`; visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB `vertexColors = true` leftovers on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental `vertexColors = true` on color-only unlit midtone stand-ins forces a vertex-color program variant and keeps a `color` attribute that `stripUnusedColorOnlyAttributes` would otherwise drop. Pinning `vertexColors = false` keeps the Quest-safe opaque unlit contract. Does **not** invent custom shaders, transparent materials, or enable vertex colors elsewhere. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.56. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.57.0/`

## 0.56.0 — 2026-09-17

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.55 pinned r170 Material boolean GPU-state defaults (`alphaHash = false` / `forceSinglePass = false`) on packed color-only unlit MeshBasic **materials**, **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 NormalBlending factor/equation companions on packed color-only unlit MeshBasic materials**: `blendSrc = SrcAlphaFactor`, `blendDst = OneMinusSrcAlphaFactor`, `blendEquation = AddEquation`, `blendSrcAlpha = null`, `blendDstAlpha = null`, `blendEquationAlpha = null`. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null`; visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB CustomBlending factor leftovers on unlit midtone stand-ins even after NormalBlending mode is restored), not a new layer.
- **Quest 3:** Accidental non-default `blendSrc` / `blendDst` / `blendEquation` (or non-null alpha companions) on color-only MeshBasic visuals still sit on the material after v0.51 pins `blending = NormalBlending`. This pulse fences r170 NormalBlending companions after the v0.55 alphaHash fence so the Quest-safe opaque unlit contract holds on a TBDR mobile GPU. Does **not** invent custom blend equations or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.55. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.56.0/`

## 0.55.0 — 2026-09-17

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.54 pinned r170 Material clipping defaults (`clippingPlanes = null` / `clipIntersection = false` / `clipShadows = false`) on packed color-only unlit MeshBasic **materials**, **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set remaining r170 Material boolean GPU-state defaults on packed color-only unlit MeshBasic materials**: `alphaHash = false`, `forceSinglePass = false`. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false`; visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB `alphaHash=true` / `forceSinglePass=true` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental `alphaHash=true` forces a stochastic discard path; `forceSinglePass=true` can change multi-pass material behavior on a TBDR mobile GPU. This pulse fences r170 Material boolean GPU-state defaults after the v0.54 clipping fence. Does **not** invent custom blend equations or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.54. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.55.0/`

## 0.54.0 — 2026-09-17

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.53 pinned r170 Material stencil defaults (`stencilWrite = false` / `AlwaysStencilFunc` / Keep ops) on packed color-only unlit MeshBasic **materials**, **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material clipping defaults on packed color-only unlit MeshBasic materials**: `clippingPlanes = null`, `clipIntersection = false`, `clipShadows = false`. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false`; visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB non-null `clippingPlanes` / `clipIntersection` / `clipShadows` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental non-null `clippingPlanes` / `clipIntersection` / `clipShadows` on color-only MeshBasic visuals forces clipping-plane fragment work on a TBDR mobile GPU. This pulse fences r170 Material clipping defaults after the v0.53 stencil fence. Does **not** invent custom blend equations or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.53. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.54.0/`

## 0.53.0 — 2026-09-17

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.52 pinned remaining r170 Material GPU-state (`wireframe = false` / `colorWrite = true` / `depthFunc = LessEqualDepth` / `polygonOffset = false`) on packed color-only unlit MeshBasic **materials**, **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set r170 Material stencil defaults on packed color-only unlit MeshBasic materials**: `stencilWrite = false`, `stencilFunc = AlwaysStencilFunc` (519), `stencilRef = 0`, `stencilWriteMask = 0xff`, `stencilFuncMask = 0xff`, `stencilFail = KeepStencilOp` (7680), `stencilZFail = KeepStencilOp`, `stencilZPass = KeepStencilOp`. Existing fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp`; visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB `stencilWrite=true` or non-Always stencil func / non-Keep ops on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental `stencilWrite=true` (or non-Always func / non-Keep ops) on color-only MeshBasic visuals forces stencil test/write on a TBDR mobile GPU. This pulse fences r170 Material stencil defaults after the v0.52 GPU-state fence. Does **not** invent custom blend equations or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.52. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.53.0/`

## 0.52.0 — 2026-09-17

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.51 pinned blending/alpha (`NormalBlending` / `premultipliedAlpha = false` / `alphaTest = 0`) on packed color-only unlit MeshBasic **materials**, **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set remaining r170 Material GPU-state on packed color-only unlit MeshBasic materials**: `wireframe = false`, `colorWrite = true`, `depthFunc = LessEqualDepth` (3), `polygonOffset = false`, `polygonOffsetFactor = 0`, `polygonOffsetUnits = 0`. Existing fog/toneMapped/opaque FrontSide/blending/alpha pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/blending/alpha/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false`; visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB wireframe / colorWrite-off / non-LessEqual depthFunc / polygonOffset on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental wireframe / colorWrite-off / non-LessEqual depthFunc / polygonOffset on color-only MeshBasic visuals forces extra fragment or depth work on a TBDR mobile GPU. This pulse finishes the remaining r170 Material GPU-state fence after v0.51 blending/alpha. Does **not** invent custom blend equations or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.51. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.52.0/`

## 0.51.0 — 2026-09-16

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.50 pinned `frustumCulled = true` on packed color-only unlit MeshBasic visual **meshes** (and after v0.48 opaque FrontSide / fog / toneMapped material pins), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set blending/alpha draw-state on packed color-only unlit MeshBasic materials**: `blending = NormalBlending`, `premultipliedAlpha = false`, `alphaTest = 0` (companions `dithering = false`, `alphaToCoverage = false`). Existing fog/toneMapped/opaque FrontSide pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/shadow/frustumCulled pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0`; visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB CustomBlending / AdditiveBlending / premultiply / alphaTest on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental CustomBlending / AdditiveBlending / premultipliedAlpha / alphaTest on color-only MeshBasic visuals forces blend or discard paths on a TBDR mobile GPU. This pulse finishes the opaque unlit contract v0.48 started. Does **not** invent custom blend equations or transparent materials. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off / frustumCulled-on count unchanged vs v0.50. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.51.0/`

## 0.50.0 — 2026-09-16

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.49 pinned `castShadow = false` / `receiveShadow = false` on packed color-only unlit MeshBasic visual **meshes**, **`pinColorOnlyUnlitBasicFrustumCulled` / `pinColorOnlyVisualFrustumCulled` set `mesh.frustumCulled = true` on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after the shadow-flag pin / material pin / raycast disable / matrix freeze pipeline. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide/shadow pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide`; visual MeshBasic frustumCulled-on **13** (`frustumCulled === true`); visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB `frustumCulled=false` on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental `frustumCulled = false` on color-only MeshBasic visuals forces every Quest 3 draw to skip frustum rejection. This pulse fences the flag true so GPU frustum rejection is restored. Does **not** disable culling or invent a custom strategy. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic / shadow-off count unchanged vs v0.49. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.50.0/`

## 0.49.0 — 2026-09-16

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.48 pinned opaque FrontSide / fog / toneMapped on packed color-only unlit MeshBasic **materials**, **`pinColorOnlyUnlitBasicShadowFlags` / `pinColorOnlyVisualShadowFlags` set `mesh.castShadow = false` and `mesh.receiveShadow = false` on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after the material pin / raycast disable / matrix freeze pipeline. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes). Mapped / lit / morph / interleaved stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / fog/toneMapped/opaque FrontSide pins / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide`; visual MeshBasic shadow-off **13** (`castShadow === false` && `receiveShadow === false`); visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DCC/GLB shadow flags on unlit midtone stand-ins), not a new layer.
- **Quest 3:** Accidental `castShadow` / `receiveShadow` true on color-only MeshBasic visuals would force shadow-map work when a light later casts. This pulse fences those flags off. Does **not** enable shadows elsewhere. Draws / tris / verts / attrBytes / raycast-off / matrix freeze / unique MeshBasic count unchanged vs v0.48. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.49.0/`

## 0.48.0 — 2026-09-16

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.47 pinned `fog = false` / `toneMapped = false`, **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` also set opaque FrontSide draw-state on packed color-only unlit MeshBasic materials**: `transparent = false`, `opacity = 1`, `depthWrite = true`, `depthTest = true`, `side = FrontSide` (0). Existing fog/toneMapped pins stay in the same helper. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at authored / r170 defaults. Collider MeshBasics stay untouched (they keep authored transparent wireframe). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide`; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (accidental DoubleSide / transparent fill on unlit midtones), not a new layer.
- **Quest 3:** Opaque FrontSide is the unlit midtone stand-in contract on a TBDR GPU — accidental DoubleSide doubles fill; transparent/alpha forces blending. Draws / tris / verts / attrBytes / raycast-off / matrix freeze unchanged vs v0.47. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.48.0/`

## 0.47.0 — 2026-09-16

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.46 disabled visual `Mesh.raycast` (and after materials are the shared color-only unlit MeshBasics), **`pinColorOnlyUnlitBasicFlags` / `pinColorOnlyVisualMaterialFlags` set `material.fog = false` and `material.toneMapped = false` on packed color-only unlit MeshBasic materials**. Procedural create pins the three shared wood/brass/steel instances once at share time and again via the entity helper after raycast disable. Packaged ingest pins color-only MeshBasics after the same detect (does not hex-dedupe; does not invent materials). Mapped / lit / morph / interleaved stay at r170 defaults. Collider MeshBasics stay untouched. Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / raycast no-ops / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): unique color-only MeshBasics **3** all `fog === false` && `toneMapped === false`; visual MeshBasic raycast-off stay **13**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (MeshBasic fog varyings/uniforms + lookdev ACES wash on unlit midtones), not a new layer.
- **Quest 3:** Color-only unlit stand-ins skip fog program variants and keep authored midtones under lookdev ACES. Draws / tris / verts / attrBytes / raycast-off / matrix freeze unchanged vs v0.46. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.47.0/`

## 0.46.0 — 2026-09-15

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.45 froze static body world-matrix auto-update, **`disableColorOnlyVisualRaycast` assigns a named no-op `mesh.raycast` (`noopColorOnlyVisualRaycast`) on packed color-only unlit MeshBasic visual meshes** (LOD0/1/2 body + lid/latch/tool + fastener). Procedural create and packaged ingest share the helper and call it after `freezeStaticColorOnlyWorldMatrices`. Colliders keep default `THREE.Mesh.prototype.raycast`. Mapped / lit / morph / interleaved geos are skipped (same color-only unlit MeshBasic gate as the pack pipeline). Does not change draws / tris / unique verts / attrBytes / material sharing / matrix freeze / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. Pick path stays collider AABB (`firstHit` / `collectPickables` + `userData.size`) — no new pick system. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): visual MeshBasic raycast-off **13** / default-raycast **0**; colliders keep `Mesh.prototype.raycast` **5**; frozen MeshBasic stay **3** / live MeshBasic stay **10**; **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (triangle raycast CPU on packed hero/LOD/fastener visuals), not a new layer.
- **Quest 3:** Interaction CPU never triangle-tests packed color-only MeshBasic visuals. Draws / tris / verts / attrBytes / matrix freeze unchanged vs v0.45. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.46.0/`

## 0.45.0 — 2026-09-15

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.44 quantized color-only positions to Float16, **`freezeStaticColorOnlyWorldMatrices` bakes one `entity.updateMatrixWorld(true)` then sets `matrixAutoUpdate = false` on static packed color-only unlit MeshBasic visual leaves that are not descendants of `lid` / `latch` / `tool` pivots**. Procedural create and packaged ingest share the helper. Fastener stays live (`applyFastenerVisual` writes `rotation.z` / `position.z` on the mesh). Colliders stay live (pick AABB). Does not change draws / tris / unique verts / attrBytes / material sharing / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): frozen MeshBasic **3** (body LOD0/1/2) / live MeshBasic **10** (lid/latch/tool LOD meshes + fastener); **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **2820 / 1176 / 432**; fastener attrBytes stay **216**. After simulated upload, CPU attrBytes on those visuals → **0**. Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (per-frame local-matrix updates on static hero/mid/far body meshes), not a new layer.
- **Quest 3:** Fewer per-frame `updateMatrix` calls on static body LOD leaves. Draws / tris / verts / attrBytes unchanged vs v0.44. Lid / latch / tool / fastener motion unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.45.0/`

## 0.44.0 — 2026-09-15

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.43 hooked post-upload CPU-array release, **`packColorOnlyGeometry` (strip → Uint16 compact → this step → StaticDrawUsage + onUpload) quantizes Float32 `position` to Three r170 `Float16BufferAttribute` (WebGL2 `HALF_FLOAT`) on color-only unlit MeshBasic LOD0/1/2 + fastener geometries**. Procedural create and packaged ingest share the helper. Collider hulls are not packed (pick path uses AABB `userData.size`, not visual arrays). Mapped / lit / morph / interleaved geos are skipped. Does not change draws / tris / unique verts / material sharing / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. Bounds are recomputed from quantized `getX`/`getY`/`getZ` before `onUpload` nulls `.array`. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes **4200 → 2820 / 1776 → 1176 / 720 → 432**; fastener attrBytes **360 → 216**. After simulated upload, CPU attrBytes on those visuals → **0** (`BufferAttribute.count` stays). Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (GPU vertex attribute width on color-only MeshBasic), not a new layer.
- **Quest 3:** Half-float positions on color-only MeshBasic visuals (~7 KB → ~4.6 KB procedural pre-upload envelope). Draws / tris / verts unchanged vs v0.43. Hidden LODs keep CPU arrays until that level is first drawn. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.44.0/`

## 0.43.0 — 2026-09-15

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.42 shared color-only MeshBasic instances across LODs, **`packColorOnlyGeometry` (strip → Uint16 compact → this step) sets `StaticDrawUsage` and Three r170 `BufferAttribute.onUpload` so the first GPU upload releases CPU `.array` on color-only unlit MeshBasic LOD0/1/2 + fastener geometries**. Procedural create and packaged ingest share the helper. Collider hulls are not packed (pick path uses AABB `userData.size`, not visual arrays). Mapped / lit / morph / interleaved geos are skipped. Does not change draws / tris / unique verts / material sharing / L4–L5. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Fastener stays outside the LOD merge skip set. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): **pre-upload** draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **4200 / 1776 / 720**; fastener attrBytes stay **360**. After simulated upload, CPU attrBytes on those visuals → **0** (`BufferAttribute.count` stays). Unique canvases stay **0**. Unique MeshBasic instances stay **3**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (CPU copies of static GPU buffers after upload), not a new layer.
- **Quest 3:** After first upload of a color-only MeshBasic visual, the JS heap no longer holds a second copy of that position/index buffer (~7 KB procedural envelope). Draws / tris / verts / pre-upload attrBytes unchanged vs v0.42. Hidden LODs keep CPU arrays until that level is first drawn. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.43.0/`

## 0.42.0 — 2026-09-15

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.41 compacted lingering Uint32 indices (and stripped fastener attrs), **procedural `createToolbox` shares identical color-only unlit `MeshBasicMaterial` instances across LOD levels when the midtone hex is the same**. Verified: `L3_LOD0/1/2_WOOD_COLOR` are all `0x633318`; `L3_LOD0/1_BRASS_COLOR` are both `0xBE7E31`; steel stays LOD0-only (`0xC1C3C9`). v0.37 already aliased woodDark/handleMat to wood *within* a LOD; v0.42 aliases **across** LODs. Unique procedural MeshBasic instances **6 → 3** (wood + brass + steel). Fastener stays on shared brass and outside the LOD merge skip set. Does not merge across body / lidPivot / latchPivot / tool, fold fastener into an LOD batch, or rewrite material type. **Packaged ingest does not hex-dedupe** (same-hex MeshBasics can still differ in side/opacity/transparent; multi-material slots must stay intact — author shared glTF slots in DCC). **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**; LOD attrBytes stay **4200 / 1776 / 720**; fastener attrBytes stay **360**. `userData.l2.uniqueMaterials` **3**. Unique canvases stay **0**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (duplicate MeshBasic instances / material state churn), not a new layer.
- **Quest 3:** Fewer duplicate MeshBasic instances (less material-state churn when switching LODs). Draws / tris / verts / attrBytes unchanged vs v0.41. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.42.0/`

## 0.41.0 — 2026-09-15

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.40 stripped unused `uv` / `normal` on color-only MeshBasic **inside** `mergeSameMaterialMeshes`, **that helper now compactIndexToUint16-copies a >16-bit index into Uint16** when `position.count` ≤ 65535 (no-op otherwise). `concatGeometries` always builds Uint32; weld only rewrites to Uint16 when it actually reduces verts — compact is the safety net. **`fastenerMesh` stays outside the LOD merge skip set** but now gets the same unused-attr strip + compact (procedural + packaged ingest of an authored root-level MeshBasic; do not invent a fastener). Does not rewrite materials. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**. LOD attribute bytes stay **4200 / 1776 / 720** (already Uint16 after v0.39 weld). Fastener attrBytes **840 → 360**. Unit/mock: forced Uint32 index halves byte length; weld-early-return concat Uint32 → Uint16. Unique canvases stay **0**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (index width + skipped-mesh attr footprint), not a new layer.
- **Quest 3:** Tighter GPU index / fastener attribute footprint. LOD draws / tris / unique verts / attrBytes unchanged vs v0.40. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.41.0/`

## 0.40.0 — 2026-09-14

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.39 welded coincident vertices and **kept** UV / normal channels, **that helper strips unused `uv` / `normal` (and other unused channels) from meshes whose material is color-only unlit MeshBasic** (no map / no lighting). Mapped or lit materials keep their attributes. Single-mesh lod groups still skip concat/weld but still strip unused attrs. Fastener stays outside the helper. Does not rewrite materials. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24**; unique verts stay **230 / 100 / 48**. Attribute bytes **8800 → 4200** (LOD0), **3776 → 1776** (LOD1), **1680 → 720** (LOD2) — position-only + index. Unit/mock: 3 coincident MeshBasic boxes stay 8 verts / 36 tris; attrBytes **2992 → 1392**. Author may omit unused attrs in DCC; runtime strip is a safety net. Unique canvases stay **0**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (unused GPU attribute footprint after weld), not a new layer.
- **Quest 3:** Tighter attribute footprint on color-only MeshBasic after merge/weld. Draws / tris / unique verts unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.40.0/`

## 0.39.0 — 2026-09-14

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.38 ran `mergeSameMaterialMeshes` on packaged `lod*` groups, **that helper welds coincident vertices after concat** (Three.js `mergeVertices`-style position hash, 1e-4 m). UV / normal channels stay (first-seen values). Does not merge across body / lidPivot / latchPivot / tool, does not fold colliders or fastener, does not rewrite materials. **Measured** (`userData.lod.stats` / `countGroupStats`; unit tests, not headset): draws stay **6 / 4 / 2** + fastener 1; tris stay **240 / 96 / 24** (index length / 3 — weld does not drop faces). Unique verts **440 → 230** (LOD0), **192 → 100** (LOD1), **48 → 48** (LOD2, no concat). Attribute bytes **16456 → 8800** (LOD0), **7080 → 3776** (LOD1), **1680** unchanged (LOD2). Unit/mock: 3 coincident MeshBasic boxes **72 → 8** verts. Author still prefers pre-welded batches in DCC; runtime weld is a safety net. Unique canvases stay **0**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (post-merge vertex reuse / GPU attribute footprint), not a new layer.
- **Quest 3:** Tighter unique-vert / attribute footprint after same-material merge. Draws unchanged. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.39.0/`

## 0.38.0 — 2026-09-14

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.37 merged same-material meshes on the procedural path, **packaged GLB ingest runs the same `mergeSameMaterialMeshes` helper** on each discovered `lod0` / `lod1` / `lod2` node (direct mesh children; same material reference; skip multi-material). Does not merge across LOD levels, across pivots outside that `lod*` node, into colliders, or into the fastener. Does not rewrite materials. Missing lod names still fail soft (v0.36). **Unit/mock** (not headset): packaged `lod0` 3 MeshBasic → 1 (tris unchanged). Procedural draws stay **6 / 4 / 2** + fastener 1; `drawCallsEstimate` **7**. Unique canvases stay **0**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (packaged LOD draw waste), not a new layer.
- **Quest 3:** Fewer draws inside authored `lod*` groups when a packaged GLB shares material instances. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.38.0/`

## 0.37.0 — 2026-09-14

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.36 wired packaged `lod0` / `lod1` / `lod2` visibility, **procedural LOD0 (and LOD1) merge meshes that share a material inside each static lodGroup**. `bodyL0` wood + woodDark boxes become one wood mesh; `toolL0` steel shaft + tip become one steel mesh; the wood grip stays separate. Lid / latch / tool stay under their pivots (do not merge across body / lidPivot / latchPivot / tool). Fastener is not an LOD mesh. Duplicate same-color MeshBasics collapse: LOD0 `wood` / `woodDark` / `handleMat` share one instance at `L3_LOD0_WOOD_COLOR` (`0x633318`); LOD1 `wood` / `woodDark` / `handleMat` share one instance. **Measured** procedural draws **14 → 6** (LOD0) and **8 → 4** (LOD1); LOD2 stays **2**. Tris unchanged (**240 / 96 / 24** — concatenate, no weld). `drawCallsEstimate` **15 → 7** (LOD0 6 + fastener 1). Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. Packaged ingest from v0.36 left intact (visibility-only, fail-soft when unnamed, no material rewrite). Unique canvases stay **0**. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (procedural LOD0/LOD1 draw waste), not a new layer.
- **Quest 3:** Fewer hero-LOD draws at arm’s length (and a cheaper LOD1 batch). No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.37.0/`

## 0.36.0 — 2026-09-14

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. **Packaged GLB ingest now discovers conventional `lod0` / `lod1` / `lod2` groups** (case-insensitive; optional `_` / `-`; or `userData.lodLevel` 0/1/2 — the names procedural `lodGroup()` and the KTX2 recipe already use), populates `userData.lod` with the same shape as the procedural crate (`current`, `mode`, `distances` 2.4 / 4.5 + hysteresis 0.2, `groups`, `stats`), and calls `setToolboxLod(..., 0)`. `setToolboxLod` / `updateToolboxLod` / keys `1` / `2` / `3` hide inactive packaged LOD meshes with visibility only. Colliders stay out of LOD groups. Fastener is not an LOD mesh. **If no LOD-named groups exist**, fail soft: do not invent fake LODs; the single authored visual set stays visible. Procedural path unchanged (color-only unlit MeshBasic on LOD0 / LOD1 / LOD2; unique canvases 0). Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Loader still does not rewrite materials; author LOD groups as `lod0` / `lod1` / `lod2` and keep `collider_*` / fastener outside those groups.
- **Layers:** still L0–L5. This revisits already-claimed L3 (packaged LOD visibility / draw waste), not a new layer.
- **Quest 3:** When a packaged GLB actually contains named LOD groups, inactive levels do not draw. No invented headset ms. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.36.0/`

## 0.35.0 — 2026-09-13

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.34 made LOD0 unlit MeshBasic with 256² albedo maps, **procedural LOD0 hero meshes drop the albedo `map`** and use color-only unlit `MeshBasicMaterial` with wood / handle midtone **`0x633318`** (`L3_LOD0_WOOD_COLOR`, same as `L3_LOD1_WOOD_COLOR` / `L3_LOD2_WOOD_COLOR`), brass midtone **`0xBE7E31`** (`L3_LOD0_BRASS_COLOR`, same as `L3_LOD1_BRASS_COLOR`), and steel midtone **`0xC1C3C9`** (`L3_LOD0_STEEL_COLOR` — 256² `steelAlbedo` pixel average ~192.53 / 194.53 / 200.53; formula midtone is the same hex). LOD1 stays v0.30 color-only unlit `MeshBasicMaterial` (wood midtone `0x633318` / brass midtone `0xBE7E31`, no map). LOD2 stays v0.29 color-only unlit `MeshBasicMaterial` at wood midtone `0x633318`. Fastener is not an LOD mesh and stays on shared LOD0 brass MeshBasic (now color-only brass midtone). `setToolboxLod` is still visibility-only. Unique canvases **3 → 0** (no remaining procedural LOD albedo canvases). `L2_TEXTURE_SIZE` stays **256**; `L3_LOD_ALBEDO_SIZE` stays **256** as the historical mid/far half-res constant. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not rewrite materials; author LOD0, LOD1, and LOD2 as unlit/basic (or `KHR_materials_unlit`) without a `baseColorTexture` (or tiny 1×1 / vertex color) per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (LOD0 fragment / texture bandwidth), not a new layer.
- **Quest 3:** Cheaper LOD0 fragments at arm’s length (no albedo texture samples / less texture bandwidth on TBDR). Draws / tris unchanged. Unique canvases 3 → 0. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.35.0/`

## 0.34.0 — 2026-09-13

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.33 made LOD0 MeshStandard albedo-only, **procedural LOD0 hero meshes switch from MeshStandard to unlit `MeshBasicMaterial`** with the same 256² wood / brass / steel albedo `map`s. LOD1 stays v0.30 color-only unlit `MeshBasicMaterial` (wood midtone `0x633318` / brass midtone `0xBE7E31`, no map). LOD2 stays v0.29 color-only unlit `MeshBasicMaterial` at wood midtone `0x633318`. Fastener is not an LOD mesh and stays on shared LOD0 brass (now MeshBasic with brass albedo map). `setToolboxLod` is still visibility-only. Unique canvases stay **3** (wood / brass / steel albedo only). `L2_TEXTURE_SIZE` stays **256**; `L3_LOD_ALBEDO_SIZE` stays **256** as the historical mid/far half-res constant. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not rewrite materials; author LOD0 as unlit/basic (or `KHR_materials_unlit`) with ≤256² albedo and no `normalTexture` + ORM; author LOD1 and LOD2 as unlit/basic without a `baseColorTexture` (or tiny 1×1 / vertex color) per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (LOD0 fragment / lighting cost), not a new layer.
- **Quest 3:** Cheaper LOD0 fragments at arm’s length (unlit / no PBR lighting math after v0.20 ambient-only fill). Draws / tris / canvas count unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.34.0/`

## 0.33.0 — 2026-09-13

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.32 dropped LOD0 `normalMap`, **procedural LOD0 MeshStandard drops packed ORM** (`roughnessMap` / `metalnessMap`) and becomes **albedo-only** (keep 256² albedo `map`; constant wood/brass/steel ORM-midtone roughness/metalness: wood/handle **220/255** / **8/255**, brass **95/255** / **230/255**, steel **77.5/255** / **235/255**). LOD1 stays v0.30 color-only unlit `MeshBasicMaterial` (wood midtone `0x633318` / brass midtone `0xBE7E31`, no map). LOD2 stays v0.29 color-only unlit `MeshBasicMaterial` at wood midtone `0x633318`. Fastener is not an LOD mesh and stays on shared LOD0 brass MeshStandard (now albedo-only, no `normalMap`, no ORM). `setToolboxLod` is still visibility-only. Unique canvases **6 → 3** (wood / brass / steel albedo only; drop the three ORM canvases). `L2_TEXTURE_SIZE` stays **256**; `L3_LOD_ALBEDO_SIZE` stays **256** as the historical mid/far half-res constant. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not rewrite materials; author LOD0 PBR that may be albedo-only ≤256² (no `normalTexture`, no ORM texture) with constant roughness/metalness; author LOD1 and LOD2 as unlit/basic (or `KHR_materials_unlit`) without a `baseColorTexture` (or tiny 1×1 / vertex color) per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (LOD0 fragment / ORM-map bandwidth), not a new layer.
- **Quest 3:** Cheaper LOD0 fragments / less ORM-map bandwidth at arm’s length. Draws / tris unchanged. Unique canvases 6 → 3. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.33.0/`

## 0.32.0 — 2026-09-13

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.31 halved LOD0 maps to 256², **procedural LOD0 MeshStandard drops `normalMap`** while keeping **256² albedo + ORM** (photoreal roughness/metalness maps stay). LOD1 stays v0.30 color-only unlit `MeshBasicMaterial` (wood midtone `0x633318` / brass midtone `0xBE7E31`, no map). LOD2 stays v0.29 color-only unlit `MeshBasicMaterial` at wood midtone `0x633318`. Fastener is not an LOD mesh and stays on shared LOD0 brass MeshStandard (now albedo+ORM only, no `normalMap`). `setToolboxLod` is still visibility-only. Unique canvases **9 → 6** (wood / brass / steel albedo + ORM only; drop the three normal canvases). `L2_TEXTURE_SIZE` stays **256**; `L3_LOD_ALBEDO_SIZE` stays **256** as the historical mid/far half-res constant. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not rewrite materials; author LOD0 PBR that may omit `normalTexture` (albedo + ORM ≤256²); author LOD1 and LOD2 as unlit/basic (or `KHR_materials_unlit`) without a `baseColorTexture` (or tiny 1×1 / vertex color) per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (LOD0 fragment / normal-map bandwidth), not a new layer.
- **Quest 3:** Cheaper LOD0 fragments / less normal-map bandwidth at arm’s length. Draws / tris unchanged. Unique canvases 9 → 6. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.32.0/`

## 0.31.0 — 2026-09-13

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.30 made LOD1 color-only, **procedural LOD0 hero maps drop from 512² → 256²** while staying `MeshStandardMaterial` with albedo + ORM + `normalMap` at full modest `normalScale` (v0.12 photoreal contract, half-res). LOD1 stays v0.30 color-only unlit `MeshBasicMaterial` (wood midtone `0x633318` / brass midtone `0xBE7E31`, no map). LOD2 stays v0.29 color-only unlit `MeshBasicMaterial` at wood midtone `0x633318`. Fastener is not an LOD mesh and stays on shared LOD0 brass MeshStandard (now using the 256² LOD0 brass maps). `setToolboxLod` is still visibility-only. Unique canvases stay **9** (wood / brass / steel albedo + ORM + normal, each now 256²). `L3_LOD_ALBEDO_SIZE` stays **256** as the historical mid/far half-res constant. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not rewrite materials; author LOD0 PBR maps at ≤256²; author LOD1 and LOD2 as unlit/basic (or `KHR_materials_unlit`) without a `baseColorTexture` (or tiny 1×1 / vertex color) per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (LOD0 texture bandwidth / footprint), not a new layer.
- **Quest 3:** Cheaper LOD0 texel footprint / bandwidth at arm’s length (256² vs 512² on the nine hero canvases). Draws / tris / canvas count unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.31.0/`

## 0.30.0 — 2026-09-13

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.29 dropped the LOD2 albedo `map`, **procedural LOD1 mid body / lid / latch / tool stub drop their 256² albedo `map`s** and use color-only unlit `MeshBasicMaterial` with wood albedo midtone **`0x633318`** (`L3_LOD1_WOOD_COLOR`, same as `L3_LOD2_WOOD_COLOR`) and brass albedo midtone **`0xBE7E31`** (`L3_LOD1_BRASS_COLOR`). LOD0 keeps the five shared v0.12 materials (512² albedo + ORM + `normalMap`, full modest `normalScale`). LOD2 stays v0.29 color-only `MeshBasicMaterial` at wood midtone `0x633318`. LOD1 materials are color only; no roughness/metalness (those uniforms do not apply). Fastener is not an LOD mesh and stays on shared LOD0 brass MeshStandard. `setToolboxLod` is still visibility-only. Unique canvases **11 → 9** (stop allocating unused 256² `woodLod` / `brassLod`; LOD0 still has its own 512² canvases). Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not rewrite materials; author LOD1 and LOD2 as unlit/basic (or `KHR_materials_unlit`) without a `baseColorTexture` (or tiny 1×1 / vertex color) per the KTX2 recipe. LOD0 stays full PBR.
- **Layers:** still L0–L5. This revisits already-claimed L3 (mid-LOD texture bandwidth), not a new layer.
- **Quest 3:** Cheaper LOD1 fragments at 2.4–4.5 m (no mid albedo sample / less texture bandwidth on TBDR). Draws / tris unchanged. Unique canvases 11 → 9. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.30.0/`

## 0.29.0 — 2026-09-13

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.27/v0.28 made mid/far unlit `MeshBasicMaterial`, **procedural LOD2 far body + lid drop the albedo `map`** and use color-only unlit `MeshBasicMaterial` with wood albedo midtone **`0x633318`** (`L3_LOD2_WOOD_COLOR`). LOD0 keeps the five shared v0.12 materials (512² albedo + ORM + `normalMap`, full modest `normalScale`). LOD1 stays v0.28 `MeshBasicMaterial` at 256² wood / brass. LOD2 materials are color only; no roughness/metalness (those uniforms do not apply). Fastener is not an LOD mesh and stays on shared LOD0 brass MeshStandard. `setToolboxLod` is still visibility-only. Unique canvases still **11** (LOD1 still needs the 256² maps). Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not rewrite materials; author LOD2 as unlit/basic (or `KHR_materials_unlit`) without a `baseColorTexture` (or tiny 1×1 / vertex color) per the KTX2 recipe. Author LOD1 as unlit/basic with ≤256² albedo and no `normalTexture` + ORM.
- **Layers:** still L0–L5. This revisits already-claimed L3 (far-LOD fragment / texture-sample cost), not a new layer.
- **Quest 3:** Cheaper LOD2 fragments at ≥4.5 m (no far albedo sample / less texture bandwidth on TBDR). Draws / tris / canvas count unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.29.0/`

## 0.28.0 — 2026-09-12

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.27 made LOD2 far wood unlit `MeshBasicMaterial`, **procedural LOD1 body / lid / latch / tool stub are unlit `MeshBasicMaterial`** (card-like) with the same 256² wood / brass albedos as v0.26. LOD0 keeps the five shared v0.12 materials (512² albedo + ORM + `normalMap`, full modest `normalScale`). LOD2 stays v0.27 `MeshBasicMaterial` at 256² wood. LOD1 materials are `map` only; no roughness/metalness (those uniforms do not apply). Fastener is not an LOD mesh and stays on shared LOD0 brass MeshStandard. `setToolboxLod` is still visibility-only. Unique canvases still **11**. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not rewrite materials; author LOD1 as unlit/basic (or `KHR_materials_unlit`) with ≤256² albedo and no `normalTexture` + ORM per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (mid-LOD fragment / lighting cost), not a new layer.
- **Quest 3:** Cheaper LOD1 fragments at 2.4–4.5 m (unlit / no PBR lighting math after v0.20 ambient-only fill). Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.28.0/`

## 0.27.0 — 2026-09-12

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.26 bound 256² albedo on LOD1/LOD2, **procedural LOD2 far wood is unlit `MeshBasicMaterial`** (card-like) with that same 256² wood albedo. LOD0 keeps the five shared v0.12 materials (512² albedo + ORM + `normalMap`, full modest `normalScale`). LOD1 stays v0.26 albedo-only `MeshStandardMaterial` at 256² with wood/brass ORM-midtone constants. LOD2 body + lid share one `woodFar` `MeshBasicMaterial` — `map` only; no roughness/metalness (those uniforms do not apply). Fastener is not an LOD mesh and stays on shared LOD0 brass MeshStandard. `setToolboxLod` is still visibility-only. Unique canvases still **11**. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not rewrite materials; author LOD2 as unlit/basic (or `KHR_materials_unlit`) with ≤256² albedo and no `normalTexture` + ORM per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (far-LOD fragment / lighting cost), not a new layer.
- **Quest 3:** Cheaper LOD2 fragments at ≥4.5 m (unlit / no PBR lighting math after v0.20 ambient-only fill). Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.27.0/`

## 0.26.0 — 2026-09-12

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.25 made LOD1 albedo-only (and v0.23 did the same for LOD2), **procedural LOD1 and LOD2 bind half-resolution (256²) albedo** instead of the shared 512² L2 albedos. LOD0 keeps the five shared v0.12 materials (512² albedo + ORM + `normalMap`, full modest `normalScale`). LOD1 body / lid / latch / tool stub and LOD2 body + lid stay `{ normalMap: false, ormMap: false }` with the same constant roughness/metalness classes (wood/handle **220/255** / **8/255**; brass latch **95/255** / **230/255**). LOD1 wood variants + LOD2 `woodFar` share one 256² wood albedo; LOD1 brass has its own 256² brass albedo. Fastener is not an LOD mesh and stays on shared LOD0 brass. `setToolboxLod` is still visibility-only. Unique canvases **9 → 11** (extra 256² wood/brass albedos). Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not strip or downsample maps; author LOD1/LOD2 albedo at ≤256² (and omit `normalTexture` + ORM) per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (mid/far texture bandwidth / footprint), not a new layer.
- **Quest 3:** Cheaper LOD1/LOD2 albedo samples at 2.4 m+ (256² vs 512²). Draws / tris unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.26.0/`

## 0.25.0 — 2026-09-12

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.24 dropped LOD1 `normalMap` and v0.23 dropped LOD2 ORM, **procedural LOD1 no longer samples packed ORM**. LOD0 keeps the five shared v0.12 materials (512² albedo + ORM + `normalMap`, full modest `normalScale`). LOD1 body / lid / latch / tool stub use **separate** materials with the same 512² albedo and `{ normalMap: false, ormMap: false }` — `normalMap = null`, no `roughnessMap` / `metalnessMap`. Wood / woodDark / handle mid use constant roughness **220/255** and metalness **8/255** (same wood ORM midtones as LOD2). Brass latch mid uses constant roughness **95/255** and metalness **230/255** (`brassOrm` G = 70 + n×50 + tarnish×90 at n 0.5 / tarnish 0; B = 230 − tarnish×80) so `MeshStandardMaterial` stays lit under the present-path ambient fill. LOD2 stays v0.23 albedo-only (`woodFar`: no `normalMap`, no ORM; wood-ORM-midtone constants). Fastener is not an LOD mesh and stays on shared LOD0 brass. `setToolboxLod` is still visibility-only. Unique canvases still 9. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not strip maps; author LOD1 without `normalTexture` and without ORM/occlusion-roughness-metallic (albedo only) per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (mid-LOD fragment / texture-sample cost), not a new layer.
- **Quest 3:** Cheaper LOD1 fragments at 2.4–4.5 m (no packed-ORM sample; albedo-only). Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.25.0/`

## 0.24.0 — 2026-09-12

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.13 dropped LOD2 `normalMap`, v0.14 halved LOD1 `normalScale`, and v0.23 dropped LOD2 ORM, **procedural LOD1 no longer samples tangent-space normals**. LOD0 keeps the five shared v0.12 materials (512² albedo + ORM + `normalMap`, full modest `normalScale`). LOD1 body / lid / latch / tool stub use **separate** materials with the same 512² albedo + packed ORM and `{ normalMap: false }` — `normalMap = null`. LOD2 stays v0.23 albedo-only (`woodFar`: no `normalMap`, no ORM; constant roughness **220/255** and metalness **8/255**). Fastener is not an LOD mesh and stays on shared LOD0 brass. `setToolboxLod` is still visibility-only. Unique canvases still 9. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept. Runtime prefers a packaged GLB when present and does not strip maps; author LOD1 without `normalTexture` (keep packed ORM) per the KTX2 recipe.
- **Layers:** still L0–L5. This revisits already-claimed L3 (mid-LOD fragment / texture-sample cost), not a new layer.
- **Quest 3:** Cheaper LOD1 fragments at 2.4–4.5 m (no tangent-space normal sample). Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.24.0/`

## 0.23.0 — 2026-09-12

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.13 dropped LOD2 `normalMap` and v0.14 halved LOD1 `normalScale`, **LOD2 no longer samples packed ORM**. LOD0 keeps the five shared v0.12 materials (512² albedo + ORM + `normalMap`, full modest `normalScale`). LOD1 keeps albedo + ORM + `normalMap` at half scale. LOD2 body + lid still share one far wood material (`woodFar`) with the same 512² albedo, `normalMap = null`, and now `{ ormMap: false }` — no `roughnessMap` / `metalnessMap`. Constant roughness **220/255** and metalness **8/255** match wood ORM midtones (`woodOrm` G = 200 + stripe×40 at stripe 0.5; B is authored 8) so `MeshStandardMaterial` stays lit under the present-path ambient fill. Fastener is not an LOD mesh and stays on shared LOD0 brass. `setToolboxLod` is still visibility-only. Unique canvases still 9. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (far-LOD fragment / texture-sample cost), not a new layer.
- **Quest 3:** Cheaper LOD2 fragments at distance (no packed-ORM sample; albedo-only). Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.23.0/`

## 0.22.0 — 2026-09-12

- **Delta (additive, shipping/perf gate UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **present-path XR framebuffer scale factor clamp to 1** in `examples/interactive-prop`, complementary to v0.15’s pixel-ratio clamp, v0.16’s antialias/MSAA off, v0.17’s NoToneMapping, v0.18’s IBL/`scene.environment` off, v0.19’s directional / punctual off, v0.20’s ambient-only fill, and v0.21’s texture anisotropy clamp. `renderer.setPixelRatio(1)` and `renderer.xr.setFramebufferScaleFactor` are different knobs — the latter scales the XR eye buffer vs the runtime recommended size. Default is often 1, but lookdev or future HUD code can raise it and blow Quest 3 fill. Three r170 exposes `setFramebufferScaleFactor` only (no getter; private default `1.0`) and snapshots the factor in `setSession` (`sessionstart` fires after `isPresenting = true`); a set while presenting does not rebuild the current `XRWebGLLayer`. On `sessionstart` (after 90/72 + FFR 0.75 + v0.15–v0.21) helpers in `present-framebuffer-scale.js` save the last-set / documented lookdev default (1) and write **1**. The example also sets 1 at renderer setup so the current session inherits the clamp. On `sessionend` restore the saved lookdev/desktop scale first (reverse-safe). Raising above 1 is stretch-only and must be measured on-device. Session events only — not per-frame (v0.8 allocation scrub kept). L4/L5 activity and LOD draws/tris unchanged.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.8 / v0.10 / v0.11 / v0.15 / v0.16 / v0.17 / v0.18 / v0.19 / v0.20 / v0.21), not a new layer.
- **Quest 3:** Cheaper / safer present-path eye-buffer fill vs accidental supersampling while presenting. Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.22.0/`

## 0.21.0 — 2026-09-11

- **Delta (additive, shipping/perf gate UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **present-path texture anisotropy clamp to 1** in `examples/interactive-prop`, complementary to v0.15’s pixel-ratio clamp, v0.16’s antialias/MSAA off, v0.17’s NoToneMapping, v0.18’s IBL/`scene.environment` off, v0.19’s directional / punctual off, and v0.20’s ambient-only fill. After those gates, Three.js / WebGL can still request high anisotropic filtering (lookdev or a packaged GLB may use GPU max, often 16). On Quest 3 TBDR that is extra texture bandwidth for little gain at present-path pixel ratio 1 + FFR. Studio docs: mipmaps on; do not assume 16× anisotropy. On `sessionstart` (after 90/72 + FFR 0.75 + v0.15–v0.20) helpers in `present-anisotropy.js` walk toolbox / scene material maps (`map`, `normalMap`, ORM `roughnessMap`/`metalnessMap`, plus packaged GLB slots), dedupe by texture identity, save each `.anisotropy`, and write **1**. On `sessionend` restore saved lookdev values (do not re-upload or dispose). Procedural 512² canvases already author at 1. Session events only — not per-frame (v0.8 allocation scrub kept). L4/L5 activity and LOD draws/tris unchanged.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.8 / v0.10 / v0.11 / v0.15 / v0.16 / v0.17 / v0.18 / v0.19 / v0.20), not a new layer.
- **Quest 3:** Cheaper present-path texture filtering / bandwidth while presenting. Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.21.0/`

## 0.20.0 — 2026-09-11

- **Delta (additive, shipping/perf gate UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **present-path ambient-only fill** (HemisphereLight off + one `AmbientLight`) in `examples/interactive-prop`, complementary to v0.15’s pixel-ratio clamp, v0.16’s antialias/MSAA off, v0.17’s NoToneMapping, v0.18’s IBL/`scene.environment` off, and v0.19’s directional / punctual off. Desktop lookdev still uses `HemisphereLight` (0.55) + `DirectionalLight` `sun` (intensity 0.9). Three r170 still counts a visible HemisphereLight with intensity 0 toward `NUM_HEMI_LIGHTS` — intensity 0 does **not** drop the hemi loop. On `sessionstart` (after 90/72 + FFR 0.75 + v0.15 `setPixelRatio(1)` + v0.16 MSAA-off verify + v0.17 NoToneMapping + v0.18 IBL off + v0.19 directional off) save hemisphere visible + intensity, set `visible = false` and intensity 0, and enable one reused `AmbientLight` at intensity **0.4** (color `0xf0e6d4`; readable fill, not a lookdev match). On `sessionend` restore lookdev hemisphere and disable/detach the present-only ambient (do not leak lights across sessions). Session events only — not per-frame (v0.8 allocation scrub kept). L4/L5 activity and LOD draws/tris unchanged.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.8 / v0.10 / v0.11 / v0.15 / v0.16 / v0.17 / v0.18 / v0.19), not a new layer.
- **Quest 3:** Cheaper present-path hemisphere / fragment cost while presenting. Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.20.0/`

## 0.19.0 — 2026-09-11

- **Delta (additive, shipping/perf gate UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **present-path directional / punctual light off** (hemisphere-only) in `examples/interactive-prop`, complementary to v0.15’s pixel-ratio clamp, v0.16’s antialias/MSAA off, v0.17’s NoToneMapping, and v0.18’s IBL/`scene.environment` off. Desktop lookdev still uses `HemisphereLight` + `DirectionalLight` `sun` (intensity 0.9). Three r170 still counts a visible DirectionalLight with intensity 0 toward `NUM_DIR_LIGHTS` — intensity 0 does **not** drop the punctual loop. On `sessionstart` (after 90/72 + FFR 0.75 + v0.15 `setPixelRatio(1)` + v0.16 MSAA-off verify + v0.17 NoToneMapping + v0.18 IBL off) save visible + intensity, set `visible = false` and intensity 0. On `sessionend` restore lookdev visible + intensity. HemisphereLight stays on. Session events only — not per-frame (v0.8 allocation scrub kept). L4/L5 activity and LOD draws/tris unchanged.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.8 / v0.10 / v0.11 / v0.15 / v0.16 / v0.17 / v0.18), not a new layer.
- **Quest 3:** Cheaper present-path punctual / fragment cost while presenting. Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.19.0/`

## 0.18.0 — 2026-09-11

- **Delta (additive, shipping/perf gate UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **present-path IBL / scene.environment off** in `examples/interactive-prop`, complementary to v0.15’s pixel-ratio clamp, v0.16’s antialias/MSAA off, and v0.17’s NoToneMapping. Desktop lookdev still assigns a PMREM from `RoomEnvironment` to `scene.environment` (`environmentIntensity` default 1). Three r170 `environmentIntensity` is a post-sample multiply after `textureCubeUV` — intensity 0 does **not** drop `USE_ENVMAP`. On `sessionstart` (after 90/72 + FFR 0.75 + v0.15 `setPixelRatio(1)` + v0.16 MSAA-off verify + v0.17 NoToneMapping) save the texture reference + intensity, null `scene.environment`, and write intensity 0 when the property exists. On `sessionend` restore the saved PMREM (do not dispose) + lookdev intensity. Session events only — not per-frame (v0.8 allocation scrub kept). L4/L5 activity and LOD draws/tris unchanged.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.8 / v0.10 / v0.11 / v0.15 / v0.16 / v0.17), not a new layer.
- **Quest 3:** Cheaper present-path IBL / fragment cost while presenting. Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.18.0/`

## 0.17.0 — 2026-09-11

- **Delta (additive, shipping/perf gate UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **present-path NoToneMapping** in `examples/interactive-prop`, complementary to v0.15’s pixel-ratio clamp and v0.16’s antialias/MSAA off. Desktop lookdev still uses `ACESFilmicToneMapping` + `toneMappingExposure` 1.05 at startup. On `sessionstart` (after 90/72 + FFR 0.75 + v0.15 `setPixelRatio(1)` + v0.16 MSAA-off verify) save the current operator + exposure and set `THREE.NoToneMapping` with identity exposure (1). Three r170 applies `renderer.toneMapping` on the output fragment; ACESFilmic is extra ALU on Quest 3 TBDR. On `sessionend` restore saved ACESFilmic + the prior exposure. Session events only — not per-frame (v0.8 allocation scrub kept). L4/L5 activity and LOD draws/tris unchanged.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.8 / v0.10 / v0.11 / v0.15 / v0.16), not a new layer.
- **Quest 3:** Cheaper present-path tone mapping / fragment cost while presenting. Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.17.0/`

## 0.16.0 — 2026-09-11

- **Delta (additive, shipping/perf gate UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **present-path WebGL antialias / MSAA off** in `examples/interactive-prop`, complementary to v0.15’s pixel-ratio clamp. Three.js r170 snapshots `getContextAttributes().antialias` into `XRWebGLLayer` (or projection-layer `samples`); the attribute cannot be flipped on a live context, and recreating the renderer would drop PMREM / GPU uploads / XR bindings. The example therefore **starts** the one renderer with `antialias: QUEST3_XR_ANTIALIAS` (**false**) so the XR layer inherits MSAA off. On `sessionstart` (after 90/72 + FFR 0.75 + v0.15 `setPixelRatio(1)`) helpers in `present-antialias.js` verify the policy; on `sessionend` they restore the *desired* lookdev policy value (true) without inventing a setter. Desktop 2D lookdev shares that context (also MSAA off) — documented trade-off. Not per-frame (v0.8 allocation scrub kept). L4/L5 activity and LOD draws/tris unchanged.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.8 / v0.10 / v0.11 / v0.15), not a new layer.
- **Quest 3:** Lower present-path MSAA / fill cost while presenting. Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.16.0/`

## 0.15.0 — 2026-09-11

- **Delta (additive, shipping/perf gate UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **present-path WebGL pixel-ratio clamp** in `examples/interactive-prop`: on `sessionstart` (after 90/72 + FFR 0.75) save the current ratio and `setPixelRatio(QUEST3_XR_PIXEL_RATIO)` (**1**); on `sessionend` restore the saved desktop/2D ratio (`min(devicePixelRatio, 2)`) and `setSize` to the current window so lookdev is unchanged. Helpers in `present-pixel-ratio.js` are unit-tested without WebXR. Not per-frame (v0.8 allocation scrub kept). L4/L5 activity and LOD draws/tris unchanged.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.8 / v0.10 / v0.11), not a new layer.
- **Quest 3:** Lower XR fragment fill / backbuffer cost while presenting. Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.15.0/`

## 0.14.0 — 2026-09-10

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After v0.13 dropped LOD2 normals, **LOD1 no longer uses full-strength tangent-space `normalScale`**. LOD0 keeps the five shared v0.12 materials (`normalMap` + modest full `normalScale`: wood 0.62, brass 0.30, steel 0.38). LOD1 body / lid / latch / tool stub use **separate** materials with the same 512² albedo + ORM + normal canvases and `normalScale × L3_LOD1_NORMAL_SCALE_MUL` (**0.5**). LOD2 stays v0.13 (`normalMap = null` on its separate wood). Fastener is not an LOD mesh and stays on shared LOD0 brass (no extra draw). `setToolboxLod` is still visibility-only (no per-switch material swap). Unique canvases still 9. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). v0.13 LOD2 no-normals, v0.12 normals, v0.11 visibility-loss, v0.10 tracking-loss, v0.9 hand hover, and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (mid-LOD fragment / lighting-slope cost), not a new layer.
- **Quest 3:** Softer mid-distance normals at 2.4–4.5 m (half LOD0 `normalScale`). Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.14.0/`

## 0.13.0 — 2026-09-10

- **Delta (additive, L3 packaging/perf UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. After the v0.12 normal pass, **LOD2 no longer samples normal maps**. The five shared LOD0/LOD1 materials keep v0.12 `normalMap` + modest `normalScale`. LOD2 body + lid use a separate wood material with the same 512² albedo + ORM and `normalMap = null` (visibility-only `setToolboxLod`; no extra draws). LOD1 keeps the shared normals (simpler Quest-safe choice vs a second material split). Fastener is not an LOD mesh and stays on shared brass. Draw / tri counts unchanged (14 / 8 / 2 + fastener 1). v0.12 normals, v0.11 visibility-loss, v0.10 tracking-loss, v0.9 hand hover, and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L3 (far-LOD fragment cost), not a new layer.
- **Quest 3:** Cheaper LOD2 fragments at distance (no tangent-space normal sample). Draws / tris / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.13.0/`

## 0.12.0 — 2026-09-10

- **Delta (additive, L2 quality UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Shared procedural **512² normal maps** (wood / brass / steel) wired through `MeshStandardMaterial.normalMap` + modest `normalScale`. Height fields follow the v0.6 albedo grain / wear so lighting matches color. Still 5 shared materials; unique canvases 6 → 9 (albedo + ORM + normal). LOD0–2 geometry, hull names, and L4/L5 activity unchanged. Draw estimates unchanged (14 / 8 / 2 + fastener 1). v0.11 visibility-loss, v0.10 tracking-loss, v0.9 hand hover, and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. This revisits already-claimed L2 (same pattern as v0.6 maps instead of plastic colors).
- **Quest 3:** `texturePref` 512, `textureMax` 1024. No 4K. Draws / tris must not worsen. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.12.0/`

## 0.11.0 — 2026-09-10

- **Delta (additive, shipping/a11y UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **visibility-loss safe release** in `examples/interactive-prop`: if the XR session or page loses visibility (`XRSession.visibilityState` `hidden` / `visible-blurred` via `visibilitychange`, or `document.hidden` while presenting) while holding the crate or screwdriver, the prop is released via the existing `endGrab` path (same return-tool / throw / table logic as squeezeend / tracking loss). Returning to `visible` does **not** auto-regrab. v0.10 tracking-loss, v0.9 hover-before-pinch, and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.5 / v0.8 / v0.9 / v0.10), not a new layer.
- **Quest 3:** Props detach when the user lifts the headset, the immersive session blurs, or Quest Browser hides the tab. Draws / tris / textures unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.11.0/`

## 0.10.0 — 2026-09-10

- **Delta (additive, shipping/a11y UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **tracking-loss / null-pose safe release** in `examples/interactive-prop`: if a controller or hand loses tracking (null `getPose` / `getJointPose` on grip, targetRay, or wrist) while holding the crate or screwdriver, or the holding `XRInputSource` disappears mid-grab (`inputsourceschange` removed / Three `disconnected`), the prop is released via the existing `endGrab` path (same return-tool / throw / table logic as squeezeend). Does not leave a prop frozen on a dead grip or wrist. v0.9 hover-before-pinch and v0.8 allocation scrub kept.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.5 / v0.8 / v0.9), not a new layer.
- **Quest 3:** Props detach when the holding pose goes null. Draws / tris / textures unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.10.0/`

## 0.9.0 — 2026-09-10

- **Delta (additive, shipping/input-parity UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **hand-tracking hover** in `examples/interactive-prop`: when presenting, if no controller ray hit this frame, index-finger-tip near-collider (then a tip-origin ray) drives the same `setHover` / `hoverEnter` emissive path. Pinch still use/grab/drive only. Controllers win on a ray hit. v0.8 allocation scrub kept (shared pick list, no per-frame `new` when `P` is off).
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.5 / v0.8), not a new layer.
- **Quest 3:** Hover-before-pinch for bare hands. Draws / tris / textures unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.9.0/`

## 0.8.0 — 2026-09-10

- **Delta (additive, shipping/perf gate UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Quest 3 **frame-loop allocation scrub** in `examples/interactive-prop`: reused pick list, AABB first-hit (no `intersectObjects` garbage), hoisted grip/nearest vectors, pose-history ring, cached tool collider + controller ray, hover without `Object.entries`. Overlay off stays one boolean. L5 activity and LOD draws unchanged.
- **Layers:** still L0–L5. Quality / shipping-gate revisit (like v0.5 / v0.7), not a new layer.
- **Quest 3:** Removes per-frame `new THREE.Vector3` / array construction on the present + non-present animation path. Draw / tri / texture caps unchanged. 90 Hz / 72 fallback **requested**, not measured. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.8.0/`

## 0.7.0 — 2026-09-09

- **Delta (additive, L3 packaging UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Documented Quest 3 `gltf-transform` KTX2/Basis recipe (≤1024², prefer 512; UASTC on ORM/normal; ETC1S on albedo; meshopt). Example **probes** `/packaged/crate-toolbox.glb` and loads it with `KTX2Loader` when present; otherwise keeps v0.6 procedural canvases. No binary GLB/KTX2 checked in.
- **Layers:** still L0–L5. This revisits already-claimed L3 (packaging / GPU-ready textures), not a new layer.
- **Quest 3:** Draws and texture caps unchanged vs v0.6.0. 90 Hz / 72 fallback. Headset ms / FFR still **TODO**.
- **Revision:** `revisions/v0.7.0/`

## 0.6.0 — 2026-09-09

- **Delta (additive, L2 quality UPGRADE):** Same `objectId`, same L0–L5 claim — not a new layer and not NEW. Flat color-only PBR replaced by **shared procedural 512² albedo + ORM** for wood / brass / steel (6 unique canvases, mipmapped). Dark wood and tool grip tint the wood maps. LOD0–2 geometry, hull names, and L4/L5 activity unchanged. Draw estimates unchanged (14 / 8 / 2 + fastener 1).
- **Layers:** still L0–L5. This revisits already-claimed L2 (maps instead of plastic colors).
- **Quest 3:** `texturePref` 512, `textureMax` 1024. No 4K. Draws must not worsen. 90 Hz / 72 fallback. Headset ms / FFR still **TODO**. Next packaging step for a DCC GLB: **KTX2 / Basis** — not blocking this pulse.
- **Revision:** `revisions/v0.6.0/`

## 0.5.0 — 2026-09-09

- **Delta (additive, shipping/perf gate):** Same L0–L5 activity. `perf.notes` now points at [quest-3-on-device-qa](../../../docs/shipping/quest-3-on-device-qa.md). Example **`P`** overlay captures supported/requested Hz, FFR, and approximate rAF Δ (off = ~zero cost). **No headset numbers added** — the results table stays blank until a Quest 3 + Quest Browser run.
- **Layers:** unchanged (L0–L5). Not a new layer claim.
- **Quest 3:** Authoring caps unchanged. Frame time / FFR on headset still **TODO**.
- **Revision:** `revisions/v0.5.0/`

## 0.4.0 — 2026-09-09

- **Delta (additive, L5):** Re-latch cancel (`unlatched` + latch → `closed`). Tool remains unpickable while closed. After `open`, grab the screwdriver (`collider_tool` wins over the body grab hull); use `collider_fastener` (4 turns, plaque `DRIVE n/4` → `SEATED`). Release near the slot (or `T`) returns the tool. Desktop `E` extracts. Declared `feedback` keys pulse `gamepad.hapticActuators` when present. LOD0–2 and L4 hull names unchanged; fastener is +12 tris / +1 draw.
- **Layers:** L5 complete. `layerTarget` met.
- **Quest 3:** Still ≪750k tris / ≪100 draws. Frame time / FFR on headset still **TODO**.
- **Revision:** `revisions/v0.4.0/`

## 0.3.0 — 2026-09-09

- **Delta (additive, L3):** Procedural LOD1 (96 tris / 8 draws) and LOD2 (24 / 2) sit beside unchanged LOD0 (240 / 14). Distance switch 2.4 m / 4.5 m with hysteresis; `1`/`2`/`3` force a level, `0` auto. Same part names and L4 hulls (`collider_grab` / latch / lid / tool); activity still `closed` → `unlatched` → `open`.
- **Layers:** L3 now complete (real cheaper visuals + one-level-at-a-time draw). L5 still not claimed.
- **Quest 3:** Authoring stays under studio caps (≪750k tris, ≪100 draws, no 4K maps). Frame time / FFR on headset still **TODO** (not measured).
- **Revision:** `revisions/v0.3.0/` (NOTES + manifest; no GLB)

## 0.2.1 — 2026-09-09

- **Delta (additive):** Manifest `targetDevice: quest3` + `perf` envelope (90 Hz / 72 fallback, ~1.2k LOD0 tris, ≤1024², ~14 draws, box colliders). No mesh change.
- **Layers:** unchanged (L3 still pending a GLB).
- **Revision:** current files (metadata only)

## 0.2.0 — 2026-09-09

- **Delta (additive):** Hollow L1/L2 body so L4 contents are visible; use-colliders win over the grab hull (8 cm near-hit bias); grab box shrunk to the body. Same `objectId`, same states, same sidecar contract.
- **Layers:** L0–L2, L4 still; L3 unset (no LOD set / no GLB). L5 not claimed (kinematic throw only).
- **Revision:** `revisions/v0.2.0/` (procedural — NOTES only; no GLB)

## 0.1.0 — 2026-09-09

- **Delta:** L0–L2 stand-in + L4 latch/lid activity (`closed` / `unlatched` / `open`). Solid body mesh hid the nested tool; grab hull swallowed lid hits.
- **Layers:** L0, L1, L2, L4
- **Revision:** `revisions/v0.1.0/`
