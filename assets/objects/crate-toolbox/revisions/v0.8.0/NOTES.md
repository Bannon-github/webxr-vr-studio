# crate-toolbox v0.8.0

Shipping/perf-gate UPGRADE on the same `objectId`. Not a new layer. No GLB in this folder.

- Runtime (`examples/interactive-prop`): XR frame-loop allocation scrub — reused pick list, AABB first-hit (no `intersectObjects` garbage), hoisted grip/nearest temps, pose-history ring, cached `toolCollider` + controller ray.
- Overlay `P` off remains one boolean (no sample).
- L5 activity contract unchanged (`closed` → `unlatched` → `open`; tool / fastener / return).
- LOD draws and texture caps unchanged (14/8/2 + fastener 1; pref 512 / max 1024).
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass.
