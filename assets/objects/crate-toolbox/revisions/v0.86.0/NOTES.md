# crate-toolbox v0.86.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v0.85 deleted leftover Mesh `morphTargetInfluences` / `morphTargetDictionary` so both are absent (`=== undefined`; `Object.hasOwn` false) on the **13** packed color-only unlit MeshBasic visual meshes (and after the v0.84 Object3D `animations` empty list), **`pinColorOnlyUnlitBasicMorphAttributes` / `pinColorOnlyVisualMorphAttributes` clear leftover BufferGeometry `morphAttributes` and pin `morphTargetsRelative` to the r170 default `false` on those same packed color-only unlit MeshBasic visual geometries** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after `pinColorOnlyVisualMorphTargets` (after material flags + raycast disable). Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes or morph targets). Mutate the existing plain object: delete own keys. Dispose a leftover BufferAttribute only when `dispose` exists and the attribute is not a live geometry attribute/index and is not still referenced by a skipped mesh. If `morphAttributes` is missing or not a plain object, assign `{}`. Do **not** reassign `null` / `undefined`. Set `morphTargetsRelative = false` only when it is not already false. Does **not** invent morph targets. Does **not** call `updateMorphTargets()`. Does **not** add morphAttributes. Does **not** enable morphing. Does **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary` (v0.85). Does **not** touch Object3D `animations` (v0.84). Does **not** touch Material `glslVersion` (v0.83). Does **not** touch Material `flatShading` (v0.82). Does **not** touch Material `defines` (v0.81). Does **not** touch Material `customProgramCacheKey` (v0.80). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including glslVersion / flatShading / defines / customProgramCacheKey / stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha. Does **not** merge meshes across `body` / `lidPivot` / `latchPivot` / `tool`, fold the fastener into an LOD batch (`fastener` / `fastenerMesh` stay outside `LOD_MERGE_SKIP_NAMES`), rewrite material type, invent a GLB, change matrix freeze, change raycast no-ops, or change L4/L5 interaction. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v0.85 merge → weld → unused-attr strip → Uint16 compact → shared MeshBasic → Float16 → upload-release → matrix-freeze → raycast-disable → material-flag → Mesh-pin → animations-clear → morph-target-clear pipeline stays intact.
- **Verified r170 API (three@0.170.0 in this repo; do not assume):** the BufferGeometry constructor assigns `this.morphAttributes = {}` and `this.morphTargetsRelative = false` (`REVISION` 170). `BufferGeometry.copy` clones each morph-attribute entry onto a fresh `{}`. r170 `WebGLRenderer` calls `WebGLMorphtargets.update` when `geometry.morphAttributes.position`, `.normal`, or `.color` is not `undefined`. An empty array or an empty BufferAttribute stored under one of those keys still counts, and `update` then builds a morph data texture. r170 `BufferAttribute` has no `dispose` method; the helper calls `dispose` only when the function exists and the attribute is not a live `geometry.attributes` / index entry (and not a morph attribute still owned by a skipped mesh). Do **not** reassign `morphAttributes` to `null` or `undefined`. Do **not** invent morph targets. Do **not** call `updateMorphTargets()`. Do **not** add morphAttributes. Do **not** enable morphing. Do **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary`. Do **not** touch Object3D `animations`. Do **not** touch Material `glslVersion` / `flatShading` / `defines` / `customProgramCacheKey` / `onBeforeCompile` / `onBeforeRender`. Do **not** touch Mesh `onBeforeRender` / `onAfterRender` / `onBeforeShadow` / `onAfterShadow`. Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** enable `castShadow` / `receiveShadow`. Do **not** pin `mesh.visible`. Do not change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins.
- **Why:** Accidental DCC / packaged GLB leftover BufferGeometry `morphAttributes` on color-only unlit midtone stand-ins keep those meshes on the r170 morph-target upload path even after v0.85 removed Mesh influence/dictionary state. Empty containers under a key still look present to `WebGLRenderer`. Deleting own keys restores the r170 empty `morphAttributes` object and the default `morphTargetsRelative = false` beside the v0.85 Mesh absence. Geometry morph-attribute pin, not a draw/tri/vert change. Does **not** invent morph targets, call `updateMorphTargets()`, add morphAttributes, enable morphing, touch Mesh morph-target properties, touch Object3D `animations`, touch Material program-cache fields, touch Mesh render or shadow callbacks, touch customDepth/Distance, enable shadows, pin `mesh.visible`, or change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins.
- **Scope vs v0.85 Mesh morph-target clear:** v0.85 hits the **13** packed color-only visual **meshes** with `morphTargetInfluences` / `morphTargetDictionary` absence. v0.86 hits those same **13** visual **geometries** (body LOD leaves + lid/latch/tool + fastener) with BufferGeometry `morphAttributes` emptiness and `morphTargetsRelative === false`. Measured unique geometries are **13** (one geometry per visual; materials stay the shared 3, geometries are not shared). Fresh r170 BufferGeometry already matches this contract. The pin deletes leftover own keys; it does not replace a plain object that is already empty, and it does not assign `morphTargetsRelative` when the value is already `false`. Mapped / lit morph attributes stay authored. Interleaved geometries stay authored. Collider meshes stay untouched. A mesh whose material is shared with a blocked collider or interleaved mesh, or whose geometry is shared with a collider, an interleaved mesh, or a mapped / lit mesh, skips the visual pin. Color-only leftover morphAttributes are cleared rather than kept: that leftover is the pulse. Prior pack steps (weld / quantize / the v0.85 influence pin) still skip geometries that have morph-attribute keys at the time those steps run.
- **Gate (same `isColorOnlyUnlitBasic` as the pack pipeline):** only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip interleaved geos (same interleaved half of `colorOnlyGeometryBlocksPack`). Skip materials shared with a blocked collider / interleaved mesh. Skip geometries shared with a collider, an interleaved mesh, or a mapped / lit mesh. Do not change `isColorOnlyUnlitBasic` (map / lightMap / aoMap / specularMap / alphaMap / envMap still must be falsy).
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope (arrays still present at `attachToolboxLod`). After simulated / real GPU upload, live CPU attrBytes on those visuals → **0**; `BufferAttribute.count` (draws / tris / verts) stays. This pulse does not change that envelope. `morphAttributes-empty` counts visual meshes whose geometry satisfies `Object.keys(geometry.morphAttributes).length === 0` && `geometry.morphTargetsRelative === false`. Unique geometries were measured, not assumed: **13** meshes, **13** distinct `BufferGeometry` instances.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset):

  | Level | Draws | Tris | Unique verts | attrBytes (pre-upload) | Unique MeshBasic | morphAttributes-empty | morphTargets-absent |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | **yes** | **yes** |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | **yes** | **yes** |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | **yes** | **yes** |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | **yes** | **yes** |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | |

  Unique procedural MeshBasic instances stay **3**. Visual MeshBasic morphAttributes-empty **13** (13 unique geometries). Visual MeshBasic morphTargets-absent stay **13**. Visual MeshBasic animations-empty stay **13**. Unique color-only MeshBasic glslVersion-absent stay **3**. flatShading-off stay **3**. defines-absent stay **3**. customProgramCacheKey-default stay **3**. material-render-callbacks-absent stay **3**. Visual MeshBasic shadow-callbacks-absent stay **13**. render-callbacks-absent stay **13**. customDepth/Distance-absent stay **13**. rotation-order-XYZ stay **13**. scale-default stay **13**. up-default stay **13**. matrixWorldAutoUpdate-on stay **13**. layers-default stay **13**. renderOrder-0 stay **13**. frustumCulled-on stay **13**. shadow-off stay **13**. raycast-off stay **13**. Frozen MeshBasic stay **3** (body LOD0/1/2). Live MeshBasic stay **10** (lid/latch/tool LOD meshes + fastener). `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes / matrix freeze / raycast-off / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent / flatShading-off / glslVersion-absent / animations-empty / morphTargets-absent unchanged vs v0.85. morphAttributes-empty **13** is the new count. After simulated upload, CPU attrBytes on those 13 visual geos → 0.

- **Unit/mock evidence** (not headset): r170 `new BufferGeometry()` leaves `morphAttributes` as `{}` and `morphTargetsRelative === false` (`REVISION` 170); a wrong color-only MeshBasic visual with `morphAttributes.position` / `.normal` / an empty `.color` array and `morphTargetsRelative === true` is corrected by `pinColorOnlyUnlitBasicMorphAttributes` in place (same object; leftover BufferAttributes with `dispose` are disposed; a morph attribute that aliases the live `position` attribute is not disposed) without calling `updateMorphTargets()`, adding morphAttributes, enabling morphing, touching Mesh `morphTargetInfluences` / `morphTargetDictionary`, touching Object3D `animations`, touching Material `glslVersion` / `flatShading` / `defines` / `customProgramCacheKey`, or changing fog / stencil / dithering / A2C / customDepth/Distance / `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order`; `morphTargetsRelative` is not assigned when it is already `false`; `null` / `undefined` `morphAttributes` become `{}`; mapped / lit MeshBasic and MeshStandard stay authored; interleaved geometries stay authored; collider meshes stay authored; a visual that shares a material with a collider, or a geometry with a mapped mesh, stays unpinned; `mesh.visible` is not pinned (LOD still uses group/mesh visibility); L4 `tryUse` closed→unlatched→open and L5 `tryDriveFastener` still mutate pivot / fastener transforms. Packaged ingest of a mock `lod*` fixture clears a DCC leftover `morphAttributes.position` BufferAttribute and an empty key on a color-only MeshBasic visual and on the fastener; fail-soft (no lod groups) still pins those meshes. Mapped MeshBasic and collider meshes stay authored.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v0.85.0 set. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub stay as they were. The v0.86 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz / 72 fallback are **requested**, not measured. Clearing leftover BufferGeometry `morphAttributes` on the 13 packed color-only unlit MeshBasic visual geometries is the intended delta (keeps those stand-ins off `WebGLMorphtargets.update`). Headset ms still **TODO**.
