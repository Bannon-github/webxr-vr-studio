# crate-toolbox v1.1.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v1.0.0 pinned leftover BufferGeometry `userData` to the r170 constructor default empty plain object (`geometry.userData = {}`) on the **13** packed color-only unlit MeshBasic visual geometries (and after the v0.99 BufferGeometry `name` pin, the v0.98 BufferAttribute `version` pin, the v0.97 attribute `name` pin, the v0.96 `gpuType` pin, the v0.95 `normalized` pin, the v0.94 `usage` pin, the v0.93 Mesh / Object3D `boundingSphere` clear, the v0.92 BufferGeometry bounds pin, the v0.91 `updateRanges` clear, and the v0.90 `updateRange` pin), **`pinColorOnlyUnlitBasicMaterialUserData` / `pinColorOnlyVisualMaterialUserData` pin leftover Material `userData` to the r170 constructor default empty plain object (`material.userData = {}`)** on the shared color-only MeshBasic materials those visuals use (wood / brass / steel; unique MeshBasic stays **3**). Procedural create runs the visual helper after `pinColorOnlyVisualGeometryUserData`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes). Fail-soft packaged paths (no `lod*` groups) still run the visual pin, including the fastener. Assign a **fresh** `material.userData = {}` in place only when it is not already an empty plain object (missing, non-object, any own string or symbol keys, or a prototype other than `Object.prototype`, including null-prototype objects). Do not share one `{}` across materials. An already-empty plain object is left as-is (same reference, including when two materials already share that empty object). Does **not** replace the material, the geometry, the attributes, or the typed arrays. Does **not** touch Material `name` (left for a later pulse). Does **not** touch prior material program-cache / flag pins (`glslVersion` / `flatShading` / `defines` / `customProgramCacheKey` / `onBeforeCompile` / `onBeforeRender` / `fog`). Does **not** touch BufferGeometry `userData` (v1.0.0 geometry-userData-empty stays). Does **not** touch BufferGeometry `name` (v0.99 geometry-name-empty stays). Does **not** touch Mesh / Object3D `userData` (studio metadata: collider, lod, parts, fastener, and the rest stay). Does **not** touch BufferAttribute `version` (v0.98 version-zero stays). Does **not** touch BufferAttribute `name` (v0.97 name-empty stays). Does **not** touch `gpuType` (v0.96 gpuType-float stays). Does **not** call `setUsage` (v0.94 usage-static stays). Does **not** reassign `normalized` (v0.95 normalized-default stays). Does **not** touch `onUpload` / `onUploadCallback` (v0.43 CPU-release hook stays). Does **not** touch `updateRange` (v0.90). Does **not** touch `updateRanges` (v0.91). Does **not** touch geometry `boundingBox` / `boundingSphere` (v0.92). Does **not** touch Mesh `boundingSphere` (v0.93). Does **not** delete `skinIndex` / `skinWeight` (v0.89). Does **not** touch `drawRange` (v0.88). Does **not** touch `groups` (v0.87). Does **not** touch `morphAttributes` / `morphTargetsRelative` (v0.86). Does **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary` (v0.85). Does **not** touch Object3D `animations` (v0.84). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` / `onBeforeShadow` / `onAfterShadow`. Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** rename the Mesh (`lidMesh` / `latchMesh` / `fastenerMesh` stay). Does **not** change `frustumCulled` (v0.50 already pins `true`). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Does **not** merge meshes across `body` / `lidPivot` / `latchPivot` / `tool`, fold the fastener into an LOD batch (`fastener` / `fastenerMesh` stay outside `LOD_MERGE_SKIP_NAMES`), rewrite material type, invent a GLB, change matrix freeze, change raycast no-ops, or change L4/L5 interaction. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v1.0.0 merge → weld → unused-attr strip → Uint16 compact → shared MeshBasic → Float16 → upload-release → matrix-freeze → raycast-disable → material-flag → Mesh-pin → animations-clear → morph-target-clear → morph-attribute-clear → groups-clear → drawRange-pin → skin-attribute-strip → updateRange-pin → updateRanges-clear → bounds-null → mesh-boundingSphere-absent → usage-static → normalized-default → gpuType-float → name-empty → version-zero → geometry-name-empty → geometry-userData-empty pipeline stays intact; v1.1.0 adds the Material `userData` pin after the geometry-userData pin.
- **Checked installed three@0.170.0:** the Material constructor assigns `this.userData = {}` (a fresh object per instance). `Material.toJSON` writes `data.userData` only when `Object.keys(this.userData).length > 0`. `Material.copy` assigns `this.userData = JSON.parse(JSON.stringify(source.userData))` (a clone, not the shared reference `BufferGeometry.copy` uses). `MaterialLoader` assigns `material.userData = json.userData` when `json.userData !== undefined`. `ObjectLoader.parseMaterials` uses that loader. Stock `GLTFLoader` `loadMaterial` calls `assignExtrasToUserData(material, materialDef)`, which `Object.assign`s material extras onto `material.userData` when extras is an object. The same loader can set `material.name` from `materialDef.name`; this pulse does **not** touch Material `name`. `WebGLRenderer` does not read `material.userData`. v1.0.0 pinned `geometry.userData` and left `material.userData` alone, so DCC / glTF material extras can still sit on the shared color-only MeshBasics. This pulse pins the leftover. It does **not** replace the material, geometry, attributes, or typed arrays. It does **not** touch Material `name`. It does **not** touch BufferGeometry `userData` or `name`. It does **not** touch Mesh / Object3D `userData`. It does **not** touch BufferAttribute `version` / `name` / `gpuType`. It does **not** call `setUsage`. It does **not** reassign `normalized`. It does **not** touch `onUpload` / `onUploadCallback`. It does **not** touch `updateRange` or `updateRanges`. It does **not** touch geometry bounds or Mesh `boundingSphere`. It does **not** change `frustumCulled`. It does **not** rename the Mesh. Do **not** delete `skinIndex` / `skinWeight`. Do **not** touch `drawRange`. Do **not** touch `groups`. Do **not** touch `morphAttributes` / `morphTargetsRelative`. Do **not** touch Mesh morph-target properties. Do **not** touch Object3D `animations`. Mapped / lit stay authored because `isColorOnlyUnlitBasic` is false when `map` / `lightMap` / `aoMap` / `specularMap` / `alphaMap` / `envMap` is set.
- **Why:** Stock GLTFLoader copies material extras onto `material.userData`. After the v1.0.0 geometry pin, DCC / glTF material extras can still sit on `material.userData` of the shared static packed color-only MeshBasics. `WebGLRenderer` does not read `material.userData`, so leftover extras do not change the draw but retain strings and objects on Quest 3 TBDR static props. Clearing to a fresh empty plain object is load-time L3 packaging only: no draw / tri / attrBytes change. These stand-ins keep float-typed position (+ index); `vertexColors` stays false (v0.57) and unused color/uv/normal/skin attrs are already stripped on the clean path. Pinning an empty plain `material.userData` restores the r170 constructor default. Pre-upload draws / tris / verts / attrBytes on clean procedural meshes are unchanged vs v1.0.0. This pulse does not invent headset milliseconds.
- **Scope vs v1.0.0 geometry-userData pin:** v1.0.0 hits the **13** packed color-only visual **geometries** so each `geometry.userData` is an empty plain object. v1.1.0 hits the **3** unique color-only MeshBasic **materials** those visuals share (wood / brass / steel, including the fastener on brass) so each `material.userData` is an empty plain object. Measured unique materials stay **3**. Fresh r170 `Material` already uses `userData = {}`, and each constructor call allocates its own object. Mapped / lit `material.userData` stays authored. Interleaved materials stay authored. A mesh whose material is shared with a blocked collider or interleaved mesh, or whose geometry is shared with a collider, an interleaved mesh, or a mapped / lit mesh, skips the visual pin. Color-only leftover non-empty `material.userData` is pinned rather than kept: that leftover is the pulse.
- **Gate (same `isColorOnlyUnlitBasic` as `pinColorOnlyVisualGeometryUserData`):** only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip interleaved geos (same interleaved half of the geometry-userData pin). Skip materials shared with a blocked collider / interleaved mesh. Skip geometries shared with a collider, an interleaved mesh, or a mapped / lit mesh. Do not change `isColorOnlyUnlitBasic` (map / lightMap / aoMap / specularMap / alphaMap / envMap still must be falsy).
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope (arrays still present at `attachToolboxLod`). After simulated / real GPU upload, live CPU attrBytes on those visuals → **0**; `BufferAttribute.count` (draws / tris / verts) stays. Replacing `material.userData` does not change attribute byte length. `material-userData-empty` counts unique color-only MeshBasic materials whose `material.userData` is an empty plain object (`Object.prototype`, no own string or symbol keys) before upload — the same unique-material set as `glslVersion-absent` / `flatShading-off` / `defines-absent`. `geometry-userData-empty` still counts visual meshes whose `geometry.userData` is an empty plain object. `geometry-name-empty` still counts visual meshes whose `geometry.name === ''`. `version-zero` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `version === 0`. `name-empty` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `name === ''`. `gpuType-float` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `gpuType === FloatType`. `normalized-default` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `normalized === false`. `usage-static` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `usage === StaticDrawUsage`. `mesh-boundingSphere-absent` still counts visual meshes whose `boundingSphere === undefined`. `bounds-null` still counts visual meshes whose geometry has `boundingBox === null` && `boundingSphere === null` before upload. Unique materials were measured, not assumed: **13** meshes, **3** distinct `MeshBasicMaterial` instances, **3** distinct empty `material.userData` objects. Wood meshes share one bag; brass (including the fastener) shares another; steel has its own. `countGroupStats` counts meshes, tris, verts, and attribute bytes, not `material.userData`, so the pin does not change the draws/tris/verts/attrBytes envelope.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset):

  | Level | Draws | Tris | Unique verts | attrBytes (pre-upload) | Unique MeshBasic | material-userData-empty | geometry-userData-empty | geometry-name-empty | version-zero | name-empty | gpuType-float | normalized-default | usage-static | mesh-boundingSphere-absent | bounds-null | updateRanges-empty | updateRange-default | skinAttributes-absent | drawRange-default | groups-empty |
  | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | | | | | | | | | | | | | | |

  Unique procedural MeshBasic instances stay **3**. Unique color-only MeshBasic material-userData-empty **3** (empty plain `material.userData`, one object per material). Visual MeshBasic geometry-userData-empty stay **13** (one object per geometry). Visual MeshBasic geometry-name-empty stay **13**. Visual MeshBasic version-zero stay **13**. Visual MeshBasic name-empty stay **13**. Visual MeshBasic gpuType-float stay **13**. Visual MeshBasic normalized-default stay **13**. Visual MeshBasic usage-static stay **13**. Visual MeshBasic mesh-boundingSphere-absent stay **13**. Visual MeshBasic bounds-null stay **13** (13 unique geometries; `boundingBox === null` && `boundingSphere === null` before upload). Visual MeshBasic updateRanges-empty stay **13**. Visual MeshBasic updateRange-default stay **13**. Visual MeshBasic skinAttributes-absent stay **13**. Visual MeshBasic drawRange-default stay **13**. Visual MeshBasic groups-empty stay **13**. Visual MeshBasic morphAttributes-empty stay **13**. Visual MeshBasic morphTargets-absent stay **13**. Visual MeshBasic animations-empty stay **13**. Unique color-only MeshBasic glslVersion-absent stay **3**. flatShading-off stay **3**. defines-absent stay **3**. customProgramCacheKey-default stay **3**. material-render-callbacks-absent stay **3**. Visual MeshBasic shadow-callbacks-absent stay **13**. render-callbacks-absent stay **13**. customDepth/Distance-absent stay **13**. rotation-order-XYZ stay **13**. scale-default stay **13**. up-default stay **13**. matrixWorldAutoUpdate-on stay **13**. layers-default stay **13**. renderOrder-0 stay **13**. frustumCulled-on stay **13**. shadow-off stay **13**. raycast-off stay **13**. Frozen MeshBasic stay **3** (body LOD0/1/2). Live MeshBasic stay **10** (lid/latch/tool LOD meshes + fastener). `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes / matrix freeze / raycast-off / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent / flatShading-off / glslVersion-absent / animations-empty / morphTargets-absent / morphAttributes-empty / groups-empty / drawRange-default / skinAttributes-absent / updateRange-default / updateRanges-empty / bounds-null / mesh-boundingSphere-absent / usage-static / normalized-default / gpuType-float / name-empty / version-zero / geometry-name-empty / geometry-userData-empty unchanged vs v1.0.0 on clean procedural meshes. material-userData-empty **3** is the new count.

- **Unit/mock evidence** (not headset): installed three@0.170.0 `Material` constructs `userData` as an empty plain object and does not share that object across instances; `toJSON` writes `userData` only when it has own keys; `copy` clones `userData` (`JSON.parse(JSON.stringify(...))`) rather than sharing the reference; `MaterialLoader` assigns `json.userData`; `ObjectLoader` assigns material `userData` without copying extras onto the Mesh. A wrong color-only MeshBasic with leftover `material.userData` extras is pinned to a fresh `{}` by `pinColorOnlyUnlitBasicMaterialUserData` without replacing the mesh, the material, the geometry, the attributes, or the typed arrays, without mutating the leftover extras object, without touching Material `name`, without touching `glslVersion` / `flatShading` / `defines` / `fog` / `onBeforeCompile` / `customProgramCacheKey`, without touching BufferGeometry `userData` or `name`, without touching Mesh / Object3D `userData`, without touching BufferAttribute `version` / `name` / `gpuType` / `normalized` / non-static `usage`, without replacing `onUploadCallback`, without calling `computeBoundingBox` / `computeBoundingSphere`, without touching geometry `boundingBox` / `boundingSphere`, without deleting Mesh `boundingSphere`, without touching `updateRanges` / `updateRange`, without deleting `skinIndex` / `skinWeight`, and without touching `drawRange` / `groups` / `morphAttributes` / `morphTargetsRelative` / Mesh `morphTargetInfluences` / `morphTargetDictionary` / Object3D `animations`; a deleted `material.userData`, `null`, a string, an array, null-prototype object, non-enumerable own key, symbol own key, and a non-`Object.prototype` prototype are each replaced with a fresh empty plain object; an already-empty `material.userData` is left as the same object, including when two color-only materials already share that empty object; two leftovers that need replacement receive distinct `{}` objects; two meshes that share one material receive one replacement bag; a material array keeps authored `userData`; authored Material `name` is left alone by this pulse; mapped MeshBasic and MeshStandard keep authored `material.userData`; interleaved materials keep authored `material.userData`; collider meshes keep authored `material.userData` and collider `userData`; a visual that shares a material with a collider, or a geometry with a mapped mesh, stays unpinned; `mesh.visible` is not pinned; the Mesh name is not cleared (`lidMesh` / `latchMesh` / `fastenerMesh` stay); `frustumCulled` stays true; L4 `tryUse` closed→unlatched→open and L5 `tryDriveFastener` still mutate pivot / fastener transforms. Packaged ingest of a mock `lod*` fixture pins a DCC leftover `material.userData` on a color-only MeshBasic visual and on the fastener while the v1.0.0 geometry pin still clears leftover non-empty `geometry.userData` and the v0.99 name pin still clears leftover non-empty `geometry.name`; fail-soft (no lod groups) still pins those meshes, including a deleted `material.userData`, leaves an already-empty `material.userData` as the same object, and leaves collider material `userData` plus Mesh `userData` authored. Mapped MeshBasic and collider meshes stay authored. The v0.43 `onUpload` CPU-release hook still runs. Mesh `boundingSphere` on the pinned visual stays the absence the v0.93 helper already produced; geometry `boundingBox` / `boundingSphere` stay the `null` the v0.92 helper already assigned; `updateRanges` stay the array the v0.91 helper already emptied; `updateRange` stays the object the v0.90 helper already pinned; `drawRange` stays the object the v0.88 helper already pinned; `groups` stay the array the v0.87 helper already cleared.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v1.0.0 set. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub stay as they were. The v1.1.0 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz / 72 fallback are **requested**, not measured. Pinning leftover Material `userData` to a fresh empty plain object on the 3 shared color-only unlit MeshBasic materials is the intended delta. Headset ms still **TODO**. Do not require 207/240 Hz.
