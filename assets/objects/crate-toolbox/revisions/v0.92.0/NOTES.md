# crate-toolbox v0.92.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v0.91 cleared leftover BufferAttribute `updateRanges` so `Array.isArray(updateRanges) && updateRanges.length === 0` on every non-interleaved `BufferAttribute` plus `geometry.index` when it is a BufferAttribute, on the **13** packed color-only unlit MeshBasic visual geometries (and after the v0.90 BufferAttribute `updateRange` pin, the v0.89 `skinIndex` / `skinWeight` strip, the v0.88 BufferGeometry `drawRange` pin, the v0.87 BufferGeometry `groups` clear, the v0.86 BufferGeometry `morphAttributes` clear, the v0.85 Mesh `morphTargetInfluences` / `morphTargetDictionary` absence, and the v0.84 Object3D `animations` empty list), **`pinColorOnlyUnlitBasicBounds` / `pinColorOnlyVisualBounds` pin leftover BufferGeometry `boundingBox` and `boundingSphere` to the r170 constructor `null`** on those same packed color-only unlit MeshBasic visual geometries (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the visual helper after `pinColorOnlyVisualUpdateRanges`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes). Fail-soft packaged paths (no `lod*` groups) still run the visual pin, including the fastener. Assign `geometry.boundingBox = null` and `geometry.boundingSphere = null`. Does **not** invent `Box3` or `Sphere` objects. Does **not** call `computeBoundingBox` or `computeBoundingSphere` in the pin. Does **not** touch `updateRanges` (v0.91). Does **not** touch `updateRange` (v0.90). Does **not** change `usage` (v0.43 `StaticDrawUsage` stays). Does **not** replace attributes or the geometry. Does **not** delete `skinIndex` / `skinWeight` (v0.89). Does **not** touch `drawRange` (v0.88). Does **not** touch `groups` (v0.87). Does **not** touch `morphAttributes` / `morphTargetsRelative` (v0.86). Does **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary` (v0.85). Does **not** touch Object3D `animations` (v0.84). Does **not** touch Material `glslVersion` / `flatShading` / `defines` / `customProgramCacheKey` / `onBeforeCompile` / `onBeforeRender`. Does **not** touch Mesh `onBeforeRender` / `onAfterRender` / `onBeforeShadow` / `onAfterShadow`. Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `frustumCulled` (v0.50 already pins `true`). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Does **not** merge meshes across `body` / `lidPivot` / `latchPivot` / `tool`, fold the fastener into an LOD batch (`fastener` / `fastenerMesh` stay outside `LOD_MERGE_SKIP_NAMES`), rewrite material type, invent a GLB, change matrix freeze, change raycast no-ops, or change L4/L5 interaction. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v0.91 merge → weld → unused-attr strip → Uint16 compact → shared MeshBasic → Float16 → upload-release → matrix-freeze → raycast-disable → material-flag → Mesh-pin → animations-clear → morph-target-clear → morph-attribute-clear → groups-clear → drawRange-pin → skin-attribute-strip → updateRange-pin → updateRanges-clear pipeline stays intact; v0.92 adds the bounds pin after the updateRanges clear.
- **Checked installed three@0.170.0:** the `BufferGeometry` constructor assigns `this.boundingBox = null` and `this.boundingSphere = null`. `computeBoundingBox` allocates a `Box3` only when `boundingBox === null`, then fills it from `position`. `computeBoundingSphere` allocates a `Sphere` only when `boundingSphere === null`, then derives center and radius from `position`. `WebGLRenderer.projectObject` skips the object when `frustumCulled` is true and `Frustum.intersectsObject` returns false. `Frustum.intersectsObject` uses `object.boundingSphere` when that property is not `undefined`. A Mesh does not assign `object.boundingSphere`, so the else branch copies `geometry.boundingSphere` and calls `geometry.computeBoundingSphere()` only when that sphere is `null`. A leftover non-null sphere is transformed by `matrixWorld` and tested as-is. `boundingBox` is not the frustum input; it is the paired constructor field and is cleared with the sphere. This pulse assigns both to `null`. It does **not** call `computeBoundingBox` or `computeBoundingSphere`. It does **not** invent `Box3` / `Sphere` objects. It does **not** touch `updateRanges` or `updateRange`. It does **not** change `usage`. It does **not** change `frustumCulled`. Do **not** replace attributes or the geometry. Do **not** delete `skinIndex` / `skinWeight`. Do **not** touch `drawRange`. Do **not** touch `groups`. Do **not** touch `morphAttributes` / `morphTargetsRelative`. Do **not** touch Mesh morph-target properties. Do **not** touch Object3D `animations`. Do **not** touch Material program-cache fields. Do **not** touch Mesh render or shadow callbacks. Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** enable shadows. Do **not** pin `mesh.visible`. Do not change `matrixAutoUpdate`. Mapped / lit stay authored because `isColorOnlyUnlitBasic` is false when `map` / `lightMap` / `aoMap` / `specularMap` / `alphaMap` / `envMap` is set.
- **Why:** Accidental DCC / exporter leftover non-null `boundingSphere` makes r170 frustum culling test that stale sphere while `Mesh.frustumCulled` stays on. Assigning `null` restores the compute-on-demand path the next time a sphere is required. `boundingBox` is cleared to the same constructor default. On the packed color-only path, v0.44 `quantizePositionToFloat16` and the v0.43 upload hook compute bounds while CPU arrays still exist; this pin then assigns `null`. The `onUpload` callback recomputes when bounds are `null` before it releases the CPU array, so a post-upload cull does not read a null `.array`. Pre-upload draws / tris / verts / attrBytes on clean procedural meshes are unchanged vs v0.91. This pulse does not invent headset milliseconds.
- **Scope vs v0.91 updateRanges clear:** v0.91 hits the **13** packed color-only visual **geometries** with empty BufferAttribute `updateRanges`. v0.92 hits those same **13** visual **geometries** (body LOD leaves + lid/latch/tool + fastener) with `boundingBox === null` and `boundingSphere === null` before upload. Measured unique geometries are **13** (one geometry per visual; materials stay the shared 3, geometries are not shared). Fresh r170 `BufferGeometry` already assigns both `null`. Pack computes them; the pin assigns `null` again. Mapped / lit bounds stay authored. Interleaved geometries stay authored. A mesh whose material is shared with a blocked collider or interleaved mesh, or whose geometry is shared with a collider, an interleaved mesh, or a mapped / lit mesh, skips the visual pin. Color-only leftover bounds are cleared rather than kept: that leftover is the pulse.
- **Gate (same `isColorOnlyUnlitBasic` as the pack pipeline):** only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip interleaved geos (same interleaved half of `colorOnlyGeometryBlocksPack`). Skip materials shared with a blocked collider / interleaved mesh. Skip geometries shared with a collider, an interleaved mesh, or a mapped / lit mesh. Do not change `isColorOnlyUnlitBasic` (map / lightMap / aoMap / specularMap / alphaMap / envMap still must be falsy).
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope (arrays still present at `attachToolboxLod`). After simulated / real GPU upload, live CPU attrBytes on those visuals → **0**; `BufferAttribute.count` (draws / tris / verts) stays. Nulling bounds does not change attribute byte length. `bounds-null` counts visual meshes whose geometry has `boundingBox === null` && `boundingSphere === null` before upload. Unique geometries were measured, not assumed: **13** meshes, **13** distinct `BufferGeometry` instances. `countGroupStats` counts meshes, tris, verts, and attribute bytes, not bounds, so the pin does not change the draws/tris/verts/attrBytes envelope. After simulated upload the v0.43 callback has recomputed a finite sphere and box.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset):

  | Level | Draws | Tris | Unique verts | attrBytes (pre-upload) | Unique MeshBasic | bounds-null | updateRanges-empty | updateRange-default | skinAttributes-absent | drawRange-default | groups-empty |
  | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | | | | | |

  Unique procedural MeshBasic instances stay **3**. Visual MeshBasic bounds-null **13** (13 unique geometries; `boundingBox === null` && `boundingSphere === null` before upload). Visual MeshBasic updateRanges-empty stay **13**. Visual MeshBasic updateRange-default stay **13**. Visual MeshBasic skinAttributes-absent stay **13**. Visual MeshBasic drawRange-default stay **13**. Visual MeshBasic groups-empty stay **13**. Visual MeshBasic morphAttributes-empty stay **13**. Visual MeshBasic morphTargets-absent stay **13**. Visual MeshBasic animations-empty stay **13**. Unique color-only MeshBasic glslVersion-absent stay **3**. flatShading-off stay **3**. defines-absent stay **3**. customProgramCacheKey-default stay **3**. material-render-callbacks-absent stay **3**. Visual MeshBasic shadow-callbacks-absent stay **13**. render-callbacks-absent stay **13**. customDepth/Distance-absent stay **13**. rotation-order-XYZ stay **13**. scale-default stay **13**. up-default stay **13**. matrixWorldAutoUpdate-on stay **13**. layers-default stay **13**. renderOrder-0 stay **13**. frustumCulled-on stay **13**. shadow-off stay **13**. raycast-off stay **13**. Frozen MeshBasic stay **3** (body LOD0/1/2). Live MeshBasic stay **10** (lid/latch/tool LOD meshes + fastener). `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes / matrix freeze / raycast-off / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent / flatShading-off / glslVersion-absent / animations-empty / morphTargets-absent / morphAttributes-empty / groups-empty / drawRange-default / skinAttributes-absent / updateRange-default / updateRanges-empty unchanged vs v0.91 on clean procedural meshes. bounds-null **13** is the new count. After simulated upload, CPU attrBytes on those 13 visual geos → 0, and bounds have been recomputed by the upload callback.

- **Unit/mock evidence** (not headset): installed three@0.170.0 `BufferGeometry` leaves `boundingBox` and `boundingSphere` `null`; a wrong color-only MeshBasic with leftover non-null bounds is assigned `null` by `pinColorOnlyUnlitBasicBounds` without replacing the mesh, the geometry, or the attributes, without calling `computeBoundingBox` / `computeBoundingSphere`, without mutating the detached `Box3` / `Sphere`, without touching `updateRanges` / `updateRange` / `usage`, without deleting `skinIndex` / `skinWeight`, and without touching `drawRange` / `groups` / `morphAttributes` / `morphTargetsRelative` / Mesh `morphTargetInfluences` / `morphTargetDictionary` / Object3D `animations`; mapped MeshBasic and MeshStandard keep authored bounds; interleaved geometries keep authored bounds; collider meshes keep authored bounds; a visual that shares a material with a collider, or a geometry with a mapped mesh, stays uncleared; `mesh.visible` is not pinned; `frustumCulled` stays true; L4 `tryUse` closed→unlatched→open and L5 `tryDriveFastener` still mutate pivot / fastener transforms. Packaged ingest of a mock `lod*` fixture clears DCC leftover bounds on a color-only MeshBasic visual and on the fastener; fail-soft (no lod groups) still clears those meshes. Mapped MeshBasic and collider meshes stay authored. `updateRanges` on the cleared visual stays the array the v0.91 helper already emptied; `updateRange` stays the object the v0.90 helper already pinned; `drawRange` stays the object the v0.88 helper already pinned; `groups` stay the array the v0.87 helper already cleared.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v0.91.0 set. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub stay as they were. The v0.92 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz / 72 fallback are **requested**, not measured. Pinning leftover BufferGeometry `boundingBox` / `boundingSphere` to `null` on the 13 packed color-only unlit MeshBasic visual geometries is the intended delta (keeps a stale sphere off the frustum test until compute-on-demand, and the upload callback restores bounds before CPU arrays go away). Headset ms still **TODO**. Do not require 207/240 Hz.
