# crate-toolbox v0.93.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v0.92 pinned leftover BufferGeometry `boundingBox` / `boundingSphere` to the r170 constructor `null` on the **13** packed color-only unlit MeshBasic visual geometries (and after the v0.91 BufferAttribute `updateRanges` clear, the v0.90 BufferAttribute `updateRange` pin, the v0.89 `skinIndex` / `skinWeight` strip, the v0.88 BufferGeometry `drawRange` pin, the v0.87 BufferGeometry `groups` clear, the v0.86 BufferGeometry `morphAttributes` clear, the v0.85 Mesh `morphTargetInfluences` / `morphTargetDictionary` absence, and the v0.84 Object3D `animations` empty list), **`pinColorOnlyUnlitBasicMeshBoundingSphere` / `pinColorOnlyVisualMeshBoundingSphere` delete leftover Mesh / Object3D `boundingSphere` so the property is absent (`mesh.boundingSphere === undefined`)** on those same packed color-only unlit MeshBasic visual meshes (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the visual helper after `pinColorOnlyVisualBounds`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes). Fail-soft packaged paths (no `lod*` groups) still run the visual pin, including the fastener. Prefer `delete`. Does **not** assign `null` (`null !== undefined`, so a null own property still takes the `Frustum.intersectsObject` object branch). Does **not** invent a `Sphere`. Does **not** call `computeBoundingSphere` on the mesh. Does **not** touch geometry `boundingBox` / `boundingSphere` (v0.92). Does **not** touch `updateRanges` (v0.91). Does **not** touch `updateRange` (v0.90). Does **not** change `usage` (v0.43 `StaticDrawUsage` stays). Does **not** replace attributes or the geometry. Does **not** delete `skinIndex` / `skinWeight` (v0.89). Does **not** touch `drawRange` (v0.88). Does **not** touch `groups` (v0.87). Does **not** touch `morphAttributes` / `morphTargetsRelative` (v0.86). Does **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary` (v0.85). Does **not** touch Object3D `animations` (v0.84). Does **not** touch Material `glslVersion` / `flatShading` / `defines` / `customProgramCacheKey` / `onBeforeCompile` / `onBeforeRender`. Does **not** touch Mesh `onBeforeRender` / `onAfterRender` / `onBeforeShadow` / `onAfterShadow`. Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `frustumCulled` (v0.50 already pins `true`). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Does **not** merge meshes across `body` / `lidPivot` / `latchPivot` / `tool`, fold the fastener into an LOD batch (`fastener` / `fastenerMesh` stay outside `LOD_MERGE_SKIP_NAMES`), rewrite material type, invent a GLB, change matrix freeze, change raycast no-ops, or change L4/L5 interaction. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v0.92 merge → weld → unused-attr strip → Uint16 compact → shared MeshBasic → Float16 → upload-release → matrix-freeze → raycast-disable → material-flag → Mesh-pin → animations-clear → morph-target-clear → morph-attribute-clear → groups-clear → drawRange-pin → skin-attribute-strip → updateRange-pin → updateRanges-clear → bounds-null pipeline stays intact; v0.93 adds the object-level `boundingSphere` clear after the geometry bounds pin.
- **Checked installed three@0.170.0:** `Frustum.intersectsObject` takes the object branch when `object.boundingSphere !== undefined`. The else branch copies `geometry.boundingSphere` and calls `geometry.computeBoundingSphere()` only when that sphere is `null`. A Mesh does not assign `object.boundingSphere` (`Mesh.prototype` and `Object3D.prototype` do not own the property; a fresh `Mesh` reads `undefined` and has no `computeBoundingSphere`). `null !== undefined`, so assigning `null` still takes the object branch and calls `object.computeBoundingSphere()`. A leftover non-undefined sphere is transformed by `matrixWorld` and tested as-is while `Mesh.frustumCulled` stays true (v0.50). This pulse `delete`s the own leftover so the property is absent. It does **not** assign `null`. It does **not** invent a `Sphere`. It does **not** call `computeBoundingSphere` on the mesh. It does **not** touch geometry `boundingBox` / `boundingSphere`. It does **not** touch `updateRanges` or `updateRange`. It does **not** change `usage`. It does **not** change `frustumCulled`. Do **not** replace attributes or the geometry. Do **not** delete `skinIndex` / `skinWeight`. Do **not** touch `drawRange`. Do **not** touch `groups`. Do **not** touch `morphAttributes` / `morphTargetsRelative`. Do **not** touch Mesh morph-target properties. Do **not** touch Object3D `animations`. Do **not** touch Material program-cache fields. Do **not** touch Mesh render or shadow callbacks. Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** enable shadows. Do **not** pin `mesh.visible`. Do not change `matrixAutoUpdate`. Mapped / lit stay authored because `isColorOnlyUnlitBasic` is false when `map` / `lightMap` / `aoMap` / `specularMap` / `alphaMap` / `envMap` is set.
- **Why:** Accidental DCC / exporter leftover non-undefined `mesh.boundingSphere` makes r170 frustum culling test that stale object sphere and skip the geometry compute-on-demand path v0.92 restored. Deleting the own property (`mesh.boundingSphere === undefined`) puts the Mesh back on the else branch. Assigning `null` would not: `null` is not `undefined`. On the packed color-only path, v0.92 assigns geometry bounds to `null` and the v0.43 `onUpload` callback recomputes those geometry volumes while CPU arrays still exist. This pulse does not change that callback and does not call `computeBoundingSphere` itself. Pre-upload draws / tris / verts / attrBytes on clean procedural meshes are unchanged vs v0.92. This pulse does not invent headset milliseconds.
- **Scope vs v0.92 geometry bounds pin:** v0.92 hits the **13** packed color-only visual **geometries** with `boundingBox === null` and `boundingSphere === null` before upload. v0.93 hits those same **13** visual **meshes** (body LOD leaves + lid/latch/tool + fastener) with `mesh.boundingSphere === undefined`. Measured unique geometries stay **13** (one geometry per visual; materials stay the shared 3, geometries are not shared). Fresh r170 `Mesh` already leaves object `boundingSphere` absent. Mapped / lit object spheres stay authored. Interleaved meshes stay authored. A mesh whose material is shared with a blocked collider or interleaved mesh, or whose geometry is shared with a collider, an interleaved mesh, or a mapped / lit mesh, skips the visual pin. Color-only leftover object spheres are deleted rather than kept: that leftover is the pulse.
- **Gate (same `isColorOnlyUnlitBasic` as `pinColorOnlyVisualBounds`):** only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip interleaved geos (same interleaved half of the bounds pin). Skip materials shared with a blocked collider / interleaved mesh. Skip geometries shared with a collider, an interleaved mesh, or a mapped / lit mesh. Do not change `isColorOnlyUnlitBasic` (map / lightMap / aoMap / specularMap / alphaMap / envMap still must be falsy).
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope (arrays still present at `attachToolboxLod`). After simulated / real GPU upload, live CPU attrBytes on those visuals → **0**; `BufferAttribute.count` (draws / tris / verts) stays. Deleting an object-level sphere does not change attribute byte length. `mesh-boundingSphere-absent` counts visual meshes whose `boundingSphere === undefined` before upload. `bounds-null` still counts visual meshes whose geometry has `boundingBox === null` && `boundingSphere === null` before upload. Unique geometries were measured, not assumed: **13** meshes, **13** distinct `BufferGeometry` instances. `countGroupStats` counts meshes, tris, verts, and attribute bytes, not spheres, so the pin does not change the draws/tris/verts/attrBytes envelope. After simulated upload the v0.43 callback has recomputed a finite geometry sphere and box. Object-level `boundingSphere` stays absent.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset):

  | Level | Draws | Tris | Unique verts | attrBytes (pre-upload) | Unique MeshBasic | mesh-boundingSphere-absent | bounds-null | updateRanges-empty | updateRange-default | skinAttributes-absent | drawRange-default | groups-empty |
  | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | | | | | | |

  Unique procedural MeshBasic instances stay **3**. Visual MeshBasic mesh-boundingSphere-absent **13** (`mesh.boundingSphere === undefined`). Visual MeshBasic bounds-null stay **13** (13 unique geometries; `boundingBox === null` && `boundingSphere === null` before upload). Visual MeshBasic updateRanges-empty stay **13**. Visual MeshBasic updateRange-default stay **13**. Visual MeshBasic skinAttributes-absent stay **13**. Visual MeshBasic drawRange-default stay **13**. Visual MeshBasic groups-empty stay **13**. Visual MeshBasic morphAttributes-empty stay **13**. Visual MeshBasic morphTargets-absent stay **13**. Visual MeshBasic animations-empty stay **13**. Unique color-only MeshBasic glslVersion-absent stay **3**. flatShading-off stay **3**. defines-absent stay **3**. customProgramCacheKey-default stay **3**. material-render-callbacks-absent stay **3**. Visual MeshBasic shadow-callbacks-absent stay **13**. render-callbacks-absent stay **13**. customDepth/Distance-absent stay **13**. rotation-order-XYZ stay **13**. scale-default stay **13**. up-default stay **13**. matrixWorldAutoUpdate-on stay **13**. layers-default stay **13**. renderOrder-0 stay **13**. frustumCulled-on stay **13**. shadow-off stay **13**. raycast-off stay **13**. Frozen MeshBasic stay **3** (body LOD0/1/2). Live MeshBasic stay **10** (lid/latch/tool LOD meshes + fastener). `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes / matrix freeze / raycast-off / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent / flatShading-off / glslVersion-absent / animations-empty / morphTargets-absent / morphAttributes-empty / groups-empty / drawRange-default / skinAttributes-absent / updateRange-default / updateRanges-empty / bounds-null unchanged vs v0.92 on clean procedural meshes. mesh-boundingSphere-absent **13** is the new count. After simulated upload, CPU attrBytes on those 13 visual geos → 0, and geometry bounds have been recomputed by the upload callback. Object-level `boundingSphere` stays absent.

- **Unit/mock evidence** (not headset): installed three@0.170.0 `Frustum.intersectsObject` uses `object.boundingSphere` only when it is not `undefined`; a fresh `Mesh` leaves the property absent and has no `computeBoundingSphere`; a `null` own property takes the object branch; a non-undefined object sphere skips `geometry.computeBoundingSphere`. A wrong color-only MeshBasic with a leftover object `Sphere` or a leftover `null` is deleted to absent by `pinColorOnlyUnlitBasicMeshBoundingSphere` without replacing the mesh, the geometry, or the attributes, without calling `computeBoundingSphere` on the mesh or the geometry, without mutating the detached `Sphere`, without touching geometry `boundingBox` / `boundingSphere`, without touching `updateRanges` / `updateRange` / `usage`, without deleting `skinIndex` / `skinWeight`, and without touching `drawRange` / `groups` / `morphAttributes` / `morphTargetsRelative` / Mesh `morphTargetInfluences` / `morphTargetDictionary` / Object3D `animations`; mapped MeshBasic and MeshStandard keep authored object spheres; interleaved meshes keep authored object spheres; collider meshes keep authored object spheres; a visual that shares a material with a collider, or a geometry with a mapped mesh, stays uncleared; `mesh.visible` is not pinned; `frustumCulled` stays true; L4 `tryUse` closed→unlatched→open and L5 `tryDriveFastener` still mutate pivot / fastener transforms. Packaged ingest of a mock `lod*` fixture deletes a DCC leftover object sphere on a color-only MeshBasic visual and on the fastener; fail-soft (no lod groups) still deletes those meshes, including a `null` leftover. Mapped MeshBasic and collider meshes stay authored. Geometry `boundingBox` / `boundingSphere` on the cleared visual stay the `null` the v0.92 helper already assigned; `updateRanges` stay the array the v0.91 helper already emptied; `updateRange` stays the object the v0.90 helper already pinned; `drawRange` stays the object the v0.88 helper already pinned; `groups` stay the array the v0.87 helper already cleared.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v0.92.0 set. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub stay as they were. The v0.93 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz / 72 fallback are **requested**, not measured. Deleting leftover object-level `boundingSphere` on the 13 packed color-only unlit MeshBasic visual meshes is the intended delta (restores the geometry compute-on-demand path beside the v0.92 `null` geometry bounds). Headset ms still **TODO**. Do not require 207/240 Hz.
