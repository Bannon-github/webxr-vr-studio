# crate-toolbox v1.11.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v1.10.0 pinned leftover BufferGeometry `indirect` to the r170 constructor default `null` (`geometry.indirect === null`) on the **13** packed color-only unlit MeshBasic visual geometries (indirect-null **13**; unusedAttributes-absent **13**; onUpload-release **13**; colorAttribute-absent **13**; matrixWorldNeedsUpdate-false **13**; material-version-zero **3**; mesh-userData-empty **13**; mesh-name-empty **10**), **`pinColorOnlyUnlitBasicMaterialExtensions` / `pinColorOnlyVisualMaterialExtensions` delete leftover Material `extensions` so the r170 MeshBasic absence remains (`material.extensions === undefined` and `Object.hasOwn(material, 'extensions') === false`)** on the **3** shared color-only MeshBasic materials (wood / brass / steel) those visuals use. Procedural create runs the visual helper after `pinColorOnlyVisualIndirect`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes). Fail-soft packaged paths (no `lod*` groups) still run the visual pin, including the fastener. Prefer `delete material.extensions` when the own property is present. An already-absent `extensions` is left alone. Pin once per shared material instance. Does **not** assign `null`. Does **not** assign a sentinel `{ clipCullDistance: false, multiDraw: false }`. Does **not** convert MeshBasic to ShaderMaterial. Does **not** invent extension maps. Does **not** enable multi-draw or clip-cull-distance. Does **not** touch `indirect` (v1.10.0 indirect-null stays). Does **not** re-run the unused-channel strip (v1.9.0 unusedAttributes-absent stays). Does **not** touch `onUpload` / `onUploadCallback` (v1.8.0 onUpload-release stays). Does **not** touch `color` (v1.7.0 colorAttribute-absent stays). Does **not** touch Material `version` / `name` / `userData`. Does **not** touch Mesh / Object3D `userData` / `name` (reserved names `lidMesh` / `latchMesh` / `fastenerMesh` and any `collider_*` stay). Does **not** touch Mesh `matrixWorldNeedsUpdate` (v1.6.0 stays). Does **not** change `matrixAutoUpdate` or `matrixWorldAutoUpdate`. Does **not** touch BufferGeometry `userData` / `name`. Does **not** touch BufferAttribute fields. Does **not** touch prior Material program-cache / flag pins. Does **not** touch bounds, morphs, animations, shadows, `frustumCulled`, or `mesh.visible`. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v1.10.0 pipeline stays intact; v1.11.0 adds the extensions pin after the indirect pin.
- **Checked installed three@0.170.0:** fresh `Material` / `MeshBasicMaterial` constructors do not assign `extensions` (`extensions === undefined` and `Object.hasOwn` is false). `ShaderMaterial` assigns `this.extensions = { clipCullDistance: false, multiDraw: false }` and `ShaderMaterial.copy` copies `this.extensions = Object.assign({}, source.extensions)`. `WebGLPrograms.getParameters` sets `HAS_EXTENSIONS = !! material.extensions`, then `extensionClipCullDistance: HAS_EXTENSIONS && material.extensions.clipCullDistance === true && extensions.has('WEBGL_clip_cull_distance')` and `extensionMultiDraw: (HAS_EXTENSIONS && material.extensions.multiDraw === true || IS_BATCHEDMESH) && extensions.has('WEBGL_multi_draw')`.
- **Why:** A leftover own `extensions` object on a color-only MeshBasic (DCC / ShaderMaterial.copy bleed / `Object.assign`) keeps `HAS_EXTENSIONS` true even when both flags are false, so those parameter forks still read the object. A sentinel `{ clipCullDistance: false, multiDraw: false }` keeps `HAS_EXTENSIONS` true. Deleting the own property restores the r170 absence and is load-time packaging for Quest 3 TBDR. On clean procedural materials `extensions` is already absent, so draws, tris, and attrBytes stay unchanged vs v1.10.0. This pulse does not invent headset milliseconds. 90 Hz ship (~11.1 ms) / 72 Hz fallback are **requested**, not measured.
- **Scope vs v1.10.0 indirect pin:** v1.10.0 hits the **13** packed color-only visual **geometries** and pins leftover `indirect` to `null` (indirect-null **13**). v1.11.0 hits the **3** shared color-only MeshBasic **materials** (wood / brass / steel) and deletes a leftover own `extensions` property. Measured unique materials stay **3**. indirect-null stays **13**. unusedAttributes-absent stays **13**. onUpload-release stays **13**. colorAttribute-absent stays **13**. matrixWorldNeedsUpdate-false stays **13**. material-version-zero stays **3**. material-name-empty stays **3**. material-userData-empty stays **3**. geometry-userData-empty stays **13**. geometry-name-empty stays **13**. mesh-name-empty stays **10**. mesh-userData-empty stays **13**. Mapped / lit / interleaved keep authored `extensions`. Collider meshes stay untouched. ShaderMaterial keeps authored `extensions`. A mesh whose material is shared with a blocked collider or interleaved mesh, or whose geometry is shared with a collider, an interleaved mesh, or a mapped / lit mesh, skips the visual pin.
- **Gate (same `isColorOnlyUnlitBasic` as `pinColorOnlyVisualIndirect`):** only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip interleaved geos. Skip materials shared with a blocked collider / interleaved mesh. Skip geometries shared with a collider, an interleaved mesh, or a mapped / lit mesh. Keep `lidMesh` / `latchMesh` / `fastenerMesh` names, their empty `mesh.userData`, and `indirect === null`; still delete leftover `extensions` on the shared materials those reserved-name visuals use.
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope. `extensions` is a Material field, not an entry in `geometry.attributes`, so deleting it does not change attribute byte length. `extensions-absent` counts the 3 unique color-only MeshBasic materials whose `extensions` is absent after the pin (wood / brass / steel; brass is shared with the fastener). `countGroupStats` does not read `extensions` on clean procedural meshes, so the pin does not change the draws/tris/verts/attrBytes envelope. L4 `tryUse` / `applyActivityVisual` and L5 `tryDriveFastener` / `applyFastenerVisual` write rotation and position; they do not assign `extensions` and they do not rename reserved meshes. The measured count after the existing L4/L5 path stays **3**.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset):

  | Level | Draws | Tris | Unique verts | attrBytes (pre-upload) | Unique MeshBasic | indirect-null | extensions-absent |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | **6** | wood + brass + steel |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | **4** | same wood + same brass |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | **2** | same wood |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | **1** | same brass |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | |

  Unique procedural MeshBasic instances stay **3**. Packed color-only visual indirect-null stay **13**. Packed color-only visual unusedAttributes-absent stay **13**. Packed color-only visual onUpload-release stay **13**. Packed color-only visual colorAttribute-absent stay **13**. Packed color-only visual matrixWorldNeedsUpdate-false stay **13**. Unique color-only MeshBasic material-version-zero stay **3**. Unique color-only MeshBasic material-name-empty stay **3**. Unique color-only MeshBasic material-userData-empty stay **3**. Packed color-only visual geometry-userData-empty stay **13**. Packed color-only visual geometry-name-empty stay **13**. Packed color-only visual mesh-name-empty stay **10**. Packed color-only visual mesh-userData-empty stay **13**. Unique color-only MeshBasic extensions-absent **3**. `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes unchanged vs v1.10.0 on clean procedural meshes. extensions-absent **3** is the new count.

- **Unit/mock evidence** (not headset): installed three@0.170.0 `Material` / `MeshBasicMaterial` constructors leave `extensions` absent. `ShaderMaterial` assigns `{ clipCullDistance: false, multiDraw: false }` and copies it with `Object.assign`. `WebGLPrograms.getParameters` sets `HAS_EXTENSIONS = !! material.extensions`. A color-only MeshBasic with a leftover own `extensions` object (`{ multiDraw: true }`, `{ clipCullDistance: true }`, or `{}`) is cleared by `pinColorOnlyUnlitBasicMaterialExtensions` via one `delete` without replacing the mesh, the material, the geometry, `position`, the index, or `indirect`. An already-absent `extensions` does not call `delete`. The indirect pin does not delete `extensions`. Mapped MeshBasic, MeshStandard, ShaderMaterial, interleaved meshes, collider meshes, and visuals that share a blocked material or geometry keep authored `extensions`. Packaged ingest of a mock `lod*` fixture deletes color-only survivors, reserved `lidMesh` / `latchMesh`, and the fastener `extensions`, and leaves mapped / lit / ShaderMaterial / interleaved / collider / shared-geometry `extensions` authored. Fail-soft (no lod groups) deletes the body and fastener `extensions`, does not invent lod groups, and keeps `lidMesh` / `latchMesh` / `fastenerMesh`. L4 `tryUse` and L5 `tryDriveFastener` still mutate pivot / fastener transforms; extensions-absent stays **3**, indirect-null stays **13**, unusedAttributes-absent stays **13**, material-version-zero stays **3**, mesh-userData-empty stays **13**, and mesh-name-empty stays **10**.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v1.10.0 set. The v1.11.0 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz ship (~11.1 ms) / 72 Hz fallback are **requested**, not measured. Deleting leftover Material `extensions` on the 3 shared color-only unlit MeshBasic materials is the intended delta. Headset ms still **TODO**. Do not require 207/240 Hz.
