# crate-toolbox v0.1.0

Procedural stand-in in `examples/interactive-prop` (no GLB).

- Solid body box (tool not visible when open)
- Grab hull covered the lid; latch/lid clicks were easy to miss
- Activity contract already: `closed` → `unlatched` → `open`
