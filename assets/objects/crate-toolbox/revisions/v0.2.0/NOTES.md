# crate-toolbox v0.2.0

Current. Procedural stand-in — no `current.glb` yet.

- Hollow walls + floor; yellow-handle screwdriver visible at `open`
- `collider_grab` is body-sized; use-layer preferred within 8 cm of the nearest hit
- Sidecar: `../../behavior.json` (objectId added; reset plate is example-only, not part of this object)
