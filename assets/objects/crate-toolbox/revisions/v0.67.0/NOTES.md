# crate-toolbox v0.67.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v0.66 pinned r170 MeshBasic `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'` on packed color-only unlit MeshBasic **materials** (and after v0.60 pinned Object3D `renderOrder = 0` on those same visual meshes), **`pinColorOnlyUnlitBasicLayers` / `pinColorOnlyVisualLayers` set r170 Object3D `layers` default (layer 0 only / `mask = 1`) on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Keeps the existing `mesh.layers` Layers instance — does not replace it with a new `Layers()`. `layers.set(0)` is equivalent to `layers.mask = 1` on a fresh r170 `Object3D` / `Mesh`. Procedural create runs the helper after `pinColorOnlyVisualRenderOrder`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes). Does **not** pin `mesh.visible` (LOD visibility uses it). Does not change `matrixWorldAutoUpdate` / `matrixAutoUpdate`. Does **not** enable extra camera/layers tricks, custom layer masks beyond the r170 default, or invent layer-based culling. Does **not** merge meshes across `body` / `lidPivot` / `latchPivot` / `tool`, fold the fastener into an LOD batch (`fastener` / `fastenerMesh` stay outside `LOD_MERGE_SKIP_NAMES`), rewrite material type, invent a GLB, change matrix freeze, change raycast no-ops, change fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/shadow/frustumCulled/renderOrder pins, or change L4/L5 interaction. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v0.66 merge → weld → unused-attr strip → Uint16 compact → shared MeshBasic → Float16 → upload-release → matrix-freeze → raycast-disable → fog/toneMapped-pin → opaque-FrontSide-pin → shadow-flag-pin → frustumCulled-pin → blending/alpha-pin → GPU-state-pin → stencil-pin → clipping-pin → alphaHash-pin → NormalBlending-companions-pin → vertexColors-pin → precision-pin → shadowSide-pin → renderOrder-pin → visible-pin → envMap-companions-pin → map-intensity-companions-pin → wireframeLinewidth-pin → wireframe-line-style-pin → envMapRotation-pin pipeline stays intact.
- **Verified r170 API (do not assume):** a fresh `Mesh` / `Object3D` already has `layers.mask === 1` (only layer 0 enabled — `layers.isEnabled(0) === true`; `REVISION` 170). This pulse still pins those r170 layers defaults on color-only unlit stand-in **meshes** so packaged GLBs that leave leftover non-default layer masks get corrected even when they still pass `isColorOnlyUnlitBasic`. A deliberately wrong color-only Mesh is corrected when it still passes `isColorOnlyUnlitBasic`. The existing Layers instance is kept — do **not** replace it with a new `Layers()`. Do **not** pin `mesh.visible` — LOD visibility uses it. Do not change `matrixWorldAutoUpdate` / `matrixAutoUpdate`. Do not invent extra camera/layers tricks or a custom layer mask.
- **Why:** Accidental DCC / packaged GLB leftover non-default Object3D layer masks can hide draws from the default camera (layer 0) or force unexpected multi-layer membership. Pinning the r170 Object3D default (layer 0 only) keeps the Quest-safe opaque unlit contract. Mesh-state fence, not a geometry pack change. Does **not** invent custom shaders, transparent materials, pin `mesh.visible`, change `matrixWorldAutoUpdate` / `matrixAutoUpdate`, or invent a layer-based culling strategy.
- **Scope vs v0.66 envMapRotation pin:** v0.66 hits the **3** unique shared color-only **materials** (wood / brass / steel) with r170 `envMapRotation` `(0, 0, 0)` / `order = 'XYZ'`. v0.67 hits the **13** packed color-only visual **meshes** (body LOD leaves + lid/latch/tool + fastener) with r170 Object3D layers default (same visual-mesh family as v0.60 renderOrder / v0.50 frustumCulled / v0.49 shadow flags). Mapped / lit stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Morph / interleaved skip the same pack-pipeline gate. Materials shared with a blocked collider / morph mesh skip the visual pin.
- **Gate (same as pack pipeline):** only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip morph / interleaved geos (conservative). Skip materials shared with a blocked collider / morph mesh. Do not change `isColorOnlyUnlitBasic` (map / lightMap / aoMap / specularMap / alphaMap / envMap still must be falsy).
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope (arrays still present at `attachToolboxLod`). After simulated / real GPU upload, live CPU attrBytes on those visuals → **0**; `BufferAttribute.count` (draws / tris / verts) stays. This pulse does not change that envelope.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset) plus unique MeshBasic flags and visual Mesh shadow / frustum / renderOrder / layers flags:

  | Level | Draws | Tris (index/3) | Unique verts (v0.39 weld) | attrBytes (pre-upload v0.44 Float16) | Unique MeshBasic | fog / toneMapped / opaque FrontSide / blending+alpha / GPU-state / stencil / clipping / alphaHash+forceSinglePass / NormalBlending companions / vertexColors / precision / shadowSide / visible / envMap companions / map-intensity companions / wireframeLinewidth / linecap / linejoin / envMapRotation | shadow-off meshes | frustumCulled-on meshes | renderOrder-0 meshes | layers-default meshes |
  | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | **false / false / yes / yes / yes / off / null / off / SrcAlpha / false / null / null / true / Multiply / 1 / 0.98 / 1 / 1 / 1 / round / round / (0,0,0)** | **yes** | **yes** | **yes** | **yes** |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | **false / false / yes / yes / yes / off / null / off / SrcAlpha / false / null / null / true / Multiply / 1 / 0.98 / 1 / 1 / 1 / round / round / (0,0,0)** | **yes** | **yes** | **yes** | **yes** |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | **false / false / yes / yes / yes / off / null / off / SrcAlpha / false / null / null / true / Multiply / 1 / 0.98 / 1 / 1 / 1 / round / round / (0,0,0)** | **yes** | **yes** | **yes** | **yes** |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | **false / false / yes / yes / yes / off / null / off / SrcAlpha / false / null / null / true / Multiply / 1 / 0.98 / 1 / 1 / 1 / round / round / (0,0,0)** | **yes** | **yes** | **yes** | **yes** |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | | | | |

  Unique procedural MeshBasic instances stay **3**, all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilFail === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null` && `visible === true` && `combine === MultiplyOperation` && `reflectivity === 1` && `refractionRatio === 0.98` && `lightMapIntensity === 1` && `aoMapIntensity === 1` && `wireframeLinewidth === 1` && `wireframeLinecap === 'round'` && `wireframeLinejoin === 'round'` && `envMap === null` && `envMapRotation.x === 0` && `envMapRotation.y === 0` && `envMapRotation.z === 0`. Visual MeshBasic layers-default **13** (`layers.mask === 1` && `layers.isEnabled(0)`). Visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`). Visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`). Visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`). Visual MeshBasic raycast-off stay **13**. Frozen MeshBasic stay **3** (body LOD0/1/2). Live MeshBasic stay **10** (lid/latch/tool LOD meshes + fastener). `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes / matrix freeze / raycast-off / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 unchanged vs v0.66. After simulated upload, CPU attrBytes on those 13 visual geos → 0.
- **Unit/mock evidence** (not headset): r170 default `new Mesh().layers` / `new Object3D().layers` is layer-0-only (`mask === 1` / `isEnabled(0)`); a wrong color-only Mesh with a non-default layer mask is corrected by `pinColorOnlyUnlitBasicLayers` (existing Layers instance kept); `mesh.visible` is not pinned (LOD still uses group/mesh visibility); `matrixWorldAutoUpdate` / `matrixAutoUpdate` are untouched by this pulse; prior material pins including `envMapRotation` stay intact; procedural wood/brass/steel stay Quest-safe; mapped / lit Mesh stay authored; collider meshes stay authored / default; L4 `tryUse` closed→unlatched→open and L5 `tryDriveFastener` still mutate pivot / fastener transforms. Packaged ingest of a mock `lod*` fixture pins body + lid/latch/tool + fastener meshes including a DCC leftover non-default layer mask color-only Mesh; fail-soft (no lod groups) still pins those visuals. Mapped Mesh stays authored.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v0.66.0 set. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub stay as they were. The v0.67 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz / 72 fallback are **requested**, not measured. Pinning Object3D layers to r170 default (layer 0 only) is the intended delta; headset ms still **TODO**.
