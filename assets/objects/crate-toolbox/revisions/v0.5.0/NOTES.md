# crate-toolbox v0.5.0

Shipping/perf-gate UPGRADE on the same `objectId`. No GLB. No new layer.

- L0–L5 activity is the v0.4.0 set (cancel, tool grab, drive fastener, return).
- `perf.notes` points at `docs/shipping/quest-3-on-device-qa.md`.
- Runtime: `examples/interactive-prop` **P** overlay (rates / FFR / rAF Δ). Overlay off does not sample.
- **Headset frame time and FFR remain unmeasured.** Do not treat authoring tris/draws as Quest 3 ms.
