# crate-toolbox v0.90.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v0.89 deleted leftover `skinIndex` / `skinWeight` so those attributes are absent on the **13** packed color-only unlit MeshBasic visual geometries (and after the v0.88 BufferGeometry `drawRange` pin, the v0.87 BufferGeometry `groups` clear, the v0.86 BufferGeometry `morphAttributes` clear, the v0.85 Mesh `morphTargetInfluences` / `morphTargetDictionary` absence, and the v0.84 Object3D `animations` empty list), **`pinColorOnlyUnlitBasicUpdateRange` / `pinColorOnlyVisualUpdateRange` pin leftover BufferAttribute `updateRange` so `offset === 0` && `count === -1` on every non-interleaved `BufferAttribute` in `geometry.attributes` plus `geometry.index` when it is a BufferAttribute** on those same packed color-only unlit MeshBasic visual geometries (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the visual helper after `pinColorOnlyVisualSkinAttributes`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes). Fail-soft packaged paths (no `lod*` groups) still run the visual pin, including the fastener. Mutate the existing `updateRange` object (`offset = 0`, `count = -1`). If `updateRange` is missing or not an object, assign `{ offset: 0, count: -1 }`. Does **not** call `addUpdateRange()`. Does **not** rewrite `updateRanges`. Does **not** change `usage` (v0.43 `StaticDrawUsage` stays). Does **not** replace attributes or the geometry. Does **not** delete `skinIndex` / `skinWeight` (v0.89). Does **not** touch `drawRange` (v0.88). Does **not** touch `groups` (v0.87). Does **not** touch `morphAttributes` / `morphTargetsRelative` (v0.86). Does **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary` (v0.85). Does **not** touch Object3D `animations` (v0.84). Does **not** touch Material `glslVersion` (v0.83). Does **not** touch Material `flatShading` (v0.82). Does **not** touch Material `defines` (v0.81). Does **not** touch Material `customProgramCacheKey` (v0.80). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Mesh `onBeforeShadow` / `onAfterShadow` (v0.79). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Does **not** merge meshes across `body` / `lidPivot` / `latchPivot` / `tool`, fold the fastener into an LOD batch (`fastener` / `fastenerMesh` stay outside `LOD_MERGE_SKIP_NAMES`), rewrite material type, invent a GLB, change matrix freeze, change raycast no-ops, or change L4/L5 interaction. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v0.89 merge → weld → unused-attr strip → Uint16 compact → shared MeshBasic → Float16 → upload-release → matrix-freeze → raycast-disable → material-flag → Mesh-pin → animations-clear → morph-target-clear → morph-attribute-clear → groups-clear → drawRange-pin → skin-attribute-strip pipeline stays intact; v0.90 adds the updateRange pin after the skin strip.
- **Checked installed three@0.170.0 (do not assume the pre-r163 field):** the `BufferAttribute` constructor assigns `this.updateRanges = []` and does **not** assign `this.updateRange`. `addUpdateRange(start, count)` pushes `{ start, count }` onto `updateRanges`. `clearUpdateRanges()` sets `updateRanges.length = 0`. `WebGLAttributes.update` calls `updateBuffer`, which `gl.bufferSubData`s the full typed array when `updateRanges.length === 0` and a partial span (`range.start`, `range.count`) for each entry otherwise, then clears the array. The legacy object `{ offset: 0, count: -1 }` (`count: -1` means the full buffer) is the pre-r163 contract. This pulse still pins that leftover object on color-only stand-ins so a truncated `count` cannot remain. It does **not** call `addUpdateRange()` (that helper invents a partial range). It does **not** call `clearUpdateRanges()`. It does **not** rewrite `updateRanges`. It does **not** change `usage`. The r170 upload span therefore stays whatever `updateRanges` already was; clean procedural attributes start with an empty array, which is already a full upload. Do **not** replace attributes or the geometry. Do **not** delete `skinIndex` / `skinWeight`. Do **not** touch `drawRange`. Do **not** touch `groups`. Do **not** touch `morphAttributes` / `morphTargetsRelative`. Do **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary`. Do **not** touch Object3D `animations`. Do **not** touch Material `glslVersion` / `flatShading` / `defines` / `customProgramCacheKey` / `onBeforeCompile` / `onBeforeRender`. Do **not** touch Mesh `onBeforeRender` / `onAfterRender` / `onBeforeShadow` / `onAfterShadow`. Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** enable `castShadow` / `receiveShadow`. Do **not** pin `mesh.visible`. Do not change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Mapped / lit stay authored because `isColorOnlyUnlitBasic` is false when `map` / `lightMap` / `aoMap` / `specularMap` / `alphaMap` / `envMap` is set.
- **Why:** Accidental DCC / exporter leftover `updateRange` with a truncated `count` can sit on color-only unlit midtone stand-ins. Pinning `{ offset: 0, count: -1 }` restores the legacy full-buffer sentinel beside the v0.88 `drawRange` fence and the v0.43 `StaticDrawUsage` + onUpload release. On installed three@0.170.0 the live GPU upload path reads `updateRanges`, not `updateRange`, so this pulse does not claim a change to that upload span and does not invent headset milliseconds. Attribute-state pin, not a draw/tri/vert/attrBytes change on clean procedural meshes. Does **not** call `addUpdateRange`, rewrite `updateRanges`, change `usage`, replace attributes or geometry, delete `skinIndex` / `skinWeight`, touch `drawRange`, touch `groups`, touch `morphAttributes`, touch Mesh morph-target properties, touch Object3D `animations`, touch Material program-cache fields, touch Mesh render or shadow callbacks, touch customDepth/Distance, enable shadows, pin `mesh.visible`, or change `matrixAutoUpdate`.
- **Scope vs v0.89 skin-attribute strip:** v0.89 hits the **13** packed color-only visual **geometries** with `skinIndex` / `skinWeight` absence. v0.90 hits those same **13** visual **geometries** (body LOD leaves + lid/latch/tool + fastener) with BufferAttribute `updateRange` at `{ offset: 0, count: -1 }`. Measured unique geometries are **13** (one geometry per visual; materials stay the shared 3, geometries are not shared). Fresh r170 `BufferAttribute` does not author `updateRange`, so the pin assigns the object on the clean path. That assignment does not change the draws/tris/verts/attrBytes envelope. Mapped / lit `updateRange` stays authored. Interleaved geometries stay authored (`InterleavedBufferAttribute` included). A mesh whose material is shared with a blocked collider or interleaved mesh, or whose geometry is shared with a collider, an interleaved mesh, or a mapped / lit mesh, skips the visual pin. Color-only leftover `updateRange` values are pinned rather than kept: that leftover is the pulse.
- **Gate (same `isColorOnlyUnlitBasic` as the pack pipeline):** only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip interleaved geos (same interleaved half of `colorOnlyGeometryBlocksPack`). Skip materials shared with a blocked collider / interleaved mesh. Skip geometries shared with a collider, an interleaved mesh, or a mapped / lit mesh. Do not change `isColorOnlyUnlitBasic` (map / lightMap / aoMap / specularMap / alphaMap / envMap still must be falsy).
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope (arrays still present at `attachToolboxLod`). After simulated / real GPU upload, live CPU attrBytes on those visuals → **0**; `BufferAttribute.count` (draws / tris / verts) stays. Assigning `updateRange` does not change attribute byte length. `updateRange-default` counts visual meshes whose every non-interleaved BufferAttribute and BufferAttribute index has `updateRange.offset === 0` && `updateRange.count === -1`. Unique geometries were measured, not assumed: **13** meshes, **13** distinct `BufferGeometry` instances. `countGroupStats` counts meshes, not `updateRange`, so the pin does not change the draws/tris/verts/attrBytes envelope.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset):

  | Level | Draws | Tris | Unique verts | attrBytes (pre-upload) | Unique MeshBasic | updateRange-default | skinAttributes-absent | drawRange-default | groups-empty |
  | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | **yes** | **yes** | **yes** | **yes** |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | **yes** | **yes** | **yes** | **yes** |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | **yes** | **yes** | **yes** | **yes** |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | **yes** | **yes** | **yes** | **yes** |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | | | |

  Unique procedural MeshBasic instances stay **3**. Visual MeshBasic updateRange-default **13** (13 unique geometries; every BufferAttribute + index on those geos has `updateRange.offset === 0` && `updateRange.count === -1`). Visual MeshBasic skinAttributes-absent stay **13**. Visual MeshBasic drawRange-default stay **13**. Visual MeshBasic groups-empty stay **13**. Visual MeshBasic morphAttributes-empty stay **13**. Visual MeshBasic morphTargets-absent stay **13**. Visual MeshBasic animations-empty stay **13**. Unique color-only MeshBasic glslVersion-absent stay **3**. flatShading-off stay **3**. defines-absent stay **3**. customProgramCacheKey-default stay **3**. material-render-callbacks-absent stay **3**. Visual MeshBasic shadow-callbacks-absent stay **13**. render-callbacks-absent stay **13**. customDepth/Distance-absent stay **13**. rotation-order-XYZ stay **13**. scale-default stay **13**. up-default stay **13**. matrixWorldAutoUpdate-on stay **13**. layers-default stay **13**. renderOrder-0 stay **13**. frustumCulled-on stay **13**. shadow-off stay **13**. raycast-off stay **13**. Frozen MeshBasic stay **3** (body LOD0/1/2). Live MeshBasic stay **10** (lid/latch/tool LOD meshes + fastener). `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes / matrix freeze / raycast-off / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent / shadow-callbacks-absent / customProgramCacheKey-default / defines-absent / flatShading-off / glslVersion-absent / animations-empty / morphTargets-absent / morphAttributes-empty / groups-empty / drawRange-default / skinAttributes-absent unchanged vs v0.89 on clean procedural meshes. updateRange-default **13** is the new count. After simulated upload, CPU attrBytes on those 13 visual geos → 0.

- **Unit/mock evidence** (not headset): installed three@0.170.0 `BufferAttribute` leaves `updateRange` absent and `updateRanges` empty; a wrong color-only MeshBasic with leftover `updateRange` `{ offset, count }` on position and index is pinned in place (`offset === 0`, `count === -1`) by `pinColorOnlyUnlitBasicUpdateRange` without replacing the mesh, the geometry, or the attributes, without calling `addUpdateRange`, without rewriting `updateRanges`, without changing `usage`, without deleting `skinIndex` / `skinWeight`, and without touching `drawRange` / `groups` / `morphAttributes` / `morphTargetsRelative` / Mesh `morphTargetInfluences` / `morphTargetDictionary` / Object3D `animations`; `null`, missing, and non-object `updateRange` values are replaced with `{ offset: 0, count: -1 }`; mapped MeshBasic and MeshStandard keep authored `updateRange`; interleaved geometries keep authored `updateRange`; collider meshes keep authored `updateRange`; a visual that shares a material with a collider, or a geometry with a mapped mesh, stays unpinned; `mesh.visible` is not pinned; L4 `tryUse` closed→unlatched→open and L5 `tryDriveFastener` still mutate pivot / fastener transforms. Packaged ingest of a mock `lod*` fixture pins DCC leftover `updateRange` on a color-only MeshBasic visual and on the fastener; fail-soft (no lod groups) still pins those meshes. Mapped MeshBasic and collider meshes stay authored. `skinIndex` / `skinWeight` on the pinned visual stay absent (v0.89); `drawRange` stays the object the v0.88 helper already pinned; `groups` stay the array the v0.87 helper already cleared.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v0.89.0 set. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub stay as they were. The v0.90 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz / 72 fallback are **requested**, not measured. Pinning leftover BufferAttribute `updateRange` to `{ offset: 0, count: -1 }` on the 13 packed color-only unlit MeshBasic visual geometries is the intended delta (keeps a truncated legacy `count` off color-only stand-ins). The r170 upload path reads `updateRanges`, which this pulse does not rewrite. Headset ms still **TODO**.
