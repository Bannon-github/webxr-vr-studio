# crate-toolbox v0.79.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v0.78 deleted leftover own-property Material `onBeforeCompile` / `onBeforeRender` so the r170 Material.prototype empty no-ops remain on the **3** shared packed color-only unlit MeshBasic materials (and after v0.77 cleared Mesh `onBeforeRender` / `onAfterRender` on the **13** visual meshes, and after v0.76 cleared Mesh `customDepthMaterial` / `customDistanceMaterial`, and after v0.49 pinned Mesh `castShadow`/`receiveShadow` false and v0.59 pinned Material `shadowSide = null`), **`pinColorOnlyUnlitBasicShadowCallbacks` / `pinColorOnlyVisualShadowCallbacks` delete leftover own-property `onBeforeShadow` / `onAfterShadow` so the r170 Object3D prototype empty no-ops remain on packed color-only unlit MeshBasic visual meshes** (body LOD leaves + lid/latch/tool + fastener). Procedural create runs the helper after `pinColorOnlyVisualRenderCallbacks`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent materials/meshes or replacement callbacks). Does **not** invent replacement callbacks. Does **not** assign `undefined` (WebGLShadowMap always invokes these when a mesh is selected for a shadow-map pass). Does **not** enable `castShadow` / `receiveShadow`. Does **not** touch Mesh `onBeforeRender` / `onAfterRender` (v0.77). Does **not** touch Material `onBeforeCompile` / `onBeforeRender` (v0.78). Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears. Does **not** merge meshes across `body` / `lidPivot` / `latchPivot` / `tool`, fold the fastener into an LOD batch (`fastener` / `fastenerMesh` stay outside `LOD_MERGE_SKIP_NAMES`), rewrite material type, invent a GLB, change matrix freeze, change raycast no-ops, change fog/toneMapped/opaque FrontSide/blending/alpha/wireframe/depth/colorWrite/polygonOffset/stencil/clipping/alphaHash/forceSinglePass/NormalBlending companions/vertexColors/precision/shadowSide/visible/envMap companions/map-intensity companions/wireframeLinewidth/wireframe line-style/envMapRotation/blendColor/blendAlpha/dithering/A2C/polygonOffset companions/stencil companions/shadow/frustumCulled/renderOrder/layers/matrixWorldAutoUpdate/up/scale/rotation.order/customDepth/Distance/Mesh render-callback/Material compile-render-callback pins, or change L4/L5 interaction. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v0.78 merge → weld → unused-attr strip → Uint16 compact → shared MeshBasic → Float16 → upload-release → matrix-freeze → raycast-disable → fog/toneMapped-pin → opaque-FrontSide-pin → shadow-flag-pin → frustumCulled-pin → blending/alpha-pin → GPU-state-pin → stencil-pin → clipping-pin → alphaHash-pin → NormalBlending-companions-pin → vertexColors-pin → precision-pin → shadowSide-pin → renderOrder-pin → visible-pin → envMap-companions-pin → map-intensity-companions-pin → wireframeLinewidth-pin → wireframe-line-style-pin → envMapRotation-pin → layers-pin → blendColor-pin → matrixWorldAutoUpdate-pin → up-pin → scale-pin → dithering/A2C-pin → rotation.order-pin → polygonOffset-companions-pin → stencil-companions-pin → customDepth/Distance-clear → Mesh render-callback-clear → Material compile/render-callback-clear pipeline stays intact.
- **Verified r170 API (do not assume):** a fresh `Mesh` / `Object3D` does **not** define `onBeforeShadow` or `onAfterShadow` as own properties (`Object.hasOwn` is false; `REVISION` 170). Both exist as empty no-ops on `Object3D.prototype`. WebGLShadowMap always invokes `object.onBeforeShadow` / `object.onAfterShadow` (no presence check) when a mesh is selected for a shadow-map pass (`castShadow` or `receiveShadow && VSMShadowMap`). Assigning `undefined` would throw. Even with `castShadow === false` / `receiveShadow === false`, leftover own-property stubs should be deleted so the prototype no-ops remain (same leftover-fence rationale as v0.76 customDepth/Distance with shadows already off). This pulse still deletes leftover own-property stubs on color-only unlit stand-in **meshes** so packaged GLBs that leave leftover callbacks get corrected even when they still pass `isColorOnlyUnlitBasic`. A deliberately wrong color-only Mesh with leftover stub functions assigned to those fields is corrected when it still passes `isColorOnlyUnlitBasic`. Do **not** invent replacement callbacks. Do **not** assign `undefined`. Do **not** enable `castShadow` / `receiveShadow`. Do **not** touch Mesh `onBeforeRender` / `onAfterRender`. Do **not** touch Material `onBeforeCompile` / `onBeforeRender`. Do **not** touch `customDepthMaterial` / `customDistanceMaterial`. Do **not** pin `mesh.visible` — LOD visibility uses it. Do not change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha / customDepth/Distance clear / Mesh+Material render-callback clears.
- **Why:** Accidental DCC / packaged GLB leftover own-property Mesh `onBeforeShadow` / `onAfterShadow` on color-only unlit midtone stand-ins become per-shadow-draw JS work on Quest Browser / TBDR if a light later has `castShadow`, because r170 WebGLShadowMap always invokes them when a mesh is selected for a shadow-map pass. Even with shadows already off they are leftover callback noise and can confuse DCC round-trips. Deleting leftover own properties so the r170 Object3D prototype empty no-ops remain keeps the Quest-safe opaque unlit contract beside the existing Mesh render-callback, Material compile/render-callback, customDepth/Distance, castShadow/receiveShadow, and shadowSide fences. Mesh leftover-callback fence, not a geometry pack change. Does **not** invent custom shaders, transparent materials, replacement callbacks, enable shadows, touch Mesh render callbacks or Material compile/render callbacks, touch customDepth/Distance, pin `mesh.visible`, or change `matrixAutoUpdate` / `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins including stencil companions / polygonOffset companions / dithering / A2C / blendColor / blendAlpha.
- **Scope vs v0.77 Mesh render-callback clear / v0.78 Material compile/render-callback clear:** v0.77 hits the **13** packed color-only visual **meshes** with r170 Object3D/Mesh `onBeforeRender` / `onAfterRender` instance absence. v0.78 hits the **3** unique packed color-only visual **materials** with r170 Material `onBeforeCompile` / `onBeforeRender` instance absence. v0.79 hits the same **13** packed color-only visual **meshes** with r170 Object3D/Mesh `onBeforeShadow` / `onAfterShadow` instance absence (the remaining Object3D callback leftover after those two clears). Mapped / lit stay at authored / r170 Mesh defaults. Collider meshes stay untouched. Morph / interleaved skip the same pack-pipeline gate. Meshes whose material is shared with a blocked collider / morph mesh skip the visual pin.
- **Gate (same as pack pipeline):** only `isMesh`. Only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip morph / interleaved geos (conservative). Skip meshes whose material is shared with a blocked collider / morph mesh. Do not change `isColorOnlyUnlitBasic` (map / lightMap / aoMap / specularMap / alphaMap / envMap still must be falsy).
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope (arrays still present at `attachToolboxLod`). After simulated / real GPU upload, live CPU attrBytes on those visuals → **0**; `BufferAttribute.count` (draws / tris / verts) stays. This pulse does not change that envelope.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset) plus unique MeshBasic flags and visual Mesh shadow / frustum / renderOrder / layers / matrixWorldAutoUpdate / up / scale / rotation.order / customDepth/Distance / render-callback / shadow-callback flags:

  | Level | Draws | Tris (index/3) | Unique verts (v0.39 weld) | attrBytes (pre-upload v0.44 Float16) | Unique MeshBasic | fog / toneMapped / opaque FrontSide / blending+alpha / GPU-state / stencil / clipping / alphaHash+forceSinglePass / NormalBlending companions / vertexColors / precision / shadowSide / visible / envMap companions / map-intensity companions / wireframeLinewidth / linecap / linejoin / envMapRotation / blendColor / blendAlpha / dithering / A2C / polygonOffsetFactor / polygonOffsetUnits / stencilRef / stencilWriteMask / stencilFuncMask / stencilZFail / stencilZPass | material-render-callbacks-absent | shadow-off meshes | frustumCulled-on meshes | renderOrder-0 meshes | layers-default meshes | matrixWorldAutoUpdate-on meshes | up-default meshes | scale-default meshes | rotation-order-XYZ meshes | customDepth/Distance-absent meshes | render-callbacks-absent meshes | shadow-callbacks-absent meshes |
  | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | **false / false / yes / yes / yes / off / null / off / SrcAlpha / false / null / null / true / Multiply / 1 / 0.98 / 1 / 1 / 1 / round / round / (0,0,0) / (0,0,0) / 0 / false / false / 0 / 0 / 0 / 0xff / 0xff / Keep / Keep** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | **false / false / yes / yes / yes / off / null / off / SrcAlpha / false / null / null / true / Multiply / 1 / 0.98 / 1 / 1 / 1 / round / round / (0,0,0) / (0,0,0) / 0 / false / false / 0 / 0 / 0 / 0xff / 0xff / Keep / Keep** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | **false / false / yes / yes / yes / off / null / off / SrcAlpha / false / null / null / true / Multiply / 1 / 0.98 / 1 / 1 / 1 / round / round / (0,0,0) / (0,0,0) / 0 / false / false / 0 / 0 / 0 / 0xff / 0xff / Keep / Keep** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | **false / false / yes / yes / yes / off / null / off / SrcAlpha / false / null / null / true / Multiply / 1 / 0.98 / 1 / 1 / 1 / round / round / (0,0,0) / (0,0,0) / 0 / false / false / 0 / 0 / 0 / 0xff / 0xff / Keep / Keep** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | | | | | | | | | | | | |

  Unique procedural MeshBasic instances stay **3**, all `fog === false` && `toneMapped === false` && `transparent === false` && `opacity === 1` && `depthWrite === true` && `depthTest === true` && `side === FrontSide` && `blending === NormalBlending` && `premultipliedAlpha === false` && `alphaTest === 0` && `wireframe === false` && `colorWrite === true` && `depthFunc === LessEqualDepth` && `polygonOffset === false` && `polygonOffsetFactor === 0` && `polygonOffsetUnits === 0` && `stencilWrite === false` && `stencilFunc === AlwaysStencilFunc` && `stencilRef === 0` && `stencilWriteMask === 0xff` && `stencilFuncMask === 0xff` && `stencilFail === KeepStencilOp` && `stencilZFail === KeepStencilOp` && `stencilZPass === KeepStencilOp` && `clippingPlanes === null` && `clipIntersection === false` && `clipShadows === false` && `alphaHash === false` && `forceSinglePass === false` && `blendSrc === SrcAlphaFactor` && `blendDst === OneMinusSrcAlphaFactor` && `blendEquation === AddEquation` && `blendSrcAlpha === null` && `blendDstAlpha === null` && `blendEquationAlpha === null` && `vertexColors === false` && `precision === null` && `shadowSide === null` && `visible === true` && `combine === MultiplyOperation` && `reflectivity === 1` && `refractionRatio === 0.98` && `lightMapIntensity === 1` && `aoMapIntensity === 1` && `wireframeLinewidth === 1` && `wireframeLinecap === 'round'` && `wireframeLinejoin === 'round'` && `envMap === null` && `envMapRotation.x === 0` && `envMapRotation.y === 0` && `envMapRotation.z === 0` && `blendColor.r === 0` && `blendColor.g === 0` && `blendColor.b === 0` && `blendAlpha === 0` && `dithering === false` && `alphaToCoverage === false`. Unique color-only MeshBasic material-render-callbacks-absent stay **3** (`!Object.hasOwn(onBeforeCompile)` && `!Object.hasOwn(onBeforeRender)` && both resolve to `Material.prototype` empty no-ops). Visual MeshBasic shadow-callbacks-absent **13** (`!Object.hasOwn(onBeforeShadow)` && `!Object.hasOwn(onAfterShadow)` && both resolve to `Object3D.prototype` empty no-ops). Visual MeshBasic render-callbacks-absent stay **13** (`!Object.hasOwn(onBeforeRender)` && `!Object.hasOwn(onAfterRender)` && both resolve to `Object3D.prototype` empty no-ops). Visual MeshBasic customDepth/Distance-absent stay **13** (`customDepthMaterial == null` && `customDistanceMaterial == null`). Visual MeshBasic rotation-order-XYZ stay **13** (`rotation.order === 'XYZ'`). Visual MeshBasic scale-default stay **13** (`scale.x === 1` && `scale.y === 1` && `scale.z === 1`). Visual MeshBasic up-default stay **13** (`up.x === 0` && `up.y === 1` && `up.z === 0`). Visual MeshBasic matrixWorldAutoUpdate-on stay **13** (`matrixWorldAutoUpdate === true`). Visual MeshBasic layers-default stay **13** (`layers.mask === 1` && `layers.isEnabled(0)`). Visual MeshBasic renderOrder-0 stay **13** (`renderOrder === 0`). Visual MeshBasic frustumCulled-on stay **13** (`frustumCulled === true`). Visual MeshBasic shadow-off stay **13** (`castShadow === false` && `receiveShadow === false`). Visual MeshBasic raycast-off stay **13**. Frozen MeshBasic stay **3** (body LOD0/1/2). Live MeshBasic stay **10** (lid/latch/tool LOD meshes + fastener). `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes / matrix freeze / raycast-off / unique MeshBasic / shadow-off / frustumCulled-on / renderOrder-0 / layers-default / matrixWorldAutoUpdate-on / up-default / scale-default / rotation-order-XYZ / customDepth/Distance-absent / render-callbacks-absent / material-render-callbacks-absent unchanged vs v0.78. After simulated upload, CPU attrBytes on those 13 visual geos → 0.
- **Unit/mock evidence** (not headset): r170 defaults `!Object.hasOwn(new Mesh(), 'onBeforeShadow')` / `!Object.hasOwn(new Mesh(), 'onAfterShadow')` and both resolve to `Object3D.prototype` empty no-ops (`REVISION` 170); WebGLShadowMap always invokes `object.onBeforeShadow` / `object.onAfterShadow` when a mesh is selected for a shadow-map pass (assigning `undefined` would throw); a wrong color-only Mesh with leftover stub functions assigned to `onBeforeShadow` / `onAfterShadow` is corrected by `pinColorOnlyUnlitBasicShadowCallbacks` without inventing replacement callbacks, assigning `undefined`, enabling shadows, touching Mesh `onBeforeRender` / `onAfterRender`, touching Material `onBeforeCompile` / `onBeforeRender`, or changing fog / stencil / dithering / A2C / customDepth/Distance; prior pins including material-render-callbacks-absent **3**, render-callbacks-absent **13**, customDepth/Distance-absent **13**, rotation-order-XYZ **13**, scale-default **13**, up-default **13**, matrixWorldAutoUpdate-on **13**, layers-default **13**, renderOrder-0 **13**, frustumCulled-on **13**, and shadow-off **13** stay intact; procedural wood/brass/steel stay Quest-safe; mapped / lit MeshBasic stay authored; collider meshes stay authored / default; `mesh.visible` is not pinned (LOD still uses group/mesh visibility); L4 `tryUse` closed→unlatched→open and L5 `tryDriveFastener` still mutate pivot / fastener transforms. Packaged ingest of a mock `lod*` fixture pins body + lid/latch/tool + fastener meshes including a DCC leftover stub `onBeforeShadow` / `onAfterShadow` color-only Mesh; fail-soft (no lod groups) still pins those meshes. Mapped MeshBasic stays authored.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v0.78.0 set. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub stay as they were. The v0.79 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz / 72 fallback are **requested**, not measured. Clearing leftover own-property Mesh `onBeforeShadow` / `onAfterShadow` so the r170 Object3D prototype empty no-ops remain is the intended delta; headset ms still **TODO**.
