# crate-toolbox v1.15.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v1.14.0 deleted leftover Material `defaultAttributeValues` so the r170 MeshBasic absence remains (`material.defaultAttributeValues === undefined` and `Object.hasOwn(material, 'defaultAttributeValues') === false`) on the **3** shared color-only MeshBasic materials (defaultAttributeValues-absent **3**; index0AttributeName-absent **3**; depthPacking-absent **3**; extensions-absent **3**; indirect-null **13**; unusedAttributes-absent **13**; onUpload-release **13**; colorAttribute-absent **13**; matrixWorldNeedsUpdate-false **13**; material-version-zero **3**; mesh-userData-empty **13**; mesh-name-empty **10**), **`pinColorOnlyUnlitBasicMaterialUniforms` / `pinColorOnlyVisualMaterialUniforms` delete leftover Material `uniforms` so the r170 MeshBasic absence remains (`material.uniforms === undefined` and `Object.hasOwn(material, 'uniforms') === false`)** on those same **3** shared color-only MeshBasic materials (wood / brass / steel). Procedural create runs the visual helper after `pinColorOnlyVisualMaterialDefaultAttributeValues`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes). Fail-soft packaged paths (no `lod*` groups) still run the visual pin, including the fastener. Prefer `delete material.uniforms` when the own property is present. An already-absent `uniforms` is left alone. Pin once per shared material instance. Does **not** assign `null`, `undefined`, or `{}`. Does **not** invent replacement uniforms. Does **not** convert MeshBasic to ShaderMaterial. Does **not** clear `uniforms` on real ShaderMaterial / RawShaderMaterial. Does **not** touch `defaultAttributeValues` (v1.14.0 defaultAttributeValues-absent stays). Does **not** touch `index0AttributeName` (v1.13.0 index0AttributeName-absent stays). Does **not** touch `depthPacking` (v1.12.0 depthPacking-absent stays). Does **not** touch `extensions` (v1.11.0 extensions-absent stays). Does **not** touch `indirect` (v1.10.0 indirect-null stays). Does **not** re-run the unused-channel strip (v1.9.0 unusedAttributes-absent stays). Does **not** touch `onUpload` / `onUploadCallback` (v1.8.0 onUpload-release stays). Does **not** touch `color` (v1.7.0 colorAttribute-absent stays). Does **not** touch Material `version` / `name` / `userData`. Does **not** touch Mesh / Object3D `userData` / `name` (reserved names `lidMesh` / `latchMesh` / `fastenerMesh` and any `collider_*` stay). Does **not** touch Mesh `matrixWorldNeedsUpdate` (v1.6.0 stays). Does **not** change `matrixAutoUpdate` or `matrixWorldAutoUpdate`. Does **not** touch BufferGeometry `userData` / `name`. Does **not** touch BufferAttribute fields. Does **not** touch prior Material program-cache / flag pins. Does **not** touch `uniformsGroups`, `uniformsNeedUpdate`, `vertexShader`, `fragmentShader`, `lights`, `clipping`, or `isShaderMaterial`. Does **not** touch bounds, morphs, animations, shadows, `frustumCulled`, or `mesh.visible`. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v1.14.0 pipeline stays intact; v1.15.0 adds the uniforms pin after the defaultAttributeValues pin.
- **Checked installed three@0.170.0:** fresh `Material` / `MeshBasicMaterial` constructors do not assign `uniforms` (`uniforms === undefined` and `Object.hasOwn` is false). `ShaderMaterial` assigns `this.uniforms = {}` and `copy` clones uniforms via `cloneUniforms(source.uniforms)`. `RawShaderMaterial` extends `ShaderMaterial`, so it keeps that constructor `uniforms` object. `WebGLPrograms.getUniforms` uses `material.uniforms` only when `shaderIDs[material.type]` is absent (custom ShaderMaterial / RawShaderMaterial path). MeshBasic resolves shaderID `'basic'` and clones `ShaderLib` uniforms instead, so a leftover `uniforms` object is not the active upload map — but it still retains heap (Texture / Vector / Color uniform values).
- **Why:** A leftover `uniforms` object on a color-only MeshBasic (DCC / mistaken ShaderMaterial assignment) is not the active upload map. `WebGLPrograms.getUniforms` uses `material.uniforms` only when `shaderIDs[material.type]` is absent. MeshBasic resolves shaderID `'basic'` and clones `ShaderLib` uniforms instead. The leftover object still retains Texture / Vector / Color uniform values on Quest 3's tight memory budget and can confuse ingest/debug. An empty `{}`, `null`, or an own `undefined` is still an own property, which is not the r170 MeshBasic absence. Deleting the own property restores the r170 absence and is load-time packaging. On clean procedural materials `uniforms` is already absent, so draws, tris, and attrBytes stay unchanged vs v1.14.0. This pulse does not invent headset milliseconds. 90 Hz ship (~11.1 ms) / 72 Hz fallback are **requested**, not measured.
- **Scope vs v1.14.0 defaultAttributeValues pin:** v1.14.0 hits the **3** shared color-only MeshBasic **materials** and deletes a leftover own `defaultAttributeValues` property (defaultAttributeValues-absent **3**). v1.15.0 hits those same **3** materials and deletes a leftover own `uniforms` property. Measured unique materials stay **3**. defaultAttributeValues-absent stays **3**. index0AttributeName-absent stays **3**. depthPacking-absent stays **3**. extensions-absent stays **3**. indirect-null stays **13**. unusedAttributes-absent stays **13**. onUpload-release stays **13**. colorAttribute-absent stays **13**. matrixWorldNeedsUpdate-false stays **13**. material-version-zero stays **3**. material-name-empty stays **3**. material-userData-empty stays **3**. geometry-userData-empty stays **13**. geometry-name-empty stays **13**. mesh-name-empty stays **10**. mesh-userData-empty stays **13**. Mapped / lit / interleaved keep authored `uniforms`. Collider meshes stay untouched. ShaderMaterial / RawShaderMaterial keep constructor `uniforms`. A mesh whose material is shared with a blocked collider or interleaved mesh, or whose geometry is shared with a collider, an interleaved mesh, or a mapped / lit mesh, skips the visual pin.
- **Gate (same `isColorOnlyUnlitBasic` as `pinColorOnlyVisualMaterialDefaultAttributeValues`):** only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip interleaved geos. Skip materials shared with a blocked collider / interleaved mesh. Skip geometries shared with a collider, an interleaved mesh, or a mapped / lit mesh. Keep `lidMesh` / `latchMesh` / `fastenerMesh` names, their empty `mesh.userData`, and `defaultAttributeValues` absent; still delete leftover `uniforms` on the shared materials those reserved-name visuals use.
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope. `uniforms` is a Material field, not an entry in `geometry.attributes`, so deleting it does not change attribute byte length. `uniforms-absent` counts the 3 unique color-only MeshBasic materials whose `uniforms` is absent after the pin (wood / brass / steel; brass is shared with the fastener). `countGroupStats` does not read `uniforms` on clean procedural meshes, so the pin does not change the draws/tris/verts/attrBytes envelope. L4 `tryUse` / `applyActivityVisual` and L5 `tryDriveFastener` / `applyFastenerVisual` write rotation and position; they do not assign `uniforms` and they do not rename reserved meshes. The measured count after the existing L4/L5 path stays **3**.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset):

  | Level | Draws | Tris | Unique verts | attrBytes (pre-upload) | Unique MeshBasic | defaultAttributeValues-absent | uniforms-absent |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | wood + brass + steel | wood + brass + steel |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | same wood + same brass | same wood + same brass |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | same wood | same wood |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | same brass | same brass |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | |

  Unique procedural MeshBasic instances stay **3**. Packed color-only visual uniforms-absent **3**. Packed color-only visual defaultAttributeValues-absent stay **3**. Packed color-only visual index0AttributeName-absent stay **3**. Packed color-only visual depthPacking-absent stay **3**. Packed color-only visual extensions-absent stay **3**. Packed color-only visual indirect-null stay **13**. Packed color-only visual unusedAttributes-absent stay **13**. Packed color-only visual onUpload-release stay **13**. Packed color-only visual colorAttribute-absent stay **13**. Packed color-only visual matrixWorldNeedsUpdate-false stay **13**. Unique color-only MeshBasic material-version-zero stay **3**. Unique color-only MeshBasic material-name-empty stay **3**. Unique color-only MeshBasic material-userData-empty stay **3**. Packed color-only visual geometry-userData-empty stay **13**. Packed color-only visual geometry-name-empty stay **13**. Packed color-only visual mesh-name-empty stay **10**. Packed color-only visual mesh-userData-empty stay **13**. `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes unchanged vs v1.14.0 on clean procedural meshes. uniforms-absent **3** is the new count.

- **Unit/mock evidence** (not headset): installed three@0.170.0 `Material` / `MeshBasicMaterial` constructors leave `uniforms` absent. `ShaderMaterial` assigns `this.uniforms = {}` and `copy` clones via `cloneUniforms(source.uniforms)`. `RawShaderMaterial` keeps that constructor map. `WebGLPrograms.getUniforms` uses `material.uniforms` only when `shaderIDs[material.type]` is absent; MeshBasic resolves `'basic'` and clones `ShaderLib` uniforms. A color-only MeshBasic with a leftover own `uniforms` (ShaderMaterial-style map, empty `{}`, `null`, or an own `undefined`) is cleared by `pinColorOnlyUnlitBasicMaterialUniforms` via one `delete` without replacing the mesh, the material, the geometry, `position`, the index, `defaultAttributeValues`, `index0AttributeName`, `depthPacking`, `extensions`, or `indirect`. An already-absent `uniforms` does not call `delete`. The defaultAttributeValues pin, the index0AttributeName pin, the depthPacking pin, the extensions pin, and the indirect pin do not delete `uniforms`. Mapped MeshBasic, MeshStandard, ShaderMaterial, RawShaderMaterial, interleaved meshes, collider meshes, and visuals that share a blocked material or geometry keep authored `uniforms`. Packaged ingest of a mock `lod*` fixture deletes color-only survivors, reserved `lidMesh` / `latchMesh`, and the fastener `uniforms`, and leaves mapped / lit / ShaderMaterial / RawShaderMaterial / interleaved / collider / shared-geometry `uniforms` authored. Fail-soft (no lod groups) deletes the body and fastener `uniforms`, does not invent lod groups, and keeps `lidMesh` / `latchMesh` / `fastenerMesh`. L4 `tryUse` and L5 `tryDriveFastener` still mutate pivot / fastener transforms; uniforms-absent stays **3**, defaultAttributeValues-absent stays **3**, index0AttributeName-absent stays **3**, depthPacking-absent stays **3**, extensions-absent stays **3**, indirect-null stays **13**, unusedAttributes-absent stays **13**, material-version-zero stays **3**, mesh-userData-empty stays **13**, and mesh-name-empty stays **10**.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v1.14.0 set. The v1.15.0 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz ship (~11.1 ms) / 72 Hz fallback are **requested**, not measured. Deleting leftover Material `uniforms` on the 3 shared color-only unlit MeshBasic materials is the intended delta. Headset ms still **TODO**. Do not require 207/240 Hz.
