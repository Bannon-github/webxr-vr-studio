# crate-toolbox v1.2.0

L3 packaging/perf UPGRADE on the same `objectId`. No GLB. Not a new layer.

- After v1.1.0 pinned leftover Material `userData` to the r170 constructor default empty plain object (`material.userData = {}`) on the **3** shared color-only MeshBasic materials (wood / brass / steel; and after the v1.0.0 BufferGeometry `userData` pin, the v0.99 BufferGeometry `name` pin, the v0.98 BufferAttribute `version` pin, the v0.97 attribute `name` pin, the v0.96 `gpuType` pin, the v0.95 `normalized` pin, the v0.94 `usage` pin, the v0.93 Mesh / Object3D `boundingSphere` clear, the v0.92 BufferGeometry bounds pin, the v0.91 `updateRanges` clear, and the v0.90 `updateRange` pin), **`pinColorOnlyUnlitBasicMaterialName` / `pinColorOnlyVisualMaterialName` pin leftover Material `name` to the r170 constructor default empty string (`material.name === ''`)** on those same shared materials. Procedural create runs the visual helper after `pinColorOnlyVisualMaterialUserData`. Packaged ingest pins after the same detect (does not hex-dedupe; does not invent meshes). Fail-soft packaged paths (no `lod*` groups) still run the visual pin, including the fastener. Assign `material.name = ''` in place only when it is not already `''`. An already-empty name is left as-is. Does **not** replace the material, the geometry, the attributes, or the typed arrays. Does **not** touch Material `userData` (v1.1.0 material-userData-empty stays). Does **not** touch prior material program-cache / flag pins (`glslVersion` / `flatShading` / `defines` / `customProgramCacheKey` / `onBeforeCompile` / `onBeforeRender` / `fog`). Does **not** touch BufferGeometry `userData` (v1.0.0 geometry-userData-empty stays). Does **not** touch BufferGeometry `name` (v0.99 geometry-name-empty stays). Does **not** touch Mesh / Object3D `userData` (studio metadata: collider, lod, parts, fastener, and the rest stay). Does **not** touch BufferAttribute `version` (v0.98 version-zero stays). Does **not** touch BufferAttribute `name` (v0.97 name-empty stays). Does **not** touch `gpuType` (v0.96 gpuType-float stays). Does **not** call `setUsage` (v0.94 usage-static stays). Does **not** reassign `normalized` (v0.95 normalized-default stays). Does **not** touch `onUpload` / `onUploadCallback` (v0.43 CPU-release hook stays). Does **not** touch `updateRange` (v0.90). Does **not** touch `updateRanges` (v0.91). Does **not** touch geometry `boundingBox` / `boundingSphere` (v0.92). Does **not** touch Mesh `boundingSphere` (v0.93). Does **not** delete `skinIndex` / `skinWeight` (v0.89). Does **not** touch `drawRange` (v0.88). Does **not** touch `groups` (v0.87). Does **not** touch `morphAttributes` / `morphTargetsRelative` (v0.86). Does **not** touch Mesh `morphTargetInfluences` / `morphTargetDictionary` (v0.85). Does **not** touch Object3D `animations` (v0.84). Does **not** touch Mesh `onBeforeRender` / `onAfterRender` / `onBeforeShadow` / `onAfterShadow`. Does **not** touch `customDepthMaterial` / `customDistanceMaterial`. Does **not** enable `castShadow` / `receiveShadow`. Does **not** pin `mesh.visible` (LOD visibility uses it). Does **not** rename the Mesh (`lidMesh` / `latchMesh` / `fastenerMesh` stay). Does **not** change `frustumCulled` (v0.50 already pins `true`). Does **not** change `matrixAutoUpdate` (v0.45 already freezes `matrixAutoUpdate` on static body LOD leaves after one `updateMatrixWorld(true)`; lid/latch/tool/fastener stay live). Does **not** change `matrixWorldAutoUpdate` / `layers` / `up` / `scale` / `rotation.order` / prior material pins. Does **not** merge meshes across `body` / `lidPivot` / `latchPivot` / `tool`, fold the fastener into an LOD batch (`fastener` / `fastenerMesh` stay outside `LOD_MERGE_SKIP_NAMES`), rewrite material type, invent a GLB, change matrix freeze, change raycast no-ops, or change L4/L5 interaction. Named meshes `lidMesh` / `latchMesh` / `fastenerMesh` kept. The v0.37–v1.1.0 merge → weld → unused-attr strip → Uint16 compact → shared MeshBasic → Float16 → upload-release → matrix-freeze → raycast-disable → material-flag → Mesh-pin → animations-clear → morph-target-clear → morph-attribute-clear → groups-clear → drawRange-pin → skin-attribute-strip → updateRange-pin → updateRanges-clear → bounds-null → mesh-boundingSphere-absent → usage-static → normalized-default → gpuType-float → name-empty → version-zero → geometry-name-empty → geometry-userData-empty → material-userData-empty pipeline stays intact; v1.2.0 adds the Material `name` pin after the material-userData pin.
- **Checked installed three@0.170.0:** the Material constructor assigns `this.name = ''`. `MeshBasicMaterial` does not override `name`. `Material.toJSON` writes `data.name` only when `this.name !== ''`. `Material.copy` copies `source.name`. `MaterialLoader` assigns `material.name = json.name` when `json.name !== undefined`. `ObjectLoader.parseMaterials` uses that loader. Stock `GLTFLoader` `loadMaterial` assigns `material.name = materialDef.name` when `materialDef.name` is set, then calls `assignExtrasToUserData`. `WebGLRenderer` does not read `material.name`. `WebGLPrograms.getParameters` copies `shaderName: material.name`, and `WebGLProgram` emits `#define SHADER_NAME` from that parameter. `getProgramCacheKey` does not include `shaderName`, so a leftover name does not fork the program cache or change the draw. v1.1.0 pinned `material.userData` and left `material.name` alone, so DCC / glTF material names can still sit on the shared color-only MeshBasics. This pulse pins the leftover. It does **not** replace the material, geometry, attributes, or typed arrays. It does **not** touch Material `userData`. It does **not** touch BufferGeometry `userData` or `name`. It does **not** touch Mesh / Object3D `userData`. It does **not** touch BufferAttribute `version` / `name` / `gpuType`. It does **not** call `setUsage`. It does **not** reassign `normalized`. It does **not** touch `onUpload` / `onUploadCallback`. It does **not** touch `updateRange` or `updateRanges`. It does **not** touch geometry bounds or Mesh `boundingSphere`. It does **not** change `frustumCulled`. It does **not** rename the Mesh. Mapped / lit stay authored because `isColorOnlyUnlitBasic` is false when `map` / `lightMap` / `aoMap` / `specularMap` / `alphaMap` / `envMap` is set.
- **Why:** Stock GLTFLoader copies `materialDef.name` onto `material.name`. After the v1.1.0 userData pin, DCC / glTF names can still sit on the shared static packed color-only MeshBasics. `WebGLRenderer` does not read `material.name`. The shader prefix `#define SHADER_NAME` is a debug label and is not part of the program cache key, so leftover names do not change the draw but retain strings on Quest 3 TBDR static props. Clearing to `''` is load-time L3 packaging only: no draw / tri / attrBytes change. These stand-ins keep float-typed position (+ index); `vertexColors` stays false (v0.57) and unused color/uv/normal/skin attrs are already stripped on the clean path. Pinning an empty `material.name` restores the r170 constructor default. Pre-upload draws / tris / verts / attrBytes on clean procedural meshes are unchanged vs v1.1.0. This pulse does not invent headset milliseconds.
- **Scope vs v1.1.0 material-userData pin:** v1.1.0 hits the **3** unique color-only MeshBasic **materials** so each `material.userData` is an empty plain object. v1.2.0 hits those same **3** materials so each `material.name === ''`. Measured unique materials stay **3**. Fresh r170 `Material` already uses `name = ''`. Mapped / lit `material.name` stays authored. Interleaved materials stay authored. A mesh whose material is shared with a blocked collider or interleaved mesh, or whose geometry is shared with a collider, an interleaved mesh, or a mapped / lit mesh, skips the visual pin. Color-only leftover non-empty `material.name` is pinned rather than kept: that leftover is the pulse.
- **Gate (same `isColorOnlyUnlitBasic` as `pinColorOnlyVisualMaterialUserData`):** only color-only unlit MeshBasic. Skip `userData.collider` / `collider_*` names. Skip mapped / lit. Skip interleaved geos (same interleaved half of the material-userData pin). Skip materials shared with a blocked collider / interleaved mesh. Skip geometries shared with a collider, an interleaved mesh, or a mapped / lit mesh. Do not change `isColorOnlyUnlitBasic` (map / lightMap / aoMap / specularMap / alphaMap / envMap still must be falsy).
- **Measurement rule:** `userData.lod.stats.attrBytes` is the **pre-upload** CPU envelope (arrays still present at `attachToolboxLod`). After simulated / real GPU upload, live CPU attrBytes on those visuals → **0**; `BufferAttribute.count` (draws / tris / verts) stays. Assigning `material.name = ''` does not change attribute byte length. `material-name-empty` counts unique color-only MeshBasic materials whose `material.name === ''` before upload — the same unique-material set as `material-userData-empty` / `glslVersion-absent` / `flatShading-off` / `defines-absent`. `material-userData-empty` still counts unique color-only MeshBasics whose `material.userData` is an empty plain object. `geometry-userData-empty` still counts visual meshes whose `geometry.userData` is an empty plain object. `geometry-name-empty` still counts visual meshes whose `geometry.name === ''`. `version-zero` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `version === 0`. `name-empty` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `name === ''`. `gpuType-float` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `gpuType === FloatType`. `normalized-default` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `normalized === false`. `usage-static` still counts visual meshes whose non-interleaved BufferAttributes and index BufferAttribute all have `usage === StaticDrawUsage`. `mesh-boundingSphere-absent` still counts visual meshes whose `boundingSphere === undefined`. `bounds-null` still counts visual meshes whose geometry has `boundingBox === null` && `boundingSphere === null` before upload. Unique materials were measured, not assumed: **13** meshes, **3** distinct `MeshBasicMaterial` instances, **3** empty `material.name` strings. Wood meshes share one material; brass (including the fastener) shares another; steel has its own. `countGroupStats` counts meshes, tris, verts, and attribute bytes, not `material.name`, so the pin does not change the draws/tris/verts/attrBytes envelope.
- **Measured** `userData.lod.stats` (Three.js index counts + `position.count` + attribute/index byte length via `countGroupStats`; colliders skipped; unit tests, **not** headset):

  | Level | Draws | Tris | Unique verts | attrBytes (pre-upload) | Unique MeshBasic | material-name-empty | material-userData-empty | geometry-userData-empty | geometry-name-empty | version-zero | name-empty | gpuType-float | normalized-default | usage-static | mesh-boundingSphere-absent | bounds-null |
  | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
  | LOD0 | **6** | **240** | **230** | **2820** | wood + brass + steel | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD1 | **4** | **96** | **100** | **1176** | same wood + same brass | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | LOD2 | **2** | **24** | **48** | **432** | same wood | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | Fastener (not LOD) | 1 | 12 | 24 | **216** | same brass | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** | **yes** |
  | `drawCallsEstimate` (LOD0 + fastener) | **7** | | | | | | | | | | | | | | | |

  Unique procedural MeshBasic instances stay **3**. Unique color-only MeshBasic material-name-empty **3** (`material.name === ''`). Unique color-only MeshBasic material-userData-empty stay **3** (empty plain `material.userData`, one object per material). Visual MeshBasic geometry-userData-empty stay **13**. Visual MeshBasic geometry-name-empty stay **13**. Visual MeshBasic version-zero stay **13**. Visual MeshBasic name-empty stay **13**. Visual MeshBasic gpuType-float stay **13**. Visual MeshBasic normalized-default stay **13**. Visual MeshBasic usage-static stay **13**. Visual MeshBasic mesh-boundingSphere-absent stay **13**. Visual MeshBasic bounds-null stay **13**. `userData.l2.uniqueMaterials` **3**. Draws / tris / unique verts / attrBytes / matrix freeze / raycast-off / unique MeshBasic / material-userData-empty / geometry-userData-empty / geometry-name-empty / version-zero / name-empty / gpuType-float / normalized-default / usage-static / mesh-boundingSphere-absent / bounds-null unchanged vs v1.1.0 on clean procedural meshes. material-name-empty **3** is the new count.

- **Unit/mock evidence** (not headset): installed three@0.170.0 `Material` constructs `name` as `''`; `toJSON` writes `name` only when it is non-empty; `copy` copies `name`; `MaterialLoader` assigns `json.name`; `ObjectLoader` assigns material `name` without copying it onto the Mesh. Installed `GLTFLoader.js` contains `if ( materialDef.name ) material.name = materialDef.name`. Installed `WebGLRenderer.js` does not contain `material.name`. Installed `WebGLPrograms.js` copies `shaderName: material.name`. A wrong color-only MeshBasic with leftover `material.name` is pinned to `''` by `pinColorOnlyUnlitBasicMaterialName` without replacing the mesh, the material, the geometry, the attributes, or the typed arrays, without replacing `material.userData`, without touching `glslVersion` / `flatShading` / `defines` / `fog` / `onBeforeCompile` / `onBeforeRender` / `customProgramCacheKey`, without touching BufferGeometry `userData` or `name`, without touching Mesh / Object3D `userData`, without touching BufferAttribute `version` / `name` / `gpuType` / `normalized` / non-static `usage`, without replacing `onUploadCallback`, without calling `computeBoundingBox` / `computeBoundingSphere`, without touching geometry `boundingBox` / `boundingSphere`, without deleting Mesh `boundingSphere`; a deleted `material.name`, `null`, and a non-string are each replaced with `''`; an already-empty `material.name` is left as `''`; two meshes that share one material receive one name clear; a material array keeps authored names; mapped MeshBasic and MeshStandard keep authored `material.name`; interleaved materials keep authored `material.name`; collider meshes keep authored `material.name` and collider `userData`; a visual that shares a material with a collider, or a geometry with a mapped mesh, stays unpinned; `mesh.visible` is not pinned; the Mesh name is not cleared (`lidMesh` / `latchMesh` / `fastenerMesh` stay); `frustumCulled` stays true; L4 `tryUse` closed→unlatched→open and L5 `tryDriveFastener` still mutate pivot / fastener transforms. Packaged ingest of a mock `lod*` fixture pins a DCC leftover `material.name` on a color-only MeshBasic visual and on the fastener while the v1.1.0 userData pin still clears leftover non-empty `material.userData` and the v1.0.0 geometry pin still clears leftover non-empty `geometry.userData`; fail-soft (no lod groups) still pins those meshes, including a deleted `material.name`, leaves an already-empty `material.name` empty, and leaves collider material `name` plus Mesh `userData` authored. Mapped MeshBasic and collider meshes stay authored.
- Named meshes required by tests stay: `lidMesh`, `latchMesh`, `fastenerMesh`. Unique canvases stay **0**. Color-only unlit MeshBasic on LOD0 / LOD1 / LOD2. `setToolboxLod` is still visibility-only. Geometry, collider names, and L4/L5 activity are the v0.4.0–v1.1.0 set. Session present-path chain (v0.15–v0.22) and v0.8 allocation scrub stay as they were. The v1.2.0 `behavior.json` sidecar is frozen next to this manifest (`source.behavior`) so the revision resolves without reading a later current sidecar.
- **KTX2 / Basis** remains the packaging step when a DCC GLB lands. No GLB in this revision.
- Quest 3 frame time / FFR still **unmeasured**. Do not treat this pulse as a 90 Hz pass. 90 Hz / 72 fallback are **requested**, not measured. Pinning leftover Material `name` to `''` on the 3 shared color-only unlit MeshBasic materials is the intended delta. Headset ms still **TODO**. Do not require 207/240 Hz.
