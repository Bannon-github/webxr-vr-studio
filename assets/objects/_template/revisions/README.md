# Revisions

Copy the shipped `manifest.json`, `behavior.json`, and `current.glb` (if any) into `vX.Y.Z/` **before** overwriting current files.

If the binary is not in git, add `POINTER.md` with an immutable URL instead of a missing file.
